import { createAdminClient } from "@civitics/db";
export const dynamic = "force-dynamic";
export async function GET() {
  const db = createAdminClient();
  const { data } = await db.rpc("chord_industry_flows");
  return Response.json(data);
}
