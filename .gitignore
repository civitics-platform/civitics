
.vscode/
.claude/*
!.claude/agents/
!.claude/agents/**
!.claude/commands/
!.claude/commands/**
!.claude/settings.local.json

node_modules/

.next/

dist/

.env.local

*.env.local

.env*.local

.env.local.*

.turbo/

*.tsbuildinfo

supabase/snippets/
supabase/.branches/
supabase/.temp/
supabase/seed.sql

data.sql

backup.sql

.data.sql

*.data.sql

.backup.sql

*.backup.sql

.prod-data.sql

*.prod-data.sql

.prod-backup.sql

*.prod-backup.sql

dev-local.ps1

dev-prod.ps1

seed.sql.bak
ngrok-policy.yml
ngrok.yml

# Qwen Code workspace
.qwen/

# Schema comparison / migration scratch
.tmp-schema-compare/

# Database dumps / backups (never commit)
backups/

# USASpending bulk pipeline state (tracks last processed archive date for delta runs)
packages/data/.usaspending-bulk-state.json

.vercel

# Pipeline / build / debug logs (e.g. fec-bulk.log was accidentally committed once at a doubled path).
*.log

# Enrichment-drain wave artifacts (see CLAUDE.md "Enrichment Queue Drain — Runbook").
.drain-tmp/


.vercel
.env*
