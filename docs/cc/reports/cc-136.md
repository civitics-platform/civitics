---
cc: 136
prompt: cc-prompt-136-fix1199-scope-pre-push-hook-in-ci-fix1178-diagnosis-entity-tags-delete-reinsert-bloat-local-clone-scratch-tables-db-types-recipe.md
started_at: 2026-09-19T02:07:00Z
finished_at: 2026-09-19T03:05:00Z
head_before: 03edf289
head_after: 0f958f01
commits:
  - {sha: ad623386, subject: "fix(hooks): FIX-1199 — pre-push trunk gate steps aside in a shallow CI checkout"}
  - {sha: 2d0cf2ff, subject: "docs(db): the db:types recipe strips the multi-line graphql_public Constants entry"}
  - {sha: 7b093e5b, subject: "docs(fixes): FIX-1178 / FIX-1199 / FIX-1169 appends; file FIX-1200"}
  - {sha: 0f958f01, subject: "chore(fixes): sync status after FIX-1199"}
fixes_closed: [FIX-1199]
fixes_filed: [FIX-1200]
fixes_reopened: []
migrations_pushed: []
prod_writes: none
ci: green
stopped_items:
  - "FIX-943 append not written — its bullet closed 2026-07-31 and lives in docs/archive/fixes-archive.md; nothing derives status from the archive, so appending there would be writing into a dead file. Substance folded into the FIX-1178 append. Same shape as cc-135's FIX-914 case."
  - "D3 partially scoped out: read 7's patterns (_proof_ref_%, _eq_%) match TEN local tables, not the eleven cc-135 counted. Five further underscore-prefixed tables exist locally; four of them (_drb_chunk_fe, _drb_donor, _drb_fe, _drb_targets) are REAL — they exist on prod and belong in database.ts — and the fifth (_proof_recips) is local-only scratch but outside the names Craig authorised. Dropped exactly the ten; left _proof_recips standing."
  - "Smoke-dispatch run id could not go in item 1's commit body as the prompt asked — the dispatch has to run against a main that already carries the fix, so the id (35415836771) exists only after the commit. Recorded in the FIX-1199 append and here instead."
  - "Prod-side entity_tags compaction (VACUUM FULL / REINDEX) explicitly NOT run — out of scope, and a cc-132 input. It is proposal (b) on FIX-1178 for Craig."
---

# cc-136 — FIX-1199 hook scope; FIX-1178 diagnosis; local-clone hygiene

Posture held: **code + docs + tests, prod READS only.** Every prod contact was a
`SELECT` against catalogs / `data_sync_log`, plus two bare `EXPLAIN`s (no
`ANALYZE`, nothing executed), all through `db-query --prod`, which wraps in
`SET TRANSACTION READ ONLY`. All reads between 02:11 and 02:42 UTC — outside the
05:45–09:00 window. No migration, no prod write, no VACUUM on prod, no hand
firing. Finished and released well before cc-132's 12:00 UTC window.

---

## 1. Read 2 — the hook's callers, and what actually failed

**Callers.** `.githooks/pre-push` is wired by the root `package.json` `prepare`
(`git config core.hooksPath .githooks || true`), which runs on **every**
`pnpm install`, CI included. Twelve of the thirteen workflows run
`pnpm install`. Three of them push:

| workflow | pushes | checkout depth |
|---|---|---|
| `refresh-disposable-emails.yml` | `peter-evans/create-pull-request` → `chore/refresh-disposable-emails` | default (1) |
| `nightly.yml:receipts` (line 581) | `git push origin HEAD:main` | default (1) |
| `audit.yml` (line 62) | `git push` | default (1) |

Only `fixes-integrity.yml` sets `fetch-depth: 0`. Everything else is shallow.

**How the receipts bot passes today: by luck, not by design.** Nightly run
**35403626528** (2026-09-18 23:08 UTC) shows the hook running in full — typecheck
clean, then `fixes:check:trunk` clean, then `7191d90..c65599c HEAD -> main`. But
its counters read `trailersScanned 0` and it reports no completions at all. In a
depth-1 store `git rev-list origin/main` yields exactly one commit, so every
`done.log` SHA fails `git cat-file -e`, classifies `unknown`, and is skipped. The
gate is **inert** and passes for reasons unrelated to trunk.

**The failure text.** Run **33769138373** (2026-09-03, schedule):

```
1 trailer completion(s) skipped — commit not an ancestor of origin/main
  (off-trunk / unmerged): FIX-962|09ab3ab9  FIX-960|aa278b7a  FIX-960|54d9206d

✗ 2 completed FIX-ID(s) backed only by off-trunk commits (not ancestors of
  origin/main) — recorded as done but never shipped:
    FIX-960  (SHAs: 54d9206d, aa278b7a)
    FIX-962  (SHAs: 09ab3ab9)

[pre-push] ✗ fixes:check:trunk failed — push aborted.
error: failed to push some refs to 'https://github.com/civitics-platform/civitics'
```

**This is not what the bullet says, and the real mechanism is worse.** The bullet
says "shallow checkout, no trunk ref". `origin/main` resolved fine — the run log
shows `actions/checkout@v4` fetching
`+808fc5a2…:refs/remotes/origin/main` at `--depth=1`. What happened is that
`create-pull-request` then fetched its long-lived branch at **`--depth=11`**
(log line 309). That branch was cut from `main` on 2026-08-03, so its ancestry
**is** early-August `main` — those objects became present in the store while
`origin/main` stayed at depth 1. `merge-base --is-ancestor` then answers *false*
for genuine ancestors. All three SHAs are ancestors of `origin/main` today, and
were on 2026-09-03:

```
09ab3ab9: ancestor   aa278b7a: ancestor   54d9206d: ancestor
```

So in CI the gate has only ever been inert (depth-1, everything `unknown`) or
actively wrong (sibling fetch deepens the store but not trunk). It has never been
meaningful there. The stop condition — "STOP D1 if the receipts bot passes by a
mechanism this change would break" — does **not** fire: the change makes the hook
exit earlier under exactly the condition where it is already inert.

---

## 2. Item 1 — the diff, the demonstrations, the smoke run

**Decision taken: D1(a), narrowed.** The prompt's D1(a) says "the hook exits 0
early". I scoped **only the trunk gate**; the `@civitics/data` typecheck gate
still runs in CI. The item's own title says "trunk gate steps aside", and D1's
rejection of `--no-verify` is partly that it would drop the typecheck gate — so
dropping it via the skip path would have reintroduced the thing D1 rejected.

The diff adds a guarded block between the two gates: under `GITHUB_ACTIONS`, if
`git rev-parse --is-shallow-repository` is `true` (with a `.git/shallow`
fallback for git < 2.15) or no `origin/main` / `main` ref resolves, print a
reason and `exit 0`. Plus `scripts/test-pre-push-hook.mjs` and a `hook:test`
script.

**Demonstration 1 — a real off-trunk completion still aborts a real push.** On a
scratch branch, `docs/done.log` got one row claiming `FIX-1199 | ad623386` while
`ad623386` was still off-trunk, pushed to a throwaway local bare remote:

```
✗ 1 completed FIX-ID(s) backed only by off-trunk commits …
    FIX-1199  (SHAs: ad623386)
[pre-push] ✗ fixes:check:trunk failed — push aborted.
PUSH EXIT=1
```

Same push with `GITHUB_ACTIONS=true` in this (non-shallow) checkout: **also
aborted, exit 1.** The CI flag alone does not disable anything. Scratch branch
deleted, `done.log` unpolluted, tree clean.

**Demonstration 2 — the false positive, reproduced on a real shallow clone.**
`git clone --depth=1 --branch main`, then the literal
`create-pull-request` move:

```
git rev-parse --is-shallow-repository      → true
git rev-list origin/main | wc -l           → 1
git cat-file -e 09ab3ab9                   → absent
git fetch --depth=11 <origin> chore/refresh-disposable-emails
git cat-file -e 09ab3ab9                   → present
git rev-list origin/main | wc -l           → 1        (still)
git merge-base --is-ancestor 09ab3ab9 origin/main
                                           → exit 1   (NOT ancestor — wrongly)
```

The same hook in that same clone:

```
GITHUB_ACTIONS=true:
  [pre-push] ✓ @civitics/data typecheck clean.
  [pre-push] fixes:check:trunk SKIPPED under GITHUB_ACTIONS: shallow checkout — ancestry is not decidable.
  HOOK EXIT=0
GITHUB_ACTIONS unset:
  [pre-push] running pnpm fixes:check:trunk ...   HOOK EXIT=0
```

**The test suite** (`pnpm hook:test`) — 15 assertions, throwaway git repos with a
real `.git/shallow` marker and a recording `pnpm` stub. It asserts the skip fires
on CI+shallow and CI+no-trunk-ref, and asserts in four negative cases that it
does *not*: full CI checkout, shallow-but-not-CI, a real violation (still exit 1),
and a typecheck failure (still exit 1, not swallowed by the skip).

**Smoke dispatch: run 35415836771**, `workflow_dispatch` on `main`,
2026-09-19 02:30 UTC, **success in 49 s**. Its log is the receipt:

```
[pre-push] ✓ @civitics/data typecheck clean.
[pre-push] fixes:check:trunk SKIPPED under GITHUB_ACTIONS: shallow checkout — ancestry is not decidable.
[pre-push]   Ancestry is gated on PRs by fixes-integrity.yml (fetch-depth: 0). FIX-1199.
…
Created pull request #4 (civitics-platform:chore/refresh-disposable-emails => main)
```

**PR #4 is open**, +698/−1 across exactly the two allowed paths — six weeks of
upstream blocklist drift. Per the workflow's own design that diff is Craig's
30-second review; I have not merged it. The **scheduled** receipt is still the
2026-10-03 11:00 UTC run.

---

## 3. Reads 3–6 — the tagger, the table, the phases

### 3.1 The unit, quoted from prod

`rebuild_pre_vote_timing_tags()` carries
`proconfig = search_path=public, pg_temp, enable_hashjoin=off, enable_mergejoin=off`
— no `statement_timeout`, so `check:proconfig` is unaffected. Body:

```sql
DELETE FROM public.entity_tags
 WHERE entity_type = 'financial_entity' AND generated_by = 'rule'
   AND tag_category = 'internal';

INSERT INTO public.entity_tags (…)
SELECT 'financial_entity', q.entity_id, 'pre_vote_timing', 'internal', …
FROM (SELECT DISTINCT fr.from_id AS entity_id
        FROM public.financial_relationships fr
       WHERE fr.to_type = 'official' AND fr.relationship_type = 'donation'
         AND fr.occurred_at IS NOT NULL
         AND EXISTS (SELECT 1 FROM public.votes v
                      WHERE v.official_id = fr.to_id
                        AND v.voted_at >= (fr.occurred_at…) + interval '1 day'
                        AND v.voted_at <  (fr.occurred_at…) + interval '91 days')) q
ON CONFLICT (entity_type, entity_id, tag, tag_category) DO NOTHING;
```

`run_rule_taggers`' daily branch is a single
`v_rows := public.rebuild_pre_vote_timing_tags();` inside one
`BEGIN … EXCEPTION … END` then `COMMIT` — **one statement, one
`elapsed_seconds` stamp**, exactly as cc-135 said. No later migration redefines
either tagger. `rebuild_financial_entity_size_tags()` is the same
DELETE-then-INSERT shape over `tag_category='size'` and carries only
`search_path`.

### 3.2 `entity_tags` on prod — two readings, and the epoch

**Epoch, stated first.** `pg_stat_database.stats_reset` is **NULL**, which does
*not* mean "since creation": `pg_postmaster_start_time()` is
**2026-09-15 15:09:48 UTC**, and `pg_stat_statements_info.stats_reset` and
`pg_stat_bgwriter.stats_reset` are the same instant to the millisecond. So every
cumulative counter below — including `vacuum_count` / `autovacuum_count` /
`idx_scan` — dates from that restart, **3.5 days**, not lifetime. cc-135 read
these as lifetime; that is corrected in the append.

| | read 1 | read 2 |
|---|---|---|
| read at (UTC) | 2026-09-19 02:11:23 | 2026-09-19 02:42:17 (**30.9 min later**) |
| `n_live_tup` | 5,359,873 | 5,359,873 |
| `n_dead_tup` | 71,198 | 71,198 |
| `n_tup_ins` / `n_tup_del` | 6,834,279 / 7,290,057 | identical |
| `last_autovacuum` | 2026-09-18 06:47:17 | identical |
| `autovacuum_count` / `vacuum_count` | 4 / 0 | 4 / 0 |
| `relpages` / `relallvisible` | 224,824 / 220,616 → **98.1 %** | identical |

**Byte-identical.** The between-dailies write rate on `entity_tags` is **zero** —
nothing touches it except the two taggers. (Three further reads at 02:18, 02:32
and 02:37 agree.) `last_autovacuum` is **17 minutes after** the 06:30 daily.
`reloptions` are already `0.05 / 0.02`, set by **FIX-652** (not FIX-1002/1003 as
the prompt guessed), and `entity_tags` is already in the FIX-943 tuned-table list
at `packages/db/CLAUDE.md:747`. Effective trigger:
`50 + 0.05 × 5,359,873 = 268,044`; dead is at **26.6 %** of it.

Heap 1,756 MB, total 4,785 MB → **3,024 MB of index over a 1,756 MB heap**, eight
indexes, and `idx_scan` since the restart:

| index | size | `idx_scan` |
|---|---|---|
| `entity_tags_type_category_id` | 878 MB | 14 |
| `entity_tags_entity_type_entity_id_tag_tag_category_key` | 751 MB | 6,834,299 |
| `idx_entity_tags_entity` | 471 MB | 7,565 |
| `idx_entity_tags_visibility` | 450 MB | **0** |
| `entity_tags_pkey` | 376 MB | **0** |
| `idx_entity_tags_tag` | 98 MB | 15 |
| `entity_tags_fe_industry_content` | 3,648 kB | 459,057 |
| `idx_entity_tags_topic` | 200 kB | 8 |

### 3.3 The phase split (clone, one `BEGIN … ROLLBACK`)

The function's own GUCs replayed (`enable_hashjoin=off`, `enable_mergejoin=off`,
`work_mem=256MB`). Clone holds 689,966 `internal` rows to prod's 865,941.

| phase | wall | share | buffers hit / read / dirtied |
|---|---|---|---|
| **DELETE** (whole node) | **5,835 ms** | **4.7 %** | 704,679 / 16,925 / 14,710 |
|  ↳ its Bitmap Heap Scan | 2,548 ms | 2.1 % | 4 / 16,925 |
|  ↳ index maintenance (derived) | 3,287 ms | 2.7 % | ~687,750 hits |
| **INSERT — scan** (FR × votes) | **58,792 ms** | **47.7 %** | 13,040,603 / 329,020 / 12,143 |
| **INSERT — own heap+index write** (derived) | **58,477 ms** | **47.5 %** | 20,084,963 / 820,938 / 838,458 |
| total | 123,104 ms | | |

Scan internals: `Nested Loop Semi Join` 1,640,063 rows → `Unique` 720,256;
`Bitmap Heap Scan on financial_relationships` 4,204,255 rows, Heap Blocks
313,643, 41,676 ms; `Index Only Scan using votes_official_voted_at` **4,204,255
loops, Heap Fetches 217,660**, 13.04M buffer hits — the largest single buffer
consumer in the subtree.

**The DELETE is 4.7 %, under the prompt's 10 % line.** Per D2 I say it plainly:
**cc-135 §6's vacuum-owner hypothesis is wrong.** And the wall is not carried by
the scan alone either — it is a near-even 47.7 / 47.5 split between the scan and
the INSERT's own index maintenance.

A second pass in the same transaction, against a table now carrying pass-1's dead
tuples, doubled the DELETE's work structurally — Heap Blocks 14,709 → **29,409**,
bitmap index scan rows 689,966 → **1,410,222** — while its *wall* fell (2,399 ms)
purely from cache warming. So pass 2 shows the block-count effect and is useless
as a timing. The prompt's third variant (re-time after a clone VACUUM) cannot
discriminate here: the clone is at **100 %** all-visible before and after.

### 3.4 Prod's own plan — which kills option (ii)

A bare `EXPLAIN` on prod (no `ANALYZE`, nothing executed):

- **DELETE** → `Bitmap Index Scan on entity_tags_type_category_id`,
  `Index Cond: (entity_type = … AND tag_category = …)` — a proper two-column
  prefix. The clone fell back to `idx_entity_tags_tag` with `tag_category` as a
  non-leading filter, so **4.7 % is an upper bound on the DELETE's prod share.**
- **INSERT** → same shape as the clone: `Nested Loop Semi Join`,
  `Bitmap Heap Scan on financial_relationships` via `financial_relationships_to`,
  `Index Only Scan using votes_official_voted_at`.

**A Bitmap Heap Scan does not consult the visibility map.** So
`financial_relationships` sitting at **82.7 %** all-visible is irrelevant to this
query, and **option (ii) — the FIX-884 shape on FR — is refuted on prod's own
plan**, not merely on the clone's. The only Index Only Scan anywhere is on
`votes`, which is at **96.7 %** all-visible with `autovacuum_count 0`, 13,931
dead, 982,966 rows, and no vacuum owner. If any table's VM is worth instrumenting
for this job, it is `votes`.

### 3.5 What is actually wrong: bloat, not vacuum state

| | prod | clone (freshly restored, post-VACUUM) |
|---|---|---|
| `sum(avg_width)` | **172** | **171** |
| `relpages` | 224,824 | 62,587 |
| ideal pages (`reltuples × (avg_width+24) / 8060`) | 130,340 | 67,000 |
| **heap bloat ratio** | **1.72** | **0.93** |
| heap | 1,756 MB (≈738 MB slack) | 489 MB |

The average row is the **same width on both sides**, so the row-mix confound
(prod is 82 % `size` tags, the clone 72 %) is dead. Prod carries 1.72× the pages
its own rows need, while running at 98.1 % all-visible with autovacuum firing on
schedule. **VACUUM makes space reusable; it does not compact.** A table rewritten
wholesale twice a week ratchets its physical size up regardless of how well it is
vacuumed.

*(The clone's per-index figures are not quoted as a baseline: they were taken
after the rollback of 1.44M inserted tuples, and a plain VACUUM does not return
btree pages, so they read ~247 MB high. Prod's index figures were never touched.)*

### 3.6 The weekly coupling — measured, not hypothesised

`data_sync_log`, pipeline `run_rule_taggers`:

| run | wall s | rows | **s / 1k rows** |
|---|---|---|---|
| dailies 09-01 → 09-08 | 713–770 | ~865,400 | **0.824 – 0.890** |
| **weekly 09-08 16:00** | 4,692 | 4,406,878 | **1.065** |
| dailies 09-09 → 09-15 | 873–899 | ~865,500 | **1.009 – 1.039** |
| **weekly 09-15 16:00** | 4,838 | 4,409,537 | **1.097** |
| dailies 09-16, 09-18 | 952, 945 | ~865,800 | **1.091 – 1.100** |

**The daily's cost per row converges onto the weekly's own cost per row**, twice,
each time within a day of that weekly. Rows moved **+0.074 %** across the window
while cost per row moved **+32 %**. Both taggers are paying the same
size-dependent marginal cost — they share one table, and against a 256 MB
`shared_buffers` with a ~10 GB working set, every extra megabyte of heap and
index is a read. That is why it is a **step and not a drift**: physical size does
not come back down.

(09-17's 7,203.5 s / `partial` / `canceled` is the 2 h `cron_job_budget` cancel,
not creep — unchanged from cc-135.)

---

## 4. D2 — the proposal, as appended

`FIX-1178` **stays OPEN**; `docs/receipts/bands.json` **untouched**. The append
carries the numbers above and this proposal, for Craig to pick from. A **vacuum
owner is deliberately not on the list.**

| | proposal | cost |
|---|---|---|
| **(a)** | Stop rewriting the whole table. Both taggers DELETE and re-INSERT a set that changed by **642 rows in 18 days**. A merge — `INSERT … ON CONFLICT DO NOTHING` plus a targeted DELETE of only rows that left the set — removes the index-maintenance phase (47.5 %) and the bloat source in one change. | Largest change; one migration per tagger plus a correctness argument that the targeted DELETE cannot strand rows. Biggest payoff. |
| **(b)** | One-off compaction on prod (`VACUUM FULL` or `REINDEX`) to reclaim ~738 MB heap slack and the index slack. | `ACCESS EXCLUSIVE`, supervised window, **not this run and not before cc-132**. Buys back the *current* bloat; without (a) it re-accrues. |
| **(c)** | Drop `idx_entity_tags_visibility` (450 MB, `idx_scan 0`); re-examine `entity_tags_pkey` (376 MB, `idx_scan 0`). | Cheap and reversible, but 3.5 days of counters is too short a window. **Proving read: a second `idx_scan` sample after a full weekly cycle.** |
| **(d)** | Stamp `delete_seconds` / `scan_seconds` / `insert_seconds` into `run_rule_taggers`' `data_sync_log` metadata. | One migration, Monday. Makes the next creep diagnosable from the receipts file instead of a clone replay — which is what this whole read cost. |

---

## 5. D3 — local-clone hygiene and the `db:types` recipe

**Ten tables, not eleven.** Read 7's patterns match ten; cc-135 §9.5 said eleven.
All ten confirmed absent from prod by name before dropping. Dropped on **LOCAL
only**, **916 MB** reclaimed:

| table | total size |
|---|---|
| `_eq_before` | 254 MB |
| `_eq_a`, `_eq_b`, `_eq_c` | 221 MB each |
| `_proof_ref_br`, `_proof_ref_odrmv`, `_proof_ref_odt`, `_proof_ref_sa`, `_proof_ref_sd`, `_proof_ref_tm` | 0–8 kB each |

**Five more exist that the authorisation does not cover — and four of them are
real.** `_drb_chunk_fe`, `_drb_donor`, `_drb_fe`, `_drb_targets` **exist on prod**
and are correctly present in the committed `database.ts`. `_proof_recips` is
local-only scratch but outside read 7's patterns, so it was left alone.

**`db:types` diff after the drops: 50 insertions / 14 deletions**, down from
cc-135's 392 / 10. Fully classified, and **none of it is schema**:

| artifact | lines |
|---|---|
| `__InternalSupabase` header (local gen omits) | −4 |
| `graphql_public` schema block in `Database` (local gen adds) | +24 |
| `graphql_public` in the `Constants` export | +4 |
| generic-constraint parenthesis churn (CLI version) | 10 pairs |
| `_proof_recips` | +11 |

Nothing committed. The recipe's splice + strip removes the first three, leaving
**10 lines of parenthesis churn + 11 lines of `_proof_recips`.**

**The recipe fix turned out bigger than "fix the regex": the recipe was not in the
repo at all.** `grep db:types` finds only the `package.json` script line; the
procedure existed solely in an agent memory file. It is now written into
`packages/db/CLAUDE.md` with the four artifacts enumerated, the confirmed
**multi-line** `Constants` shape

```
  graphql_public: {
    Enums: {},
  },
```

(so a single-line regex matches nothing and the churn lands silently — hence
"assert after stripping, don't trust the replace"), the `--workdir` requirement
when generating from a worktree, and the check-scratch-tables-against-prod step.

---

## 6. FIX status

```
FIX-1199  closed  2026-09-18 ad623386 local-only
FIX-1178  OPEN    (no done.log row)          — diagnosed, decision Craig's
FIX-1200  OPEN    (filed this run)
```

**Open count 67 → 67**, not the 67 → 66 the prompt predicted: 67 at start, −1
(FIX-1199 closed), +1 (FIX-1200 filed, unplanned — see §8).

---

## 7. Commits and CI

Four commits, `03edf289` → `0f958f01`, every push a fast-forward from the
`fix-1199` worktree, pre-push `fixes:check:trunk` clean on each.

Verification run before item 1: `turbo run typecheck` 10/10, `turbo run lint`
9/9, `@civitics/data` 780 pass / 0 fail, `@civitics/app-civitics` 275 / 0,
`@civitics/db` 205 / 0, `check:reads` OK (158 reads), `check:render-timeouts` OK
(161), `check:workflow-parity` clean (its one waiver is the pre-existing
`fec-indiv` divergence — the hook change does not trip it), `check:proconfig`
0 undeclared, plus `fixes:test`, `fix:add:test`, `session:worktree:test`,
`drain:test`, `check:proconfig:test` and the new `hook:test` (15/15).

CI on `main`, all green:

| workflow | head | run | conclusion |
|---|---|---|---|
| `tests` | `ad623386` | 35415828087 | success |
| `fixes-integrity` | `ad623386` | 35415828169 | success |
| `refresh-disposable-emails` | `ad623386` | 35415836771 | success (the smoke) |
| `tests` | `7b093e5b` | 35416618628 | success |
| `hit-test` | `7b093e5b` | 35416618632 | success |
| `fixes-integrity` | `7b093e5b` | 35416618699 | success |
| `fixes-integrity` | `0f958f01` | 35416640757 | success |

`2d0cf2ff` carries no run of its own — it is an ancestor of `7b093e5b`, which was
pushed in the same fast-forward. `0f958f01` is `done.log`-only, so `tests.yml`
skips it via `paths-ignore` and `fixes-integrity.yml` still runs, which is the
designed behaviour.

---

## 8. What contradicted the bullets, cc-135, or this prompt

**Five, and the first is the reason the whole item needed re-deriving.**

1. **FIX-1199's bullet has the wrong mechanism.** "where `fixes:check:trunk`
   cannot pass (shallow checkout, no trunk ref)" — `origin/main` resolves fine in
   that checkout. The gate did not fail to run; it ran and returned a **false
   accusation** against FIX-960 and FIX-962, whose SHAs were on `main` then and
   are now. The bullet's *conclusion* ("the hook is doing its job — scope it,
   don't disable it") survives, but only half of it is true: the hook was not
   doing its job, it was doing the opposite. Scoping is still the right fix, and
   the reason is stronger than the bullet knew.

2. **cc-135 read `entity_tags`' vacuum counters as lifetime. They are 3.5 days
   old.** `pg_stat_database.stats_reset` is NULL, which reads like "never reset" —
   but `pg_postmaster_start_time()` is 2026-09-15 15:09:48 UTC and every stats
   view's epoch matches it to the millisecond. "`vacuum_count 0`,
   `autovacuum_count 4` lifetime" became, in cc-135's reasoning, evidence of a
   neglected table. Four autovacuums in 3.5 days is the opposite: a healthy
   cadence, the last one 17 minutes after the bulk rewrite that triggered it.

3. **Both of D2's named shapes are refuted, and the answer is a third thing.**
   (i) the vacuum owner: the DELETE is 4.7 % of the wall and autovacuum is already
   on time. (ii) the FIX-884 shape on FR: prod's own plan reads FR by a **Bitmap
   Heap Scan**, which never consults the visibility map, so FR's 82.7 %
   all-visible cannot be what moved — a fact the prompt's framing (and FIX-884's
   precedent) made easy to assume the other way. What is actually wrong is
   **1.72× heap bloat on a table that every vacuum instrument reports as
   healthy**, and bloat is precisely what VACUUM does not fix. The
   memory-rule *"EXPLAIN first; no Index Only Scan ⇒ VM decay is not the cause"*
   earned its keep here.

4. **Read 7 lists ten tables, not eleven, and the pattern is not the boundary of
   the problem.** Five further underscore-prefixed tables exist locally, four of
   which are **real prod tables** that belong in `database.ts`. A drop authorised
   by pattern rather than by verified-absent-from-prod would have deleted live
   schema from the clone. `_proof_recips` — genuinely scratch — is outside the
   authorised names and is still there, so the `db:types` diff is 21 lines rather
   than the 10 D3 anticipated.

5. **The `db:types` "recipe" does not exist in the repo.** The prompt asks where
   it is written and offers `packages/db/CLAUDE.md` or `docs/`. It is in neither:
   only an agent memory file, which no human and no fresh session reads. Fixing
   the multi-line `graphql_public` strip in a memory file would have left the next
   session to rediscover the same 392-line diff. It is now in
   `packages/db/CLAUDE.md`.

**One more worth knowing, not a contradiction:** the receipts bot and the audit
bot have been pushing through this hook daily and weekly with the trunk gate
**completely inert** — `trailersScanned 0`, every SHA `unknown`. Nobody was being
protected in CI; the guard's real coverage has always been the developer machine
plus `fixes-integrity.yml`'s `fetch-depth: 0` PR check. That is now stated in the
hook's own comment rather than being an accident.
