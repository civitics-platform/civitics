---
cc: 153
prompt: cc-prompt-153-cc149-followups-1073-streak-recalibration-job-run-details-retention-gate-d-materialized-revoke-legacy-canary-upstash-dup.md
started_at: 2026-09-24T23:42:02Z
finished_at: 2026-09-25T00:26:41Z
head_before: 27e66239
head_after: a530026d
commits:
  - {sha: 347db426, subject: "docs(fixes): file FIX-1220, FIX-1221, FIX-1222 (cc-153: the */1 probe's streak recalibration; job_run_details retention blocked by a 90-day reader; the burst rule stopped)"}
  - {sha: f9b9b43c, subject: "feat(db): FIX-1220 — a startup-timeout streak must also span 12 min at the job's cadence (the */1 probe needs 12 runs, a */2 job 6); FIX-1194 — prod_op_gate (d) reads one MATERIALIZED scan; record_box_health loses service_role"}
  - {sha: 5a4e4072, subject: "chore(app,db): FIX-1125 — the box-health ring's head is the latest sample; the SET duplicate goes"}
  - {sha: 5f0ae4bd, subject: "docs(fixes): FIX-1194 / 1125 / 1218 appends (cc-153: gate (d) one scan and the service_role revoke; the ring head is the latest sample; the 02:00 canary kept, it hosts the grant sweep)"}
  - {sha: a530026d, subject: "chore(fixes): sync status after FIX-1220"}
fixes_closed: [FIX-1220]
fixes_filed: [FIX-1220, FIX-1221, FIX-1222]
fixes_reopened: []
migrations_pushed: [supabase/migrations/20260920150000_fix1220_1194_streak_minutes_gate_d_one_scan_revoke.sql]
prod_writes: "One migration, 20260920150000, applied to Pro 00:15:44-00:15:47 UTC. wait-for-gate --then ran db-push-prod: the gate read OK on its first poll at 00:15:44Z, it claimed the FIX-950 session, pushed and released. The migration redefined check_cron_job_health() (+31/-0 lines against prod) and prod_op_gate() ((d) rewritten, +40/-24), re-asserted check_cron_job_health's grants and COMMENT, and revoked service_role EXECUTE on record_box_health(timestamptz). No cron.schedule, no cron_job_budget row, no DELETE: the retention job (D2) was stopped. Pushes to main, each a Vercel deploy: 347db426 at ~00:01:25, 5a4e4072 at ~00:21:05 (production deploy success 00:22:57), and a530026d at ~00:22:50 UTC, plus this report. The deploy changed /api/cron/box-health to 2 Upstash commands a sample. Every other prod contact was a read: db-query --prod, EXPLAIN ANALYZE, pg_get_functiondef, prod_op_gate() and check_cron_job_health() calls, 3 Upstash read commands (x2) and gh."
ci: green
stopped_items:
  - "D2, the cron.job_run_details retention job: read 4's STOP fired. list_scheduled_rollup_pipelines() (the canary calls it with the default p_lookback_days = 90) reads the table over 90 days and takes its FIX-1135 era boundary from min(start_time) over all history. Nothing was scheduled or deleted. Filed as FIX-1221 with the blocker, the numbers and a candidate slot (01:50 UTC). Item 5 (the first-firing receipt) therefore does not apply."
  - "D1's burst half. The distinct-minutes rule replayed on prod's retained history gives 50 escalating buckets on 11 days against today's 64 on 13, silencing two paging days (08-26, 09-14). That contradicts the prompt's premise that burstThreshold is unchanged in meaning. The burst is unchanged; filed as FIX-1222 with the measured alternatives. D1's streak half was built and FIX-1220 closed on it."
  - "D5, retiring /api/cron/nightly-sync: the tree contradicts the premise twice. The route also hosts FIX-557's grant-expiry sweep, and the dashboard's nightly_dispatch card reads the dispatched rows. Nothing was deleted; one append on FIX-1218 records the shape that works."
---

# cc-153: the probe's streak is recalibrated and the gate's (d) is one scan; retention, the burst and the 02:00 canary are each stopped by a read

**Summary.**
- **Built and live on Pro** (migration `20260920150000`, 00:15:44–00:15:47 UTC):
  - D1 streak half. A startup-timeout streak now escalates only at **runs ≥ 6 AND runs × the job's interval ≥ 12 min**. Replayed on every retained streak, it reproduces today's decision on **87 of 87**, and the `*/1` probe now needs 12 runs. **FIX-1220**, closed `local + prod`.
  - D3. `prod_op_gate()`'s (d) reads are **one MATERIALIZED scan**: 112–133 ms → **23–26 ms**, and the whole gate 373–387 ms → **285–288 ms**. Same keys.
  - D4. `record_box_health` **lost service_role** (prod ACL now `postgres=X/postgres`).
- **D6 is live:** `/api/cron/box-health` sends **2 Upstash commands** a sample, not 3. The receipt is the first post-deploy firing: the ring head moved to 00:24:22.316, and `latest` stayed frozen at 00:22:22.423.
- **Three items stopped on a read.** Each is recorded, and nothing was worked around:
  - **D2 (retention).** A 90-day reader blocks it (FIX-1221).
  - **D1's burst half.** Distinct minutes silences two historical paging days (FIX-1222).
  - **D5 (the 02:00 canary).** The route also runs the grant sweep, and the dashboard reads its rows (append on FIX-1218).
- **Filed 3** (FIX-1220/1221/1222; the prompt planned 2). **Closed 1.** **60 open** (58 at start, +1219 from `/cc 152`, +3 filed, −1218 by `/cc 152`, −1220).

**Push times (UTC), for `/cc 152`:**

| event | UTC | `held` read just before |
|---|---|---|
| push #1 (`347db426`, filing) | ~00:01:25 | false @ 00:01:18.567 |
| DDL `20260920150000` | 00:15:44 – 00:15:47 | false @ 00:15:42.124 (plus the gate's own (e)) |
| push #2 (`f9b9b43c` + `5a4e4072`, code) | ~00:21:05; deploy success 00:22:57 | false @ 00:21:00.871 |
| push #3 (`5f0ae4bd` + `a530026d`) | first try rejected non-ff at ~00:22:35 (`382c3013` landed first); rebased; pushed ~00:22:50 | false @ 00:22:23.056, then 00:22:43.194 |
| push #4 (this report) | after 00:26:41 (a file cannot carry its own push time) | read before the push |

---

## 1. Reads 2–8

**Read 1** (23:42–23:45 UTC):
- `session:check` read **58 open** at `27e66239`. One unmerged branch and one open PR (disposable-email refresh, pre-existing). 3 reopened (pre-existing).
- `tests.yml` has the seven checks. `.env.local` points at local.
- Prod `schema_migrations` head was **`20260920140000`**, as expected.
- FIX status: 1073 closed · 1194 open · 1125 open · 1218 open (closed by `/cc 152` during the run) · 1054 closed.

**Read 2 — where the tiers are computed.** SQL, not TS. `check_cron_job_health()` computes them, and `canary-check.ts` only parses them. From prod's `pg_get_functiondef`, read 23:45:
- **Streak.** `v_streak_n int := 6`. Consecutive **runs** by gaps-and-islands over `hist` (2 × lookback = 52 h): `WHERE s.streak >= v_streak_n AND s.last_at >= v_since`. `span_minutes` was reported but never compared against anything.
- **Burst.** `v_burst_m int := 10`. `date_trunc('hour')` buckets, `count(*)` of startup-timeout **runs** across all jobs: `FROM buckets WHERE n >= v_burst_m`.

What the probe adds today, measured on prod 23:46–23:59 UTC:
- **Nothing.** `box-health-probe` has had **0 startup timeouts** since it went live 09-24 02:02.
- In the last 7 days every startup timeout was on Tue 09-22, 15:00–22:00. The two watchdogs had 78 each, ec-crawl 6, fe-crawl 2, and each twice-daily vacuum job and `rule-taggers-weekly` had 1.
- cc-149's "9 more burst hours" was a model of the probe's likely failures, not an observation.

Streak replay over **all retained history** (07-26 onward), using the median firing interval per job:

| rule | 7 d | older | note |
|---|---|---|---|
| runs ≥ 6 (today) | 2 | 85 | |
| **runs ≥ 6 AND runs × interval ≥ 12 min (built)** | **2** | **85** | **0 demoted** |
| minutes only (last − first + interval ≥ 12, the prompt's wording) | 13 | 158 | **+84**, all short runs on slow jobs |

Intervals measured on prod: both watchdogs 2.0, ec-crawl 15.0, fe-crawl 30.0.

Burst replay, hourly buckets reaching the threshold:

| rule | 7 d | older | paging days |
|---|---|---|---|
| runs ≥ 10 (today) | 6 | 58 | 13 |
| distinct minutes ≥ 10 (the prompt's D1) | 4 | 46 | **11** |
| (job, 2-min slot) ≥ 10 | 6 | 58 | 13 |

The two days distinct minutes silences:

| day | max runs | max distinct minutes |
|---|---|---|
| 08-26 | 11 | 6 |
| 09-14 | 16 | 8 |

**Read 3 — `cron.job_run_details` on prod**, 23:52:35 UTC:
- **64,862 rows**; oldest `2026-06-29 08:00:00.39`.
- **16,270 rows older than 30 days** (the first purge, under the 200k batching line).
- Heap `pg_relation_size` **10,551,296 bytes** (10 MB); total 12 MB.
- Owner **`supabase_admin`**. `postgres` holds DELETE (`a*r*w*d*…` with grant option) but is not a member of the owner, so it cannot create an index. The only index is `job_run_details_pkey (runid)`.
- Rows/day: 09-21 1,612 · 09-22 1,486 · 09-23 1,613 · 09-24 **2,917** (partial; the probe started at 02:02).
- `n_dead_tup` 0. One autovacuum since the stats epoch (09-22 22:33). The trigger is 50 + 0.2 × 60k ≈ 12k.

**Read 4 — readers.**
- `grep -rln job_run_details packages apps supabase/migrations docs`: **119 files**. Without `docs/` it is **50** (packages 10, apps 1, migrations 39). The prompt expected 49 at `f6014452`; the one-file difference was not chased.
- Live readers on prod (`pg_proc.prosrc ILIKE '%job_run_details%'`) and the window each scans:

| reader | window on `job_run_details` |
|---|---|
| **`list_scheduled_rollup_pipelines(p_lookback_days DEFAULT 90)`** | **90 days** (`job_counts`, `by_time`) plus **min(start_time) over ALL history** (`driver_start.first_firing`, FIX-1135's era boundary). The canary calls it with no argument. **STOP for D2.** |
| `check_cron_job_escalations` | 336 h (14 d); its `last_failed_at` subquery is unbounded but only runs for a job already failing inside 14 d |
| `prod_op_gate` | 14 d in (b), plus an unbounded newest-run `last_wall_s`; (d) 1 d |
| `check_cron_job_health` | 52 h; `missing_daily`'s "no run row ever" check only matters for jobs first seen < 26 h ago |
| `get_ec_crawl_health` | 7 d (its 30 days is elsewhere) |
| `record_box_health` | 1 d |
| `enforce_cron_job_budgets` | `status='running'` rows only (0 older than 30 d) |
| `reap_stale_sync_log`, `refresh_derived_mvs`, `grant_pg_cron_access` | comments or grants only |

TS readers:

| reader | window |
|---|---|
| receipts `CRON_LOOKBACK_DAYS` | 14 d |
| status page `CRON_STREAK_LOOKBACK_HOURS` | 336 h |
| `donor-party-bootstrap-runner.ts` | 1 h |

**Read 5 — `prod_op_gate` (d) on prod**, 23:52 UTC. `EXPLAIN (ANALYZE, BUFFERS)`, three runs each:

| (d) read | ms |
|---|---|
| watchdog walls (60 min, cron.job LEFT JOIN, 2 loops × 1,288 buf) | 24.8 / 22.6 / 23.2 |
| startup timeouts (60 min; the ILIKE evaluated over all 64,862 rows) | 78.8 / 79.3 / **100.6** |
| budget overruns (1 d) | 9.8 / 9.8 / 9.7 |
| **sum** | **112 / 112 / 133** |
| **MATERIALIZED form** (1 d, 2,923 rows, one scan, 1,288 buf) | **23.6 / 23.3 / 26.2** |
| whole `prod_op_gate(1200)` before | 387 / 373 (36–38k buf) |
| whole `prod_op_gate(1200)` after (00:16 UTC) | **288 / 285 / 285** (32,544 buf) |

The remaining ~255 ms is (b)'s vacuum co-tenancy. Its per-job LATERAL covers 14 days, and its `last_wall_s` subquery has no `start_time` bound. The SET clause is `search_path=public, pg_catalog` only.

**Read 6 — grants on prod**, 23:54:40:

| function | service_role EXECUTE | SECURITY | proconfig |
|---|---|---|---|
| `record_box_health(timestamptz)` | **t** | INVOKER | search_path |
| `record_box_health_mem(jsonb)` | t | DEFINER | search_path |
| `box_is_saturated(int,int,timestamptz)` | t | INVOKER | search_path |

**Read 7 — the two routes.**

`/api/cron/nightly-sync`, whole file:
- It upserts `pipeline_state.cron_last_started`.
- It inserts a `nightly-sync` / `dispatched` row.
- **It also calls `expire_lapsed_grants`**, FIX-557's grant sweep: "Lives here because the Hobby plan caps Vercel crons at 2".

Consumers of what it writes:
- `cron_last_started`: **no reader**. Only a `nightly.yml` comment ("nothing compares it").
- The `dispatched` rows: **`DashboardClient.tsx:593`**, card `nightly_dispatch`, `aliases: ["nightly-sync"]`, "Daily, 02:00 UTC — the scheduled trigger that starts each night's ingest run". **It READS them.**
- The canary registry lists `nightly-sync` as a never-closes pipeline (no finding).
- `receipts-format.ts:301` reads the new `gha_dispatch` stamp, not these rows.
- `20260818001000` closed 104 `triggered` rows once (FIX-1054) and stays as history.

Prod: **142** `nightly-sync`/`dispatched` rows, the last at 09-24 02:00:37. `gha_dispatch_nightly` = `dispatched` @ 09-24 21:00:12.

`/api/cron/box-health` and `box-health-store.ts`, whole files. Readers of `civitics:box_health:latest` / `BOX_HEALTH_LATEST_KEY` across the whole tree, docs and workflows: **none**. Only the writer, its test, the `index.ts` re-export, the route comment and `.env.example`.

**Read 8 — co-tenancy** (prod `cron.job`, 23:58 UTC). Not used, because D2 stopped. Recorded in FIX-1221 for the next session:
- Candidate slot **01:50 UTC**.
- Neighbours: `officials-vacuum-analyze` Mon 01:30 (3 s) and `dpr-vacuum-analyze` Sun/Wed 02:00 (4 s).
- `refresh-derived-mvs-weekly` Tue 00:47 ends by ~01:10.
- Outside the 05:45–09:00 blackout and the 21:00 nightly (long nights end ~22:46).
- `abuse-events-retention` (04:15) is the existing retention precedent.

## 2. D1–D6

**D1, streak half (FIX-1220).** The logic is SQL, so it rides the one migration.
- `v_streak_min int := 12`.
- A new `cadence` CTE: the median of `start_time − lag(start_time)` per job over `hist`, rounded to 0.1 min.
- `AND s.streak * COALESCE(c.interval_minutes, 2) >= v_streak_min`.
- New keys: `per_job.interval_minutes`, `per_job.streak_minutes`, and `streak_minutes_threshold` in both the degrade branch and the answer.
- **Diff against prod's `pg_get_functiondef`: +31 / −0.** STABLE SECURITY DEFINER and the search_path SET are unchanged, and the grants and COMMENT are re-asserted. Local and Pro bodies after the push: 0 lines of diff.
- Canary: `describeStreak` prints `N consecutive … = M min at a K-min cadence (first to last S min)`, and the alert text names the minutes floor.
- Fixtures (`cron-startup-streak-minutes.test.ts`, on the clone, rolled back):

| fixture | result |
|---|---|
| probe shape, 6 in a row (6 min) | **no** escalation |
| 12 in a row | escalates, 12.0 min |
| `*/2` watchdog, 6 in a row | escalates, 12.0 min (unchanged) |
| bunched queued release (first-to-last 6 min) | escalates: runs × interval = 12, where a span rule would read 8 |
| hourly job, 5 in a row | no escalation (runs floor) |
| daily job, one lost firing | no escalation |

- Cost: old vs new locally 60 / 50 / 51 ms vs 51 / 52 / 68 ms, which is noise. On prod, `check_cron_job_health(26) - 'runs'` = 238 ms after.
- Everything except `startup_timeout_tiers`, and the burst itself, is byte-equal between old and new.

**D2: stopped** (read 4). See FIX-1221.

**D3 (FIX-1194).**
- The three (d) statements became one `WITH d AS MATERIALIZED (… 1 day …), wd, bo SELECT … INTO v_wd_rows, v_n, v_t, v_json`.
- The watchdog rows round-trip through `jsonb_to_recordset(...) ORDER BY jobname`, so the loop body is byte-identical.
- Same windows, the same predicate (rule 93; both tests grep for it) and the **same JSON keys**. On prod at 00:16: `ok: true`; top keys, readings keys and watchdog keys identical.
- **Diff against prod: +40 / −24**, all inside DECLARE (+1 `v_wd_rows`) and the (d) block (prod lines 272–340 of 271–350). Signature, LANGUAGE and SET: 0 diff.
- Equivalence: old (as `pg_temp`) against new on the clone, **82 of 82** points equal over 20 days × 2 expected-seconds values, 52 of them with (d) blocking.
- The (b) co-tenancy LATERAL was left alone: it cannot share a 1-day CTE without changing its 14-day window.

**D4.** `REVOKE EXECUTE … record_box_health(timestamptz) FROM service_role`. On Pro, `has_function_privilege` is **f** and the ACL is `{postgres=X/postgres}`. `_mem`, `box_is_saturated` and `check_cron_job_health` are still t.

**The push.**
- Dry run from the worktree: exactly one pending migration.
- Then `wait-for-gate.ts --expected-minutes 2 --reason "cc-153 FIX-1220/FIX-1194 DDL 20260920150000" --then "node <primary>/scripts/db-push-prod.mjs --workdir <worktree>"`, run with the primary's `.env.local.prod`.
- The gate read `00:15:44Z OK (expected 120 s, span to 00:19Z) watchdogs max 0.028/0.012 s` on the first poll. There was **no wait**: `/cc 152` never claimed prod tonight (its tail was not launched, per `b6fda36f`).
- It claimed and held 16 guarded pipelines, applied, cleared the holds and released; `--then` exited 0.
- Pro head is now `20260920150000`.

**D5: stopped** (read 7). Append on FIX-1218.

**D6 (FIX-1125).**
- The SET and `BOX_HEALTH_LATEST_KEY` are gone. `BOX_HEALTH_COMMANDS_PER_SAMPLE` is 2, and the reply-count check reads it.
- A new test: a 3-reply pipeline is refused.
- The route and `.env.example` comments are updated.
- Commands a month: 64,800 → 43,200 (−21,600).
- **Receipt:** read 00:24:37Z, the ring head `route_at` is **00:24:22.316** (ring 670 → 671) and `latest.route_at` is **00:22:22.423**, frozen. The on-box stamp landed at 00:24:22.694.

## 3. Status, commits, CI, `/cc 152`

**FIX status:**
- FIX-1220 closed (`f9b9b43c local+prod`).
- FIX-1221 and FIX-1222 open.
- FIX-1194 and FIX-1125 open (appended).
- FIX-1218 closed by `/cc 152` (appended).
- **60 open** of 1,209 ids. `fixes:check` is clean after every commit.

**CI:** `tests.yml` run **36077145989** on `5a4e4072`: **success**. The docs and status commits skip `tests.yml`.

**Local verification:**

| check | result |
|---|---|
| unit suites | data 976 → **979** (972 pass, 7 skipped, 0 fail); db 226 → **227**; app 321 |
| typecheck + lint | 5/5 turbo tasks |
| check:proconfig | 23 migrations scanned, 0 undeclared |
| check:reads / render-timeouts | 0 / 0 |
| check:workflow-parity | 0 undeclared |
| six `scripts/test-*.mjs` suites | pass |
| app build | pass |

**The first full data run showed 5 failures, all environmental.** They were the FIX-1215 "all clear" case and three prod-session `claim:` cases (plus the parent). `/cc 152` was running its FIX-1212 clone rehearsal on the **local** DB and held the local session from 00:14:19 (pid 15148, `donor_party_rollup_refresh` live). Once it released, those four files passed 49/49, and the full suite ran clean. No test could take its lock: `--force` cannot override a held session, by design.

**`/cc 152`'s pushes as seen from `git log`:**
- `7ee63e55`, `0d96d726`, `23adb556` (~23:55 UTC)
- `56a0fad5`, `6e7d106b`, `b6fda36f` (~00:20:46)
- `382c3013` (~00:23)

**Overlaps:**
- No push landed on top of another. Push #3's first attempt was refused non-ff because `382c3013` landed first; it was rebased and retried.
- The filing commit conflicted with FIX-1219's bullet at the same insertion point in INFRASTRUCTURE & PERFORMANCE; both were kept.
- I touched none of its files. Its local claim collided with my test run, as described above.

## 4. What contradicted cc-149, cc-150 or this prompt

1. **The streak is computed per run, in SQL, not in TS.** The TS only parses and prints. So D1 is a function redefinition, and items 1 and 2 are one commit.
2. **The prompt's streak rule, read literally, recalibrates the other way.** "A streak is span_minutes (last − first + interval) … escalates at ≥ 12 min" drops the runs count. A slow job's single lost firing spans days, so that rule escalates it: **+84 escalations** over retained history, 11 in the last week (`rule-taggers-weekly`'s one lost 09-22 firing, each twice-daily vacuum's).
   - That contradicts FIX-1073's own reading that a lone startup timeout is weather.
   - The prompt's fixtures and its phrase "the `*/2` calibration (6 runs) preserved for every cadence" are satisfied by **runs ≥ 6 AND minutes ≥ 12**, which is what was built (87/87 unchanged).
   - The interval is the job's median firing gap, not start-time arithmetic. Under stress pg_cron fires late and bunched (09-22: 16:01:04, 16:02:07, 16:03:25, 16:04:06, 16:06:00, 16:12:21), and a span can undercount.
3. **The burst's distinct-minutes rule does not do what the design says.** The threshold of 10 was sized while both `*/2` watchdogs failed on the same even minutes and counted twice.
   - Distinct minutes halves them on history: 64 → 50 buckets, and **13 → 11 paging days** (08-26 and 09-14 go silent).
   - "A `*/1` job cannot fill a bucket on its own" is false under it: each probe failure is its own minute.
   - It preserves the old calibration only in a full stall where the probe also fails on odd minutes, and it halves the burst the day the probe is paused.
   - Stopped. FIX-1222 carries the measured alternative: weight each failure by min(1, interval/2), which is exact on all 64 buckets and reaches 10 at ~6.7 min of a full stall instead of 10.
   - The probe had 0 startup timeouts in its first 22 h, so leaving the burst unchanged costs nothing measured.
4. **A live reader scans `job_run_details` past 14 days: 90 days, and all of history for its era boundary.** `list_scheduled_rollup_pipelines()` (FIX-1135) is called by the canary with its default. The prompt's "every reader scans ≤ 14 days" missed it because it reads through `v_since := now() - make_interval(days => p_lookback_days)`, not an `interval '…'` literal.
   - A longer retention would not rescue the plan. At ~3,050 rows/day, 100 days is ~305k rows, which puts the probe's scan near 100 ms.
   - Even the intended 30-day purge **caps** the table near 91k rows (1.4× today) rather than shrinking it. cc-149's "~100 ms by early December" is avoided only by a purge, and the purge needs that reader rewritten first.
5. **The probe's form ported to (d) without changing a key.** One statement, the same windows, the predicate byte-identical, and 82/82 equal on the clone. But **(d) was not the gate's main cost**: (b)'s vacuum co-tenancy is ~255 of the remaining ~285 ms, through an unbounded per-job `last_wall_s` subquery. Out of scope and left alone; worth a look when the retention work touches this table's readers.
6. **What reads `cron_last_started`:** nothing. **What reads the `dispatched` rows:** the dashboard's `nightly_dispatch` card. **The route is not only a canary:** it hosts FIX-557's grant-expiry sweep. D5's "delete the route" would have stopped grant expiry silently.
   - The retirement that works: the sweep gets its own cron path, the card repoints to `pipeline_state.gha_dispatch_nightly`, and then the canary writes and the `0 2` entry go.
   - The card has also shown the wrong thing since FIX-1218 ("Daily, 02:00 UTC — the trigger that starts each night's ingest run").
7. **Who reads `box_health:latest`:** nobody, so D6 went in as designed. The key keeps its last pre-deploy sample, and a `DEL` is optional housekeeping.
8. **FIX-1215's anchor test assumed `prod_op_gate` is alone in its migration.** It sliced from the file's first `AS $function$` to its last `$function$`. It now reads the gate's own body. `box-health-probe.test.ts` pinned the gate file to `20260920120000` by name, the same stale-pin cc-150 fixed in the gate's own test, and now derives the latest defining migration.
9. **Out of scope, as asked: `platform-snapshot` `*/30` vs `*/10`.** They coexist by design. The `*/30` Vercel cron hits `/api/cron/platform-snapshot` (the snapshot writes, FIX-1127). The `*/10` dispatcher fires `platform-snapshot.yml`, whose only job is now FIX-1026's request-path probe; the name was kept deliberately (the workflow header says so). Nothing changed.
10. **Environment:**
    - `../civitics-worktrees/fix-1194` was again a de-registered orphan. The first `session:worktree:done 1194` hit EPERM because my own shell's cwd was inside it; the second removed it.
    - Discarding an uncommitted draft bullet with `git checkout -- docs/FIXES.md` was denied, so the filing was committed and rebased over FIX-1219 instead (both bullets kept).
    - Prod reads and the push ran the primary's `db-query.mjs`, `db-push-prod.mjs` and `.env.local.prod` by absolute path from the worktree. Nothing ran in, or was written to, the primary tree except reads of its env files.
    - The prompt's "Written … Fri 00:05 UTC" header was ~23 min ahead of the clock at start (23:42 UTC).
