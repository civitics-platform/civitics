---
cc: 148
prompt: cc-prompt-148-fix1212-relaunch-runner-paced-windows-wall-threshold-3s-census-per-call-resume-cursor-tonight.md
started_at: 2026-09-23T23:07:54Z
finished_at: 2026-09-24T00:28:10Z
head_before: aadf5058
head_after: c0199205
commits:
  - {sha: eb0018dd, subject: "fix(data): FIX-1212 — the bootstrap runner paces windows (N per CALL, a breather until the watchdogs recover), trips on the forker and the front door, and treats a 1–3 s watchdog wall as this op's load"}
  - {sha: fdb16614, subject: "docs(fixes): file FIX-1216 (the 7-day cycle age treadmills a weekly firing)"}
  - {sha: c0199205, subject: "docs(fixes): FIX-1212 / 1215 appends (cc-148: the paced runner is on main and rehearsed; the relaunch did not launch)"}
fixes_closed: []
fixes_filed: [FIX-1216]
fixes_reopened: []
migrations_pushed: []
prod_writes: "none to the database. Every prod contact was a read: db-query.mjs --prod (read-only by construction), the Logs API census, and Cloudflare analytics. The runner was NOT launched, so there was no claim, no donor_party_crawl write, and no CALL. Pushes to main at 23:52 (fdb16614) and ~00:25 UTC (c0199205) deploy to Vercel; no app-route code changed."
ci: green
stopped_items:
  - "Item 5, the prod launch, did NOT run: launch gate (iv) failed. The 60-min census read 6 x 57014 at 23:55 UTC and 12 at 00:23 UTC (ratio 3.03, then ~6, against <= 2). All 12 were PostgREST front-door reads (11 financial_relationships, 1 check_senate_reference_cohort), in bursts at 23:30-23:35 (1), 23:40-23:42 (5) and 00:08-00:23 (6). The window could not come back under 3 events before ~01:10, after the 01:00 cutoff (vi). Every other gate held: CI green, both rehearsals passed, cursor [1,2], crawl row absent, interlock clear, and prod_op_gate ok=true from ~23:43. Next slot: Thu ~23:15 UTC, same command."
  - "Item 6 (the Pattern B record) did not apply: there was no caught_up. FIX-1212 stays open with no trailer."
  - "Item 8's chore(fixes) sync was not run: no Fixes/Closes trailer landed, so fixes:sync has nothing to append."
  - "Item 7 appends were written for FIX-1212 and FIX-1215 only. FIX-969's condition (jobid 17's first caught_up) was not met. FIX-1125 and FIX-1194 have no new prod per-window wall or watchdog series, because no prod CALL ran. FIX-1216 was filed this run with its evidence."
  - "The unit suites for @civitics/app-civitics and @civitics/db were not run locally (the change is in packages/data only); tests.yml ran all three green on fdb16614 (run 35935745258)."
---

# cc-148: the paced runner is built, rehearsed and on main, but the relaunch did not launch because the front-door census held it

**FIX-1212 is still open, and prod is exactly as cc-147 left it.** The cursor
is `windows_done [1,2]` (started 09-23 09:02:48), the watermark is
2026-07-28 05:54:28, and there is no crawl row.

**The runner is ready for Thursday.** Commit eb0018dd adds:
- two windows per CALL;
- a breather after each CALL until the watchdogs recover;
- a 3.0 s wall trip, with 1–3 s logged as elevated load rather than a stop;
- a census before every CALL;
- the cursor read and logged before the first CALL;
- a check that `caught_up` really left no cursor and moved the watermark to the target.

Both clone rehearsals passed. `pnpm cc:verify` is the last step (below).

**Why there was no launch.** Every gate held except the front door:
- `prod_op_gate` opened at about 23:43.
- CI was green.
- The cursor was `[1,2]`.

The census, though, counted 12 front-door 57014s in the hour before the
01:00 cutoff. All were PostgREST reads, mostly on `financial_relationships`,
and none came from a database op. Launching into that is exactly what
rule 65 forbids.

Thu ~23:15 UTC is the next slot, with the same command. If Thursday cannot
launch either, the Tue 09-29 15:00 firing will resume the cursor
unsupervised for about 10 windows.

## 1. Reads 2–5

**Read 1.**
- `git pull` found nothing new. `session:check`: 55 open.
- `tests.yml` has seven checks.
- `supabase migration list` was not re-run, because this is a code-only run with no migration. HEAD's newest migration file is still `20260920120000`.
- `.env.local` points at local.

**Read 2 — FIX-1212 on Pro at 23:09 UTC: every expectation held.**
- `donor_party_full_rebuild` = `{mode full, target 2026-09-21T00:18:25.831727, started_at 2026-09-23T09:02:48.434717, windows_done [1,2]}`.
- `donor_party_crawl` absent.
- Watermark 2026-07-28 05:54:28.48494.
- MV 1,728,538 rows / SUM 732,520,894,800.
- `donor_party_rollup_refresh` rows since 09:00: exactly `0e7c8e74`.
- `prod_session_state()`: held=false, no label, live_writers [].
- 0 `civitics_dp_bootstrap` backends.
- The same reads at 00:26 UTC were identical.

**Read 3 — the runner at 3a8eff87.**
- The flag table and the hard-coded `thresholdS: 1.0` at both `evaluateWatchdogs` call sites were as the prompt said.
- The probe row was written only when `n === 1` and restored only on the probe.
- `maxCalls` defaulted to 8.
- The census ran every 15 min through `maybeCensus`.
- The outcome vocabulary is caught_up / stopped / skipped / gate_timeout / census_fail / max_calls / error.

**Read 4 — the instruments at 23:09 UTC (the evening baseline).**

The last 30 completed runs of each watchdog:

| watchdog | min s | median s | max s |
|---|---|---|---|
| cron-job-budget-watchdog | 0.014 | 0.016 | 0.027 |
| derived-mvs-unit-watchdog | 0.003 | 0.004 | 0.007 |

- `job startup timeout` failures in the last 6 h: 0.
- Census, 60 min (22:19–23:19): 0 × 57014 against the 0.033/min baseline (ratio 0); edge 0 of 192 requests 5xx; pass.
- `prod_op_gate(3600)` at 23:09:20 read `ok=false`, blocked only by `a:nightly_due` ("nightly due since 22:35:00 UTC and no nightly_cron row yet"). Its other readings:
  - vacuum means: ec 299.9 s, fr 118.7 s, fe 31.2 s;
  - blackout 05:45–09:00;
  - watchdogs 30/30 runs;
  - 0 stuck units of 17.

**Read 5 — the 09-23 nightly started late.**

| phase | ran (UTC) |
|---|---|
| fec | 23:22–23:27 |
| enrichment-heavy | 23:27 |
| enrichment-light | 23:28 |
| enrichment-tail | 23:28–23:33 |

GHA `nightly-sync` ran 23:21:55–23:34:36. With the 10-min phase guard, the
gate should open at about 23:43. It read `ok=true, blocked_by []` at
23:49:28.

## 2. D1–D3: the changed lines, the tests, the rehearsals

**D1 — the stop rule** (`evaluateWatchdogs`, `elevatedJobs`, `evaluateCensus`).
- `--wall-trip-s` (default 3.0) is passed at both call sites and compared with `>=`.
- One completed run still counts as one vote (3a8eff87).
- The functions now return a `Trip {rule, reason, job, run_ids}` instead of a string.
- Walls in `[1.0, wall-trip-s)` are marked `(elevated)` in every tick, breather and pre-CALL line. They are counted in `elevated_ticks` and in each CALL's `elevated` count, and never trip.
- The census runs before every CALL (`--minutes 15`, after the breather), plus the 15-min cadence during long CALLs.
- Rules (1) and (4) are unchanged. The pre-CALL reading now counts startup timeouts from the *previous* CALL's start, so one during a breather is not missed.

**D2 — pacing.**
- `--units-per-call` (default 2) replaces `--probe-units`. The old flag is now refused as unknown, so an old command cannot silently run unpaced.
- The crawl row is upserted before every CALL and restored after every CALL (see §5), and again in both `finally` blocks.
- `breathe()` runs from CALL 2:
  - it reads every 30 s;
  - it releases when both watchdogs' latest *completed* run started after the CALL returned (on the DB clock) with a wall under `--breather-until-wall-s`;
  - otherwise it gives up at `--breather-max-s` with `breather_timeout`;
  - it still evaluates the real stop rule while it waits;
  - it writes one receipt row each (`waited_s`, `released_by`, `walls_at_release`, `pending_at_release`).
- New defaults: `--max-calls` 12 and `--expected-minutes` 60.

**D3 — resume and the caught_up check.**
- `resumeWindow()` logs `resuming at window N`. `receipt.resume` carries `windows_done_before` and `target_before`.
- The runner never writes the cursor.
- On `caught_up`, one SQL read asserts two things: the cursor is gone, and the watermark equals the cycle target (a timestamptz comparison, so microseconds survive). A mismatch is `outcome: error`.

**Also: receipts no longer overwrite each other.** `receiptPaths` now adds a
`-HHMMSSZ` suffix when the file already exists (§5, first item).

**Tests.**
- The runner suite went from 13 to 19 tests. New or changed:
  - 2.9 s on four distinct runs stays elevated;
  - 3.0 s on two distinct runs trips and names `[R10, R12]`;
  - the cc-147 probe profile (0.874, 1.341, 1.341 s) is now load, not a trip;
  - the elevated band's edges (0.999 / 1.0 / 2.999 / 3.0);
  - the breather ignores a run that straddled the CALL, keeps waiting on 0.612 s, and releases on 0.02 / 0.004 s;
  - resume window; the caught_up mismatch cases; the receipt suffix;
  - the new args, including the full cc-148 launch command and `--probe-units` refused.
- `@civitics/data`: 908 → 914 (907 pass, 7 skipped, 0 fail).
- Turbo typecheck and lint: 19/19.
- The four checks: reads 158/0, render-timeouts 161/0, workflow-parity 0 undeclared, proconfig 20 migrations / 0 undeclared.
- Script suites: fixes, fix-add, cc-verify, session-worktree, drain, check-proconfig 18/0, db-query 12/0.
- `fixes:check`: clean.

**Rehearsal 1 — clone, resume path.**

Setup, all local:
- The watermark was set 60 days back to 2026-06-21.
- The cursor was seeded as `{full, target 2026-09-03T01:34:43.542642 (the clone's LEAST(max FR.updated_at, horizon)), started_at now, [1,2]}`.
- A scratch `nightly_cron` row (complete, 22:40–22:50) was added. After 22:35 a clone reads `nightly_due`, because it never runs a nightly.
- The blackout needed no edit at 23:xx.

The run:
- Command: `--units-per-call 2 --breather-max-s 20 --poll-seconds 5 --max-wait-minutes 45`.
- The gate held 23:17–23:40 on the local watchdogs' run count: local Docker had slept 13:14–22:46, so they had fewer than 28 runs in 60 min.
- It resumed at window 3.

| CALL | wall s | windows | stage s |
|---|---|---|---|
| 1 | 70.5 | 3, 4 | 43.74, 24.53 (cold) |
| 2 | 22.4 | 5, 6 | 11.30, 9.31 |
| 3 | 13.4 | 7, 8 | 5.81, 5.51 |
| 4 | 10.7 | 9, 10 | 4.98, 4.01 |
| 5 | 10.7 | 11, 12 | 4.39, 4.30 |
| 6 | 58.7 | 13, 14 | 11.83, 40.86 |
| 7 | 44.4 | 15, 16 | 20.36, 17.99 |

- Six breathers ran. The one before CALL 3 released on walls (0.026 / 0.023 s). The other five were `breather_timeout` at 20 s.
- `caught_up`: cursor gone, and the watermark 2026-09-03 01:34:43.542642 equals the target.
- The crawl row was upserted 7 times and restored 7 times, and is absent.
- Elevated ticks: 0. The CALL was verified no longer running. The claim was released; exit 0.
- Receipt: `docs/audits/2026-09-23-fix1212-bootstrap-runner-local-231801Z.md`.

**Rehearsal 2 — clone, the trip.**

The clone was re-seeded the same way, and the run used
`--trip-on-wall-ms 0 --trip-from-call 2 --tick-seconds 2`.

- CALL 1 banked windows 3–4 in 66.0 s, followed by a `breather_timeout`.
- The trip fired at CALL 2 +2 s: rule (2), job `cron-job-budget-watchdog`, both run ids named.
- `pg_cancel_backend` landed in window 5. The row closed partial: "canceled — window 5: canceling statement due to user request".
- The cursor kept `[1,2,3,4]`, the watermark did not move, and the crawl row was restored (2 upserts / 2 restores).
- `backend_gone_verified` is true; `stopped`, exit 4.
- Receipt: `…-local-trip-234945Z.md`. Its trip text says "two distinct runs"; the committed code now says "two consecutive readings" for the test trip (§5).

**The clone was restored after both:** watermark 2026-08-20 00:13:37.208343,
no cursor, no crawl row, blackout `[]`, the scratch nightly row deleted,
session clear.

## 3. Item 5: the launch that did not happen

**Gates at 23:53 UTC:**
- (i) eb0018dd and fdb16614 on `origin/main`; `tests.yml` run 35935745258 green on fdb16614, with all seven checks passing as their own steps.
- (ii) both rehearsals passed.
- (iii) cursor `[1,2]`; crawl row absent.
- (v) held=false, live_writers [], 0 bootstrap backends.
- (vi) the clock was fine.
- `prod_op_gate(3600)`: ok=true, blocked_by [].

**Gate (iv) failed.** The 60-min census at 23:55:26 read:
- 6 × 57014 = 0.1/min, ratio 3.03 (> 2), so pass=false;
- edge last-closed bucket: 6 of 1,034 requests 5xx = 0.58 % (under 1 %, passes).

The Supabase edge buckets went 95 → 192 → **1,443** (23:15) → **1,034**
(23:30) requests per 15 minutes. Cloudflare showed civitics.com quiet in
the 23:00 hour (123 requests), so the jump was on the API side.

**Census re-reads** (the launch window ran to 01:00, so I kept reading every
5 min):

| UTC | 60 min | 15 min |
|---|---|---|
| 23:57 | 6 FAIL | 0 |
| 00:02 | 6 FAIL | 0 |
| 00:07 | 6 FAIL | 0 |
| 00:13 | 6 FAIL | 0 |
| 00:18 | 6 FAIL | 0 |
| 00:23 | **12 FAIL** | **6 FAIL** |

- 5-min slices put the events at 23:30–23:35 (1) and 23:40–23:42 (5); the third burst was 00:08–00:23 (6).
- `--by query` attribution: financial_relationships 11, check_senate_reference_cohort 1.
- `--by application_name`: postgrest 12. `--by user_name`: authenticator 12.
- Nothing was active on the database. The only non-watchdog cron runs were ec-crawl and fe-crawl at 0.0–24.2 s. No pipeline ran after the nightly closed at 23:33.

After 00:23, passing needed at least three of the 00:08–00:23 events to
roll out of the window, which could not happen before about 01:10. **No
launch**, per the gate. I stopped reading at 00:23.

**What was not measured tonight:** window walls under pacing, the watchdog
series per CALL, and whether the box recovers between CALLs. Those are
still Thursday's to measure.

## 4. FIX status, commits, CI

- **Status:**
  - FIX-1212 open (no row).
  - FIX-1216 filed, open.
  - FIX-1213 closed (82f69001).
  - FIX-1215 closed (6a826bf9).
  - FIX-969, 1125 and 1178 open.
- **Open: 55 → 56.** FIX-1216 was added and nothing closed. The prompt expected 56 if 1212 did not close.
- **Commits:** aadf5058 → c0199205, all on `origin/main`:
  - eb0018dd (the runner);
  - fdb16614 (FIX-1216);
  - c0199205 (appends to FIX-1212 and FIX-1215).
  - dbc09aa6 (`docs(receipts): 2026-09-24`) landed between them from the nightly; it is not this run's.
- **CI:** `tests.yml` green on fdb16614 (run 35935745258). c0199205 is docs-only, so paths-ignored. `fixes-integrity` green.

## 5. What contradicted cc-147, the receipt, or this prompt

**1. The tree would have overwritten the design of record.**
`receiptPaths` named a prod receipt `${day}-fix1212-bootstrap-runner.md`.
Tonight's relaunch fell on the same UTC day as cc-147's probe, so the
runner's `finally` would have replaced
`docs/audits/2026-09-23-fix1212-bootstrap-runner.md`, the receipt this
prompt rests on. The fix now adds a `-HHMMSSZ` suffix for a second launch
on the same day. Both rehearsals exercised it (`-231801Z`, `-234945Z`).

**2. The binding condition tonight was the front door, not the box and not the gate.**
- The gate did its job: blocked on `nightly_due` at 23:09, open by 23:49.
- The census held the launch on PostgREST `financial_relationships` reads timing out with nothing heavy running in the database.
- Over the trailing 24 h there were 47 front-door 57014s, 0.033/min, which is exactly the baseline. **72 % of them (34) were `financial_relationships` reads**, and 10 were `enrichment_queue`. So the baseline the gate compares against is mostly one read that times out.
- Tonight's 12 in about an hour was a burst on top of that.
- Two deploys landed near the bursts: 23:36:06 (dbc09aa6) and 23:54:18 (fdb16614). The 23:40–23:42 burst began 4–6 minutes after the first. That is correlation, not an established cause; the 00:08 burst matches the second deploy less well.
- **Not filed.** Candidate filing: which route issues the `financial_relationships` read that dominates the front-door baseline.

**3. At `--minutes 15`, one event is enough to trip.** The baseline expects
0.033 × 15 = 0.5 events in 15 minutes, so a single 57014 reads as ratio 2.02
and fails. D1(b) adds seven such checks, one before each CALL, on top of the
15-min cadence. On a night like 09-23/24 (47 events in 24 h), the chance of
at least one event in some pre-CALL window over a ~1 h run is high even with
no burst. This is "as today" in kind, but the per-CALL census multiplies how
often it gets checked. Decide before Thursday whether the pre-CALL census
should use `--minutes 60`, the same rule as the pre-launch census, which
tolerates up to 3. Noted in the FIX-1212 append; not changed, because the
prompt set 15.

**4. The header's clock was off.** It said the weekday nightly runs "~22:35–23:00".
It ran 23:22–23:33, and the two nights before started 23:23 and 23:44 (GHA
`nightly.yml` start times). The gate's `nightly_due` handles this correctly;
it just opens later than the prose said. The prompt's launch estimate of
~23:45 would have been right on the gate.

**5. The clone needs two things before it can pass the gate late at night.**
- A scratch `nightly_cron` row: from 22:35 to 01:35 a clone reads (a) `nightly_due`.
- Watchdogs with at least 28 runs in the last 60 min, which it won't have if local Docker slept.
- D4's "blackout scratch-edited clear" was not needed at 23:xx, because the 05:45–09:00 fallback does not intersect the span.

**6. Breathers on the clone mostly timed out.** Release needs a watchdog run
that started after the CALL returned, and on the */2 cadence that is up to
120 s away, 60 s on average. So at `--breather-max-s 20`, five of six
breathers timed out. On prod with 600 s, each breather will last about 1–2
minutes when the box recovers the way cc-147 measured (0.612 / 0.061 s within
a minute). That adds roughly 6–12 minutes over seven CALLs, inside the
prompt's 40–70 min estimate.

**7. Two departures from the prompt's wording, both deliberate.**
- **The crawl row is restored after every CALL**, not only after the last one. The difference: a hard kill during a breather leaves no `{max_units: 2}` row behind for Tuesday's unsupervised firing to inherit.
- **The test trip counts every reading** (the distinct-run check is off), and it never fires during a breather. Without that, the cancel cannot land inside a 10–20 s clone CALL. That was cc-147's rehearsal-3 gap, now closed.
- The committed trip text says "two consecutive readings" for the test trip. Rehearsal 2's receipt predates that one-line wording fix.

**8. Known and unchanged.**
- The claim label still reads `FIX-1212 bootstrap (cc-147 runner)`.
- The procedure's `source: 'pg_cron'` mislabel remains.
- `claimProdSession` holds 16 guarded pipelines where the gate counts 17.
- The treadmill is now FIX-1216.
