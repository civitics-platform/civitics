---
cc: 141
prompt: cc-prompt-141-monday-drop-night-reads-fix1191-fix1184-records-residue-reaudit-fix1182-exp1-fix1106-rescoped-apply-jobid17-restart-read.md
started_at: 2026-09-21T23:21:00Z
finished_at: 2026-09-22T00:05:00Z
head_before: 9c4e29e2
head_after: 27134ff1
commits:
  - {sha: 9b1fc92c, subject: "feat(receipts): FR daily vacuum joins the section-4 series; VACUUM_JOBS by name"}
  - {sha: 45d136b9, subject: "docs(fixes): fifteen appends - 1191 1184 1196 1182 1106 1074 1177 1172 1181 1169 1178 969 1203 1194 1125"}
  - {sha: de0b4339, subject: "fix(db): FIX-1191 - fr-vacuum-analyze daily at 03:00 has its receipt"}
  - {sha: 27134ff1, subject: "chore(fixes): sync status after FIX-1191"}
fixes_closed: [FIX-1191]
fixes_filed: []
fixes_reopened: []
migrations_pushed: []
prod_writes: none
ci: green
stopped_items:
  - "Items 3-5 (three prod residue audits / experiment (1) / derived manifests / both dry runs) NOT RUN. Session opened 23:21 UTC, inside the prompt's own 22:30-01:00 blackout, so template gate (g) failed. Craig chose defer over a ~1.6h hold."
  - "Items 6-7 (the FIX-1106 apply) NOT RUN. Both Monday windows (12:50-17:00 and 18:50-21:30 UTC) had closed before the session opened; the apply also needs a per-set go-ahead inside a valid window."
  - "Read 11(d) (clone EXPLAIN of jobid 17's heaviest statement) SKIPPED. It existed to characterise a memory blowup; read 11(e) refuted the causal hypothesis and Craig closed the question, so it no longer bears on a decision."
  - "FIX-1184 record NOT MADE. Read 4's precondition - the supersession visible on prod - is unmeetable, because the rows were destroyed by the 09-15 crash recovery rather than superseded."
  - "The jobid-17 FIX filing NOT FILED, by Craig's decision. Read 11(e) refutes the premise asserted in the title the prompt pre-wrote; the negative result is recorded on FIX-969 instead."
  - "FIX-1182 / FIX-1074 / FIX-1177 / FIX-1172 closes NOT MADE. Each was conditional on work that did not run."
---

# cc-141 — Monday after the drop night

Posture actually exercised: **prod reads only, no writes, no migration, no apply.**
One code commit, fifteen appends, one record.

The session opened at **23:21 UTC**, which is inside the prompt's own
22:30–01:00 blackout, and after both of Monday's nominated apply windows had
closed. That fact decided the shape of the run before any measurement: the three
~250 s audits and the FIX-1106 apply were out of window from the first minute.
Craig took the two decisions in-session — leave jobid 17 active and record the
negative result; do the reads and the code item now, defer the audits.

---

## 1. The drop night

GHA run **35542803868**, `fec-phase` job 106163402116, 22:49:08 → 00:21:18 UTC
(5,530 s wall; the receipts' 5,496.7 s phase plus ~27 s of setup). Interlock
acquired 22:52:15, released 00:21:12 — no `[prod-session]` deferral, so the
phase ran with the interlock clear.

**2(a) promote-candidates** — one line, no `moved` line anywhere:

```
promote-candidates: detected=0 promoted=0 failed=0 skipped_bound=527
```

527 is exactly Saturday's figure. No STOP.

**2(b) cn26** — `new=3` is the stub-mint count:

```
cn26: lines=8620 new=3 existing=8616 name_updated=19 no_office=0 failed=0
```

**2(c) the FIX-941 collision block — PRESENT, where the prompt expected it
absent.** Two blocks, 8 CAND_IDs and 5 FEC IDs:

```
⚠ FIX-941: 8 CAND_ID(s) claimed by more than one officials row. Resolved by tier preference (elected wins) instead of uuid order:
  H2CA01177 … H0CA50178 … H0CA53115 … H0NE02125 … H4FL07137 … H2NJ01138 … H8NV03200 … H0IA02180
⚠ FIX-941: 5 FEC ID(s) claimed by more than one officials row …
Officials indexed by FEC ID: 19606
```

Not a STOP, and not a FIX-1196 regression: **none of the 8 ids is one of the
twelve held ids**, every collision is `(candidate)` vs `(candidate)` — so the
"elected wins" preference never actually discriminated — and each resolved with
a KEEPS/refused outcome rather than a failure. This is the known duplicate
candidate-row population, surfacing where the prompt assumed silence.

**2(d)** three cycle-2026 emit sets plus one 2024, matching the receipts exactly:

```
⟳ emit set complete — cycle 2024 fec_bulk_pac: 135,358 keys
⟳ emit set complete — cycle 2026 fec_bulk_pac: 112,592 keys
⟳ emit set complete — cycle 2026 fec_bulk_indiv: 1,015,185 keys
⟳ emit set complete — cycle 2026 fec_bulk_indiv_to_committee: 1,051,502 keys
⟳ Cleared fec_bulk_run_state — cycle 2026 complete (FIX-754)
```

### The twelve ids — all hold

The all-surfaces census and the authoritative census disagree, and the
**authoritative** one is the test that matters (`restore-fec-ids.ts:105-108`
says so explicitly: a retired marker on some *other* row is the correct
post-merge shape, and counting it as a holder "reports a correct restore as a
failure"). Authoritative holders, all twelve:

| cand_id | holders | who |
|---|---|---|
| H0CA27085 / S4CA00555 | 1 / 1 | Adam B. Schiff [elected] |
| H8MI09068 / S4MI00355 | 1 / 1 | Gary C. Peters [elected] |
| H8UT03238 / S4UT00282 | 1 / 1 | John R. Curtis [elected] |
| H8WY00148 / S0WY00137 | 1 / 1 | Cynthia M. Lummis [elected] |
| H6KS01179 / S0KS00315 | 1 / 1 | Roger Marshall [elected] |
| H2NJ13075 | 1 | Robert Menendez [elected] |
| H6NC09200 | 1 | Mark Harris [elected] |

Stubs for the twelve held ids: **0**. The run's own minting: **3 rows at
2026-09-20 22:53:36.428 UTC**, equal to cn26's `new=3` logged one second later
— two instruments, one population. No STOP.

**Two of the prompt's stated expectations were wrong, and the data is right.**
(i) "the four S ids and the set-1 ids `live=1`" — set 1 contains only *one*
Senate id. The live/prior split is by chamber, not by set: every Senate id is
`live`, every House id is `prior`, which is exactly right for seven
House-to-Senate movers. (ii) "every `merged=0`" — nine of twelve carry
`merged=1`, which is the correct post-merge shape per the script's own comment.

---

## 2. The instrument reads

**FR — two readings, both 100 %, so D1 holds.**

| reading | when (UTC) | pct_all_visible | dead |
|---|---|---|---|
| 1 (receipts) | 2026-09-21T00:27 | 100 % | 67,144 |
| 2 (this run) | 2026-09-21T23:37:53 | **100.00 %** (631,265/631,265) | **0** |

`fr-vacuum-analyze`: Mon 09-21 03:00 = 161.1 s succeeded; Sun 09-20 = 212.0 s;
then a gap back to 09-14 01:00 — the visible trace of the slot only becoming
daily on 09-20. FR held 100 % across a full day that included a ~1M-row emit.

**EC drain — fully drained, no STOP.** `ec_crawl_gate()` returns
`{"run": false, "reason": "cycle_cooldown", "detail": "last cycle closed 291 min ago; minimum interval is 360 min"}`
→ 69 min of cooldown left. `last_indexed_at` **and all sixteen windows** sit at
`2026-09-21 00:18:25.831727+00`, past set 2's 09-19 19:17 commit; nothing is
stranded at `09-17 23:20:27`. The arm closed a 16/16 cycle at 18:47:13.

**The `cycle` sub-key question is settled: FIX-1116 holds.** The row has
**exactly two** top-level keys, `last_indexed_at` and `windows`. So the
`v_state->'cycle'` read at `20260903010000_fix983…:2536` can never return
anything on prod — it is dead code against the real shape.

**Startup timeouts 09-19 → now: ZERO.** No `job startup timeout` on any job and
**zero** `cron_job_budget_action` rows. Nothing to attribute.

**First `vm_probe` rows:**

| probe | at (UTC) | relation | pct | dead | last_vacuum |
|---|---|---|---|---|---|
| pre-fr | 02:55:00 | financial_relationships | 100.0 | 67,144 | 09-20 03:03 |
| pre-ec | 04:25:00 | entity_connections | 91.3 | 112,391 | 09-20 04:30 |
| pre-ec | 04:25:00 | **votes** | 82.6 | 26,929 | **(null)** |
| pre-fe | 04:45:00 | financial_entities | 100.0 | 77,541 | 09-21 00:20 |

Two things fall out. FR was **already at 100 %** when its 03:00 vacuum fired, so
the daily slot now pays 161 s to clear dead tuples rather than to repair the
visibility map. And EC went 91.3 % (04:25) → **76.44 %** (23:37) *despite* its
own daily vacuum. The two relations want opposite gates — which is the sharpest
input FIX-1169 has had. `votes` sits in the probe with `last_vacuum` NULL: a
relation being measured that no vacuum job owns.

**First prod `phase_seconds`** — `run_rule_taggers`, 09-21 06:30 UTC:
`{"delete": 46.5, "insert": 947.6}`, wall 994 s, 868,167 rows. That is **95.3 %
INSERT** against cc-138's clone prediction of 94.3 %: the clone got the *shape*
right while understating scale 6–8× (delete 7.5→46.5, insert 124.3→947.6). 994 s
is the **fourth** consecutive above-band run (band 713–858; 998.9 / 987.3 /
994.0), drifting further above the 899.5 / 891.2 / 873.3 the bullet was filed on.

**FIX-1181 weekly read #1 — clean.** pgss `stats_reset` =
`2026-09-15 15:09:48.238+00`, i.e. the crash restart itself, independently
corroborating `pg_postmaster_start_time()` (15:09:48.333). 600 `service_role`
completions, max **7,577 ms**, mean 104 ms; `postgres` 2 calls, max 94 ms. No
completion past the 8,000 ms bound → no recurrence, though 7,577 ms is within
5 % of it. Three clean weeks still owed.

**`entity_tags` idx_scan (note only, for cc-143 D4):**

| index | idx_scan | size |
|---|---|---|
| entity_tags_entity_type_entity_id_tag_tag_category_key | 9,579,952 | 751 MB |
| entity_tags_fe_industry_content | 541,408 | 3.6 MB |
| idx_entity_tags_entity | 14,247 | 471 MB |
| entity_tags_type_category_id | 68 | 880 MB |
| idx_entity_tags_tag | 18 | 98 MB |
| idx_entity_tags_topic | 12 | 200 kB |
| entity_tags_pkey | 1 | 376 MB |
| **idx_entity_tags_visibility** | **0** | **450 MB** |

---

## 3. Read 11 — jobid 17 vs the restarts: REFUTED

Done first, because the Logs API window for 2026-09-15 closed Tue ~15:09 UTC.

**11(c), the log itself.** Filtered for OOM / signal / shutdown / ready /
donor_party across 15:00–15:20, exactly one row came back — the job starting.
Widening to every row in 15:07–15:13:

```
15:08:00.197  cron job 44 starting: SELECT public.enforce_cron_job_budgets();
15:08:01.829  cron job 44 completed: 1 row
     ←—— 106 seconds, zero log lines ——→
15:09:48.281  ending log output to stderr
15:09:48.281  starting PostgreSQL 17.6 on aarch64-unknown-linux-gnu …
15:09:48.341  database system was interrupted; last known up at 2026-09-15 15:06:05 UTC
15:09:50.718  pg_cron scheduler started
```

Coverage is continuous 14:40–15:40 (2–6 rows/min, spiking to 12 at 15:09), so
nothing was lost. **Present:** crash recovery — `was interrupted`, not `was shut
down`. **Absent:** `out of memory`, `terminated by signal`, `server process …
was terminated`, any shutdown request. A backend OOM-kill writes
`terminated by signal 9` and `terminating any other active server processes`;
neither appears. The **postmaster itself** vanished without logging, which is a
container/host-level event, not a Postgres-side memory kill. The box was
measurably healthy 106 s earlier — a time checkpoint writing 14 buffers (0.0 %)
and both `*/2` watchdogs completing in under 2 s.

**11(e) is what actually decides it.** Every `server restarted` mark in full
retention:

| restart | day | job in flight |
|---|---|---|
| 08-18 ~10:00 | Tuesday | rule-taggers-weekly |
| 08-24 ~16:00 | Monday | entity-connection-stats-rebuild |
| 08-26 ~16:00 | Wednesday | entity-connection-stats-rebuild |
| 08-29 ~06:00 | Saturday | ec-crawl + refresh-derived-mvs-daily |
| 08-29 ~07:30 | Saturday | ec-crawl |
| 09-08 ~15:12 | Tuesday | **donor-party-rollup-refresh** |
| 09-15 ~15:09:48 | Tuesday | **donor-party-rollup-refresh** |

Seven restarts across five weekdays; jobid 17 in flight for **two**. Only long
jobs are ever caught — precisely what random restart timing produces, since
catch probability scales with time in flight. No causation is required.

**11(a)/(b).** jobid 17 is `active = t` on `0 15 * * 2`, budget 1,800 s, and has
**zero successes since 2026-07-28**: 08-04 hit the 6 h ceiling (21,628.6 s);
08-11/08-18/08-25 startup timeouts; 09-01 cancelled at 1,814.6 s by the watchdog
naming `SELECT array_agg(DISTINCT fr.from_id)`; 09-08 and 09-15 server restarted.

**And the one confirmed resource failure is already fixed.** FIX-950 removed
that array build — its own comment says *"That array build IS the 1,814 s that
the watchdog killed on 2026-09-01. It is gone."* — and prod confirms:
`prosrc ILIKE '%array_agg(DISTINCT fr.from_id)%'` = **false**. So the 09-15
crash cannot be the 09-01 cause recurring.

jobid 26 `treemap-individuals-global-refresh` (Tue 14:00) is 2 of 4 over 30 days
and **succeeded on both restart days** (09-15 994.1 s, 09-08 1084.2 s), finishing
~53 min before the 15:09:48 restart. Not implicated.

**Craig's decision (2026-09-21):** leave jobid 17 **active**, file the negative
result. No `cron.alter_job`, no `rollup_watch_overrides` row, no FIX filed — the
title the prompt pre-wrote asserts the premise the evidence removes. FIX-969
carries it.

---

## 4. Audits and experiment (1) — not run

Deferred by Craig. The session was inside the blackout; gate (g) failed on the
clock, not on the data. Nothing was measured against the 09-14 baseline, so the
indiv ≈ 34 expectation, the ITC 626 / $536.8M class and the pac numbers are all
still owed, and no derived manifest exists.

**But one premise moved underneath the design while nobody was looking** — see
§5. Set A's prod copy is gone.

---

## 5. What contradicted the design

**1. Both emit tables are UNLOGGED, and prod crash-recovered on 09-15 — so the
FIX-1184 evidence and the FIX-1182 experiment's set A were destroyed.**

This is the finding of the run, and it ties reads 4 and 11 together.

```
relname        | relpersistence | reltuples
fec_emit_keys  | u              | 2518699
fec_emit_runs  | u              | -1
```

PostgreSQL truncates unlogged tables on crash recovery. Consequences, all
measured:

- `fec_emit_runs` holds **6 rows**, every one started 09-19 or later.
- The three 09-12/13 open pac rows (`96260b92`, `5697a031`, `b09496d6`) and the
  09-14 complete indiv run `84470d3b`: **all four absent**, not superseded.
- `superseded_at` is set on **none**; open rows **0**.
- `84470d3b` holds **zero** emit keys.

Nothing in the tree deletes these rows — no SQL, no TypeScript, no procedure on
prod references the table — and `emit-set.ts:119-140` only ever supersedes
`complete_at IS NULL` rows, so it cannot explain a missing *complete* run. The
wipe is the only explanation that fits, and it fits every row.

So **FIX-1184 does not record**: the drop night had nothing older to supersede,
and the mechanism has still never been exercised against prod. A zero open-row
count is the right state, but it is not evidence for the code path.

And **FIX-1182's set A now exists only on disk.** The tracked
`docs/audits/2026-09-14-fix1106-residue-2026-fec_bulk_indiv.tsv` still carries
`run_id=84470d3b-d147-5a01-b43a-e4a4d7930890`, `1,014,747 keys`,
`DROPPED-OUT 128 recipients / 67933 rows / $106,645,325`. That file is now the
sole authority the two-set comparison can join against; a two-SET *key*
comparison is no longer available from the database at all. The prompt
anticipated the keys might be gone — it did not anticipate the run row going
with them, nor the reason.

Worth a follow-up somebody should file: an UNLOGGED table holds the reference
set that a destructive FIX-1106 apply keys on, and any crash silently destroys
it. The audit guards key on the newest run for a slice, so the failure mode
after a wipe is not an error — it is a *different*, smaller baseline that still
looks well-formed.

**2. The jobid-17 hypothesis is refuted, not confirmed** — §3. The prompt's
STOP fired on its letter (crash recovery is present) while its substance
dissolved. The filing rule it carried would have written a 🔴 FIX asserting the
job "may be taking the box down"; the census says otherwise.

**3. Read 3's two stated expectations were wrong** — §1. The live/prior split is
by chamber, and `merged=1` is the correct post-merge shape.

**4. The FIX-941 collision block was expected absent and is present** — §1.
Benign here, but the expectation should not be re-stated as a silence next time.

**5. Minor:** three stray untracked duplicates of the 09-14 manifests sit under
`packages/data/docs/audits/` — a wrong-cwd artefact of the run that generated
them. The tracked copies under `docs/audits/` are the real ones.

---

## 6. FIX status

`FIX-1191` **closed** (`de0b4339`, `prod-only`). `FIX-1196` remains closed from
09-17. The other thirteen stay OPEN: 969, 1074, 1106, 1125, 1169, 1172, 1177,
1178, 1181, 1182, 1184, 1194, 1203.

Open count **56 → 55**. The prompt projected 51–53 on the strength of the
audits, the apply and three conditional closes, none of which ran.

---

## 7. Commits and CI

| sha | what |
|---|---|
| `9b1fc92c` | receipts §4 gains `fr-vacuum-analyze`; section retitled "Daily vacuums" (D7, no trailer) |
| `45d136b9` | the fifteen appends |
| `de0b4339` | FIX-1191 record — Pattern B empty commit, `Fixes:` + prod |
| `27134ff1` | done.log sync |

Verification: `pnpm --filter @civitics/data test` 812 tests / 809 pass / 0 fail
(3 pre-existing skips; no test added or removed, so the count is unchanged
before and after). `turbo run typecheck` 10/10, `lint` 9/9, `check:reads` OK
(158 reads, 0 silent-zero). `pnpm fixes:check` clean after each commit.

CI: **`tests.yml` green** on `de0b4339` (run 35669355209 — the seven blocking
checks are steps inside the single `unit` job). **`fixes-integrity.yml` green**
on both `de0b4339` and `27134ff1`.

`prod_writes: none` — every prod contact this run was a read through
`scripts/db-query.mjs --prod` (read-only by construction), `gh`, or the Logs API.
