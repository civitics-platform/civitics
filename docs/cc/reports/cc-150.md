---
cc: 150
prompt: cc-prompt-150-gha-dispatcher-vercel-cron-fires-workflow-dispatch-at-the-slot-nightly-idempotent-by-nominal-day-schedule-stays-as-fallback.md
started_at: 2026-09-24T01:36:50Z
finished_at: 2026-09-24T03:05:14Z
head_before: 87cdd6cc
head_after: 414b0c43
commits:
  - {sha: 769f8dd4, subject: "docs(fixes): file FIX-1218 (GHA scheduled runs start 1.6-2.7 h after the slot; a Vercel-cron dispatcher fires workflow_dispatch at the slot)"}
  - {sha: c6ec2e8d, subject: "feat(data): FIX-1218 — the nightly is idempotent by nominal day: nominal_day + dispatched_by in nightly_cron metadata; data:nightly:preflight; slot_offset_hours as a dispatch input"}
  - {sha: 30e3b5c2, subject: "ci(nightly): FIX-1218 — workflow_dispatch inputs slot_offset_hours/dispatched_by; the offset env honours them; fec-phase preflight gates every downstream job"}
  - {sha: 68523359, subject: "feat(app): FIX-1218 — /api/cron/gha-dispatch fires workflow_dispatch at the slot with a per-call stamp; three vercel.json crons"}
  - {sha: dcc3bf7a, subject: "test(data): FIX-1218 — prod_op_gate's source anchors read the LATEST defining migration; branch (a) cases at the 21:00 start"}
  - {sha: cde8e947, subject: "feat(data): FIX-1218 — receipts §1 reads dispatched runs and the dispatcher's stamp; canary report-only keys"}
  - {sha: a478880e, subject: "[skip vercel] test(app): FIX-1218 — the hung-GitHub case holds a ref'd handle, as a real socket does"}
  - {sha: 414b0c43, subject: "docs(fixes): FIX-1218 / 1163 / 1215 / 1176 / 1011 appends; PROMPT_TEMPLATE gate (g) reads the 21:00 dispatch time, rule 71 rewritten"}
fixes_closed: []
fixes_filed: [FIX-1218]
fixes_reopened: []
migrations_pushed: [supabase/migrations/20260920140000_fix1218_gha_dispatch_stamp_and_gate_nightly_start.sql]
prod_writes: "One migration, 20260920140000, applied to Pro 02:49:52.6-02:49:54.2 UTC with db:push:prod after wait-for-gate read OK at 02:49:48Z. It created record_gha_dispatch(jsonb) and redefined prod_op_gate() with c_nightly_start 22:35 -> 21:00. Both bodies have 0 lines of diff against the file. Pushes to main: 769f8dd4 at 01:44:33 (docs, the filing), cde8e947 at 02:49:22 (the code, Vercel deploy success ~02:51), a478880e (skip vercel), 414b0c43 at 03:03:30 (docs). Three Vercel crons are live: gha-dispatch/nightly at 0 21, gha-dispatch/sync-canary-check at 0 5, gha-dispatch/platform-snapshot every 10 min. Every call upserts pipeline_state.gha_dispatch_<stem>, and the first automatic one was platform-snapshot at 03:00:10. One hand call at 02:55:05.951Z to /api/cron/gha-dispatch/sync-canary-check: GitHub 204, stamp at 02:55:07.669, run 35949260302. That dispatched run executed the canary on prod: one canary_check data_sync_log row, and one RECOVERED alert email to ADMIN_EMAIL (transitions new:gha_dispatch_stale plus three recoveries). No CALL, no VACUUM, no cron.alter_job. Every other prod contact was a read: db-query --prod, pg_get_functiondef, wait-for-gate."
ci: green
stopped_items:
  - "The token's expiry DATE is not recorded. Craig confirmed GITHUB_DISPATCH_TOKEN is in Vercel Production, minted to the scope given (civitics-platform, this repo, Actions: Read and write, 1-year expiry requested), but gave no date. The canary's gha_dispatch_refused reports the expiry day whenever it comes."
  - "FIX-1011's append rides on FIX-1176's bullet with a [[FIX-1011]] cross-link, because FIX-1011's own bullet is archived. This follows cc-149's precedent for FIX-1073."
  - "The pre-push interlock read for a478880e ran, but its value was cut off by a tail -2 before I looked. A read at 02:52:33, seconds after the push, shows held=false with empty live_writers. The runner does not claim until ~09:00. The commit was [skip vercel], so no deploy happened. Every other push had its held=false read observed first (02:48:59, 03:03:14)."
  - "Pattern B is not recorded here: the Fixes: FIX-1218 record is the next session's (see §7). The prompt's planned Closes: FIX-1163 must NOT ride on it. FIX-1163 has been closed since 2026-09-13 (bd60f873)."
  - "Commits carry Co-Authored-By only, with no Claude-Session trailer. This VS Code session has no claude.ai session URL, and I did not invent one."
---

# cc-150: the nightly now starts when we say — a Vercel cron dispatches it at 21:00 UTC; the late `schedule:` run stands down by nominal day

**Summary.**
- **The dispatcher is live on prod.** Three Vercel crons call `/api/cron/gha-dispatch/<stem>`.
  - The hand call for `sync-canary-check` went end to end: call → GitHub 204 → stamp → run created about 1.7 s later.
  - The first automatic `platform-snapshot` dispatch fired at 03:00:10 and succeeded.
- **The nightly is idempotent by nominal day.** A preflight step in fec-phase stands down the late scheduled fallback when the dispatched run already did the day.
- **Receipts §1 now reads all events.** Left alone, it would have reported every dispatched night as `missing`.
- **`prod_op_gate()` reads a 21:00 start** instead of the old 22:35.
- **Nothing closed.** The receipt is Thursday's 21:00 dispatch plus its stood-down fallback (§7).
- **Five departures from the prompt.** Each was forced by a source, and all are listed in §7.

## 1. Read 2 — the drift series

`gh run list --workflow nightly.yml --event schedule -L 30`, read 2026-09-24 01:40 UTC. Every run since `0 21` landed, as createdAt minus the 21:00 slot:

| slot day | createdAt (UTC) | offset |
|---|---|---|
| 09-10 | 22:51:49 | 1.86 h |
| 09-11 | 22:54:15 | 1.90 h |
| 09-12 | 22:44:00 | 1.73 h |
| 09-13 | 22:43:56 | 1.73 h |
| 09-14 | 23:37:42 | 2.63 h |
| 09-15 | 23:19:41 | 2.33 h |
| 09-16 | 23:26:24 | 2.44 h |
| 09-17 | 23:17:24 | 2.29 h |
| 09-18 | 22:55:34 | 1.93 h |
| 09-19 | 22:35:19 | 1.59 h |
| 09-20 | 22:49:05 | 1.82 h |
| 09-21 | 23:44:30 | 2.74 h |
| 09-22 | 23:23:25 | 2.39 h |
| 09-23 | 23:21:55 | 2.37 h |

- **n = 14.** Min 1.59 h, median 2.11 h, max 2.74 h.
- **No missing day** between 09-10 and 09-23. The 09-08 skip happened under the old slot.
- **The band has widened.** FIX-1163's last append recorded 1h44–1h54 from the first four starts; the second week sits at 2.3–2.7 h.

**platform-snapshot.yml** (configured to run every 10 min):
- 13 runs in the 48 h to 01:45 UTC, all `schedule`, against 288 advertised: 0.27 runs/hour.
- Gaps of 132–315 min, mean 233 min.
- Worse than the "roughly hourly" its own header states.

**sync-canary-check.yml** (`0 5`): starts land 3.67–5.25 h late every day of the last month, typically 09:00–10:15 UTC.

## 2. Read 3 — co-tenancy at T (rule 143)

Source: prod `cron.job` plus 14 days of `cron.job_run_details`, read 01:45 UTC.

**T = 21:00, window [21:00, 23:00].** No job fires on any weekday except the continuous ones:
- the two `*/2` watchdogs (mean 1.2–1.3 s);
- `ec-crawl` every 15 min (mean 43 s);
- `fe-crawl` every 30 min (mean 5.5 s);
- since cc-149, `box-health-probe` every minute (31–51 ms).

**The nearest scheduled jobs, all well clear:**
- The 17:05–17:20 vacuum series. The worst 14-day max, `odr-mv-vacuum-analyze` at 3,739 s, ends ~18:07.
- Jobid 17 `donor-party-rollup-refresh` on Tuesday at 15:00 (mean 11,335 s) ends ~18:09.
- `rule-taggers-weekly` on Tuesday at 16:00 (max 4,838 s) ends ≤17:21.
- After T: `agency-staffing-rollup-refresh` on Tuesday at 00:05 (50 s) and jobid 10 `refresh-derived-mvs-weekly` on Tuesday at 00:47 (mean 1,194 s).

**T = 22:35, window [22:35, 00:35]:** the same, plus `agency-staffing` at 00:05 on Monday nights.

**Nightly walls** (prod `data_sync_log`, per nominal day):
- Weekdays: 11–18 min.
- **Both Sunday and Monday nominal days are long**: 90 / 106 min (09-13 / 09-14) and 87 / 98 min (09-20 / 09-21). The prompt expected only Sunday.

**Why T = 21:00:** it is clean on every day. Even a Monday-nominal 106-min night ends ~22:46, 79 min before `agency-staffing`. The nominal-day rule needs no change, because the dispatch carries `slot_offset_hours=3`. `prod_op_gate(5400)` now reads `next_start = 21:00` (verified on prod at 02:50:08).

## 3. D3 — the preflight's proof, and the weekly-gate row

**Clone proof** (`data:nightly:preflight --now …`, one seeded row, deleted after):

| case | answer |
|---|---|
| empty day, `schedule` | `already_ran=false` |
| seeded dispatched row (run 111), `schedule` fallback run 222 | `already_ran=true`, `::notice::` line; `$GITHUB_OUTPUT` = `already_ran=true`, `nominal_day=2026-09-25` |
| same run id 111 (a GitHub re-run attempt) | `false` — "1 row(s) ignored: this run's own" |
| `dispatched_by=manual` | `false` — "not an automated firing path" |
| next nominal day (Fri 21:00:05) | `false`, `nominal_day=2026-09-26` |
| admin-client construction failure | `false` — FAIL OPEN |

**Weekly-gate matrix row** (`weekly-gate.test.ts`):
- Sat 21:00:05 with `readSlotOffsetHours("3")` (the input) names Sunday, with `runWeekly=true`.
- The same instant at offset 0, which is what every dispatch got before this change, skipped it.
- Friday's dispatch names Saturday and skips.

**YAML** (parsed with js-yaml and read back):
- fec-phase exposes `outputs.already_ran`.
- "Run FEC phase" and fec-phase's mark-killed skip on `true`.
- heavy, light and tail keep `!cancelled()` and add `&& needs.fec-phase.outputs.already_ran != 'true'`.
- light and tail list fec-phase in `needs:` so they can read the output.
- receipts keeps `always()`.
- The `NIGHTLY_SLOT_OFFSET_HOURS` expression is updated at all 5 of 5 sites.
- `check:workflow-parity`: 1 group, 17 jobs, 0 undeclared.

## 4. D2 — the route and the hand dispatch

**Route** `/api/cron/gha-dispatch/<stem>` (`nightly | sync-canary-check | platform-snapshot`):
- `force-dynamic`, `maxDuration 30`, `CRON_DISABLED`, and an inline `CRON_SECRET` check.
- The stem is a key into a literal allowlist; the file is always `<stem>.yml` from the map.
- `fetch` is `no-store` with a 10 s timeout.
- nightly sends `{slot_offset_hours: "3", dispatched_by: "vercel-cron"}`; the other two send `{}`.
- Every call is stamped via `record_gha_dispatch()`.

**Local smoke** (`next dev :3150` against local Docker):

| request | result |
|---|---|
| no auth | 401 |
| `audit` | 400, allowlist refusal |
| `..%2Fnightly` | 400, allowlist refusal |
| no token | `refused`, stamped |
| deliberately invalid token | real GitHub `401 "Bad credentials"` → `refused`, http 401, stamped |

**Prod hand call:**
- Call at 02:55:05.951Z: HTTP 200 with `{"status":"dispatched","http_status":204,"at":"2026-09-24T02:55:07.669241+00:00"}`.
- `pipeline_state.gha_dispatch_sync-canary-check` = `dispatched / 204 / last_dispatched_at 02:55:07.669`.
- Run **35949260302**: created 02:55:07Z as `workflow_dispatch`, `triggering_actor=civitics-platform`, concluded success at ~02:55:57.

**First automatic dispatch:** `platform-snapshot` at 03:00:10. Stamp `dispatched / 204`; run 35949595052 (`workflow_dispatch`) succeeded.

**Token:** fine-grained, owned by `civitics-platform` (the repo's owner is a USER account, so no other account's PAT can target it), this repo only, **Actions: Read and write** — GitHub's fine-grained permissions table lists `POST /repos/{owner}/{repo}/actions/workflows/{workflow_id}/dispatches | write`. The expiry date was not given; 1 year was requested. Because `triggering_actor` is `civitics-platform`, GitHub's "runs you triggered" notifications for dispatched runs go to the same inbox the scheduled pager emails go to.

## 5. D4 — §1 rendered from a local run

`data:receipts:daily --local --date 2026-09-24 --dry-run`. Seeds: two local `nightly_cron` rows keyed to the real 09-23 run, plus a local `gha_dispatch_nightly` stamp; deleted after. `gh` read the real GitHub history across all events.

```
### Runs for this nominal day
| run | event | dispatched_by | createdAt (UTC) | offset | conclusion | role |
|---|---|---|---|---|---|---|
| 35933221923 | schedule | schedule | 2026-09-23T23:21:55Z | 2.37 h | success | did the work |

Dispatcher last fired 2026-09-24T02:18:07.841727+00:00 (0.0 h before this file) — status **dispatched**, http 200, run 35933221923
```

`did the work` comes from that run's fec-phase "Run FEC phase" step concluding `success`. The two-run case, dispatched plus `already_ran (stood down)`, is pinned by `receipts-dispatch.test.ts`.

**Canary:** `gha_dispatch_refused` and `gha_dispatch_stale`, both report-only and registered. Its first live read, in the hand-dispatched canary run, reported `gha_dispatch_stale` for "nightly never stamped; platform-snapshot never stamped". That was correct at 02:55 and should read clean after the 21:00 nightly dispatch.

## 6. Status, commits, CI

- `pnpm fixes:status`:
  - FIX-1218 is **OPEN** (no done.log row — receipt pending).
  - FIX-1163, 1176 and 1215 are closed.
  - **58 open** of 1,205 ids (was 57).
  - `fixes:sync:dry`: 0 new rows.
- Commits: the eight in the front matter, all on `origin/main`.
- **CI:**
  - `tests.yml` run 35948861696 on cde8e947 went **red**: one test I wrote, "a hung GitHub → error at the timeout", failed on Node 20/Linux (`AbortSignal.timeout`'s timer is unref'd, so a handle-less fake let the loop exit), and the rest of that file cascaded. The route is unaffected, since a real fetch holds a socket.
  - Fixed by a478880e: run 35949078519 **green**, all seven checks.
  - 414b0c43 is docs-only, so `tests.yml` skips it.
- **Unit suites after the rebase onto cc-149:** data 970 (963 pass, 7 skipped), app 321, db 226. `check:reads`, `check:render-timeouts`, `check:proconfig`, `check:workflow-parity` and `pnpm build` all pass.
- **Flaky, pre-existing:** `detector-coverage.test.ts`'s FIX-1135 crawl-cadence case failed once locally and passed on the immediate rerun with no change. It reads live local crawl state; noted, not touched.

## 7. What contradicted FIX-1163, the receipts, or this prompt — and the two receipts owed

**Contradictions:**
- **FIX-1163 is CLOSED, not reopened** (bd60f873, 2026-09-13). Its reopen row (09-09) was followed by a close on 09-13. Item 8's `Closes: FIX-1163 / Verified[FIX-1163]: closes-as-superseded` would append a second closure to a closed id. Its 09-24 append says half (a) is now answered by FIX-1218.
- **GitHub's REST docs now document the dispatch endpoint as `200` + `{workflow_run_id, run_url, html_url}`**, not the prompt's 204. The live calls answered **204** with no body. The route treats both as `dispatched` and stamps the run id when one comes back.
- **The route shape is `/api/cron/gha-dispatch/<stem>`**, not `?workflow=<file>`. Vercel documents one cron per dynamic-route segment and says nothing about query strings in `path`.
- **The preflight departs from the prompt's wording in three ways:**
  - It counts **every status except `skipped`**. The tree's vocabulary also writes `partial` (ran, one pipeline failed) and `reaped`; re-running a whole nightly for one flaky upstream doubles the box's load.
  - It guards **only `schedule` and `vercel-cron`**. A manual dispatch defaults to offset 0, which names the current UTC day, and the previous evening's slot has always claimed that day. Guarding it would turn every manual dispatch into a no-op, FIX-743's `force_weekly` included.
  - It **ignores rows from its own `GITHUB_RUN_ID`**, so a GitHub re-run isn't refused as a duplicate of its first attempt.
- **Vercel's docs say cron delivery can occasionally fire twice.** A duplicated nightly dispatch queues in `nightly-sync` and the preflight stands it down. The design did not name this case, but it covers it.
- **The receipts job in a duplicate run is not "writes nothing new" (D3 c).** It rewrites the same nominal day's file with a later reading. That is what D4 needs, because it is the only file that sees both runs. The `nightly.yml` comment says so (rule 48).
- **FIX-1215's test pinned its source anchors to `20260920120000` by name.** After this migration they would have kept passing on a superseded body, so they now derive the latest defining migration.
- **Both Sunday and Monday nominal nights are long** (87–106 min), not only Sunday.
- **Rule 71's gloss lived in neither the playbook nor the template.** It is now in the template's Verification list.
- **The prompt expected to wait for cc-149's migration** (possibly until after ~10:00 UTC). cc-149 landed it at 02:01:42 without waiting for cc-151's runner, so this run pushed at 02:49 instead of waiting hours.

**The two receipts the next session must read before the Pattern B record** (`docs/receipts/2026-09-25.md` §1, "Runs for this nominal day", plus `gha_dispatch_nightly`):
1. **The dispatched nightly for nominal 2026-09-25:**
   - `event=workflow_dispatch`, `dispatched_by=vercel-cron`;
   - createdAt − 21:00 **< 5 min**;
   - all phases as usual;
   - `pipeline_state.gha_dispatch_nightly` = `dispatched` at ~2026-09-24T21:00.
2. **The same day's `schedule` fallback** (expected ~22:35–23:45 UTC):
   - concluded with fec-phase "Run FEC phase" = `skipped` (role `already_ran (stood down)`);
   - no new `nightly_cron` rows for 2026-09-25 from its run id.

Then record `Fixes: FIX-1218` with `Verified: prod`, and **no `Closes: FIX-1163`**.

The first receipt is Thursday's: the deploy landed at ~02:51 UTC, before 21:00. The `nightly.yml` change was pushed at 02:49:22 UTC, outside [21:00, the day's nightly start] (rule 83).
