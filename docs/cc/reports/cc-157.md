---
cc: 157
prompt: cc-prompt-157-canary-prod-session-read-dsn-fix1221-first-firing-close.md
started_at: 2026-09-25T22:55:56Z
finished_at: 2026-09-25T23:10:00Z
head_before: 47cc9e1c
head_after: 3425c251
commits:
  - {sha: 2633980f, subject: "docs(fixes): file FIX-1224 (the canary's prod-session detector has read unknown on every GHA run — no DSN in the workflow env)"}
  - {sha: a6986bc9, subject: "ci(canary): FIX-1224 — sync-canary-check passes SUPABASE_DB_PASSWORD; a failed prod-session read is a report-only canary key"}
  - {sha: 3425c251, subject: "docs(fixes): FIX-1221 / 1224 appends (cc-157: the retention has not fired, 1,596 rows past 42 d at 22:58 UTC; the canary is the one DSN-needing workflow that lacked the password)"}
fixes_closed: []
fixes_filed: [FIX-1224]
fixes_reopened: []
migrations_pushed: []
prod_writes: "none. Every prod contact was a read: db-query --prod (the canary meta-row census, jobid 54 and its job_run_details rows, the 42-day count, the cron_job_budget row, and prod_session_state() + session-hold rows read twice as the held=false check before each push), and gh (run lists and logs, secret NAMES). Two pushes to main (~23:06:00 and ~23:06:55 UTC) plus this report's are Vercel deploys; the change they carry is a GHA workflow env line and canary code, which first runs on prod at the 2026-09-26 05:00 UTC canary."
ci: green
stopped_items:
  - "FIX-1224's receipt: the next dispatched canary run is 2026-09-26 05:00 UTC, outside this session (it started 22:55 UTC 09-25). No Fixes trailer; FIX-1224 is open, and its bullet names the receipt."
  - "FIX-1221's first-firing receipt: jobid 54 first fires 2026-09-26 01:51 UTC, outside this session. No Fixes trailer; FIX-1221 stays open, and its append names the slot and what the receipt reads."
---

# cc-157: the canary's prod-session reads get their DSN; both receipts land after this session

**Summary.**
- **FIX-1224 (new).** The canary's two direct-pg reads have read nothing on every GHA run since they shipped. On prod, **0 of 17** `canary_check` meta rows since 2026-09-11 carry a prod-session read, and **0 of 11** a session-hold read. The cause is that `sync-canary-check.yml` never passed `SUPABASE_DB_PASSWORD`.
  - The workflow now passes it.
  - A failed read now raises the report-only key `prod_session_read_failed` instead of only a log line.
  - Pushed at ~23:06 UTC. It closes on the 2026-09-26 05:00 UTC dispatched run.
- **FIX-1221: not fired.** jobid 54 is active with 0 runs. **1,596** rows are older than 42 d at 22:58:22 UTC. The first firing is **2026-09-26 01:51 UTC**.
- **Filed 1, closed 0. Open: 57 → 58.**
- **Worktree:** the `fix-1221` slot was blocked by a non-git leftover directory from cc-156's teardown. This session ran in `fix-1221-cc157` instead (§5.1).

**Push times (UTC):**

| event | UTC | `held` read just before |
|---|---|---|
| push #1 (`2633980f` + `a6986bc9`) | ~23:06:00 | false @ 23:05:46.121, 0 session holds, no live writers |
| push #2 (`3425c251`, appends) | ~23:06:55 | false @ 23:06:45.436 |
| push #3 (this report) | after 23:10 (a file cannot carry its own push time) | read before the push |

---

## 1. Reads

**Read 1** (22:55–22:57 UTC).
- `git pull`: `cde3614e` → **`47cc9e1c`** (`docs(receipts): 2026-09-26`, generated 21:13 UTC).
- `session:check`: **57 open**. It also flags one stranded branch and open PRs; that is the standing `chore/refresh-disposable-emails` pair. It lists 3 reopened FIXes (029, 957, 1075), all pre-existing.
- `tests.yml` has seven checks: `typecheck` · `lint` · `check:reads` · `check:render-timeouts` · `check:workflow-parity` · `check:proconfig` · unit tests (`@civitics/data`, `@civitics/app-civitics`, `@civitics/db`). The latest run on main (`5dab7e84`) was green.
- `.env.local` → local in both the primary checkout and the worktree.
- `fixes:status` (rule 179): FIX-1221 **open**; FIX-1223 closed (`e174cfe3`); FIX-950 **closed** (`9214414c`, 09-11); FIX-1150 closed (`a557e3d8`).

**Read 2 — the detector and its readers** (`canary-check.ts` at `47cc9e1c`).

`fetchProdSessionStatus()`, `:733-747`:

```ts
async function fetchProdSessionStatus(): Promise<ProdSessionStatus | null> {
  try {
    const state = await withClient(buildDbUrl(), readProdSessionState);
    return classifyProdSession(state);
  } catch (err) {
    // Non-fatal, same contract as every other detector here.
    console.warn(`[canary-check] prod session read failed (non-fatal): ${…}`);
    return null;
  }
}
```

**It is one of two.** `fetchHoldStale()` (`:801-823`, FIX-1177/1172) reads through the same `withClient(buildDbUrl(), …)`, so both fail together.

Where the results go:
- **The meta row.** `buildMetadata` writes `prod_session: prodSession` and `session_holds: holdStale` (`:1185-1189`).
- **The email subject.** `sendAlert` adds `prod session overrunning` or `stale prod-session label` (`:1369-1372`), plus a section at `:1534-1552`.
- **The conditions.** `KEY_OVERRUN` or `KEY_LABEL_STALE` from `prodSession.tier` (`:1893-1899`), and `KEY_HOLD_STALE` from `holdStale.tier` (`:1905-1907`).
- **The tile.** `receipts-daily.ts:1144` renders the canary's `conditions` in the daily receipt, sorted by tier. Nothing in `apps/` reads `report_only` or `conditions`.

`canary-prod-session.ts` has three keys, all `report`: `prod_session_overrun`, `prod_session_label_stale` and `prod_session_hold_stale`.

The precedent cc-154 set with `front_door_corroborator_unavailable` (`47428fd1`):
- a pure classifier in its own module;
- a `KEY_*` constant;
- a report-only `push` after the classification;
- registration in `canary-keys.ts` `CONDITION_KEYS` and in the census test's `CONSTANTS`.

This change mirrors all four.

`buildDbUrl()` (`packages/data/src/lib/heavy-rebuild.ts:37-52`) accepts, in order:
1. `SUPABASE_DB_URL`;
2. the local Docker DSN, when `NEXT_PUBLIC_SUPABASE_URL` is 127.0.0.1 or localhost;
3. a composed session-pooler URL from `NEXT_PUBLIC_SUPABASE_URL` + `SUPABASE_DB_PASSWORD`.

Anything else throws `buildDbUrl: need …`.

**Read 3 — workflow env census** (rule 122; derived with grep over `.github/workflows/*.yml` for `pnpm --filter @civitics/data`, then each script's imports checked for `buildDbUrl`/`pg`).

The canary's env block before the change (`sync-canary-check.yml:25-31`):

```yaml
    env:
      NEXT_PUBLIC_SUPABASE_URL: https://xsazcoxinpgttgquwvuf.supabase.co
      SUPABASE_SECRET_KEY:      ${{ secrets.SUPABASE_SECRET_KEY }}
      ADMIN_EMAIL:              ${{ secrets.ADMIN_EMAIL }}
      RESEND_API_KEY:           ${{ secrets.RESEND_API_KEY }}
      # FIX-289: unify From: address across canary + kill-switch alerts.
      RESEND_FROM:              ${{ secrets.RESEND_FROM }}
```

| workflow : job | `@civitics/data` script(s) | needs a DSN? | passes `SUPABASE_DB_PASSWORD`? |
|---|---|---|---|
| `nightly.yml` : fec-phase / enrichment-heavy / -light / -tail | `data:nightly:*:ci`, `data:mark-killed:ci` | yes | **yes** (`:184`, `:351`, `:432`, `:515`) |
| `nightly.yml` : receipts | `data:receipts:daily:ci` | yes | **yes** (`:631`) |
| `audit.yml` | `data:audit:ci` | yes | **yes** (`:30`) |
| `fec-backfill.yml` | `data:fec-bulk:ci`, `mark-killed` | yes | **yes** (`:115`) |
| `irs990.yml` | `data:irs990:ci`, `mark-killed` | yes | **yes** (`:66`) |
| `rebuild-entity-connections.yml` | `data:rebuild-connections:ci`, `data:refresh-donor-mv:ci`, `mark-killed` | yes | **yes** (`:63`) |
| `usaspending-bulk.yml` | `data:usaspending-bulk:ci`, `mark-killed` | yes | **yes** (`:51`) |
| **`sync-canary-check.yml`** | `data:canary-check:ci` | **yes** (two reads) | **no → yes (this run)** |
| `crs-summaries.yml` | `data:backfill-crs-summaries:ci` | no. Imports are `createAdminClient` + `fetchCongressApi`, PostgREST only, as its header says | no (not needed) |
| `refresh-disposable-emails.yml` | `data:refresh-disposable-emails:ci` | no. Imports are fs/url/path only | no (not needed) |
| `platform-snapshot.yml` : request-path-probe | **none** (curl only) | no | no (not needed) |
| `tests.yml`, `hit-test.yml`, `fixes-integrity.yml` | tests, app build | no | — |

`gh secret list` (names only) includes `SUPABASE_DB_PASSWORD` and `SUPABASE_SECRET_KEY`. There is no `SUPABASE_DB_URL` secret.

**Read 4 — the "before"** (`gh run list --workflow sync-canary-check.yml --limit 4`, no `--event` filter):

| run | event | created |
|---|---|---|
| 36120891482 | schedule (fallback) | 2026-09-25 09:52:46 |
| 36096724716 | workflow_dispatch | 2026-09-25 05:00:13 |
| 35982177366 | schedule (fallback) | 2026-09-24 09:35:38 |

The lines, identical in shape across all three runs (quoted from 36096724716):

```
2026-09-25T05:00:43.4986795Z [canary-check] prod session read failed (non-fatal): buildDbUrl: need SUPABASE_DB_URL, a local NEXT_PUBLIC_SUPABASE_URL, or (NEXT_PUBLIC_SUPABASE_URL + SUPABASE_DB_PASSWORD) to compose a DSN
2026-09-25T05:00:43.4987992Z [canary-check] prod session: unknown (read failed)
2026-09-25T05:00:43.7109182Z [canary-check] session-hold read failed (non-fatal): buildDbUrl: need SUPABASE_DB_URL, a local NEXT_PUBLIC_SUPABASE_URL, or (NEXT_PUBLIC_SUPABASE_URL + SUPABASE_DB_PASSWORD) to compose a DSN
2026-09-25T05:00:43.7110770Z [canary-check] session holds: unknown (read failed)
```

The same four lines appear in 36120891482 at 09:53:16.85–09:53:17.06 and in 35982177366 at 09:36:15.85–09:36:15.98.

**Since when, on prod** (`data_sync_log`, `pipeline='canary_check'`, `started_at >= 2026-09-11`, read 22:58:16 UTC):

| runs | carry `prod_session` | read it | carry `session_holds` | read it | first | last |
|---|---|---|---|---|---|---|
| 18 | 17 | **0** | 11 | **0** | 09-11 09:12:49 | 09-25 09:53:11 |

The detector shipped in `de8a9525` (09-11) and the hold read in `cd81917c` (09-17), so both have been blind for their whole lives.

**Read 5 — FIX-1221's job on prod** (read 22:58:22.78 UTC):
- `cron.job` jobid **54** `cron-job-run-details-retention`, `51 1 * * *`, active, `postgres`, `DELETE FROM cron.job_run_details WHERE start_time < now() - make_interval(days => 42)`.
- `cron.job_run_details WHERE jobid = 54`: **0 rows**. It has not fired.
- The table: **67,799 rows**, **1,596** older than 42 d, oldest `2026-06-29 08:00:00`. cc-156 read 986 older than 42 d at 03:15 UTC the same day.
- `cron_job_budget`: 300 s, updated 03:47:04. Its note still says "First purge 986 rows", which was true at 03:15.

## 2. Item 2 — the env line and the key

**The env diff** (`sync-canary-check.yml`, +7):

```yaml
      SUPABASE_SECRET_KEY:      ${{ secrets.SUPABASE_SECRET_KEY }}
+     # FIX-1224 — the prod-session and session-hold detectors (FIX-950,
+     # FIX-1177/1172) read over direct pg, and buildDbUrl() composes that DSN
+     # from NEXT_PUBLIC_SUPABASE_URL + this, exactly as nightly.yml's phase
+     # jobs do. Without it both reads threw on every run from 2026-09-11 and
+     # their three keys could never fire; a failed read now raises the
+     # report-only prod_session_read_failed instead of a log line.
+     SUPABASE_DB_PASSWORD:     ${{ secrets.SUPABASE_DB_PASSWORD }}
      ADMIN_EMAIL:              ${{ secrets.ADMIN_EMAIL }}
```

**The key: `prod_session_read_failed`.** It is report-only and lives in `canary-prod-session.ts` as `KEY_READ_FAILED` and `classifySessionReads()`.

- **Input.** Both reads, as `{name, ok, error}`. `ok` is false when the fetcher returned no status.
- **Tier.** `report` if either read failed; `null` if both answered. It never escalates, the same posture as `front_door_corroborator_unavailable`: an instrument is blind, nothing on the box is broken.
- **One key for both reads.** They share a DSN and a connection path, so they fail together and have the same remedy.
- **Severity.** The number of reads that failed, so the count is monotone for the FIX-1036 transition classifier.
- **Detail.** It names each failed read and its error, and the three keys it blinds. Example from the cc-156 shape:
  > `prod-session detector blind — 2 of 2 direct-pg read(s) failed: prod session: buildDbUrl: need SUPABASE_DB_URL, …; session holds: buildDbUrl: need …. prod_session_overrun, prod_session_label_stale and prod_session_hold_stale cannot fire, and this run's prod_session / session_holds trail is null. A workflow env with no SUPABASE_DB_PASSWORD reads exactly like this — FIX-1224`
- **Wiring in `canary-check.ts`.** Both fetchers return `DirectRead<T> = {value, error}`. `main()` classifies the pair right after the session-holds log line and pushes the key after the hold-stale push. The key is registered in `CONDITION_KEYS` and in the census test's `CONSTANTS`.

**Tests.**
- `canary-prod-session.test.ts` +5:
  - both reads ok → no finding;
  - the verbatim no-DSN error on both → `report`, severity 2, never escalates, the detail names both reads and all three blinded keys;
  - one read failing → severity 1, below both failing;
  - a quiet null on both → `report` with `QUIET_FAILURE`;
  - the key is stable.
- **Mutation check.** Removing the key from `CONDITION_KEYS` fails `every key the source emits is in the registry`.

**Local smoke** (worktree, local Docker, `tsx --env-file=../../.env.local`):

| run | log lines | new key |
|---|---|---|
| normal | `prod session: clear — no supervised prod session` · `session holds: no session-set rollup holds` | absent; report-only (36) |
| `SUPABASE_DB_URL=postgresql://…@127.0.0.1:1/postgres` | `[prod-session] preflight connection failed (connect ECONNREFUSED 127.0.0.1:1)` ×2 · `prod session: unknown (read failed)` · `session holds: unknown (read failed)` | **raised**; report-only (37) |

Both runs exited 1. That is the local clone's own escalations (nightly_cron missing 7 days, stale rollups), not this change. Each run wrote one local `canary_check` meta row.

**The receipt: not read.** It is the 2026-09-26 05:00 UTC run, dispatched by `/api/cron/gha-dispatch/sync-canary-check`.
- **What it should show:** no `prod session read failed` or `session-hold read failed` line, and `prod session:` and `session holds:` reading a state. The meta row's `prod_session` and `session_holds` should be objects, and there should be no `prod_session_read_failed` condition.
- **Predicted state:** prod read 23:05:46 gives `held=false`, `label_stale=false`, no live writers, 0 session-set holds. So unless a session is claimed at 05:00, the lines should be `prod session: clear — no supervised prod session` and `session holds: no session-set rollup holds`.
- **Closing it:** a docs-only allow-empty commit with `Fixes: FIX-1224` and `Verified: prod`, quoting those lines.

## 3. Item 3 — FIX-1221

Not fired. jobid 54's first firing is **2026-09-26 01:51 UTC**, about 2 h 45 m after this session's reads.

The receipt for the next docs-only session:
- the count of rows older than 42 d just before the firing, and just after it;
- the job's own `cron.job_run_details` row: status `succeeded`, wall `end_time − start_time`, and `return_message`. pg_cron records the command tag there, so it should read `DELETE n` and give the purged count directly.

The bullet carries that as a one-line append (`3425c251`). No trailer this run.

## 4. Status, commits, CI

**`fixes:status`** (23:08):

| FIX | status |
|---|---|
| FIX-1221 | **open**, no done.log row |
| FIX-1224 | **open**, filed this run |
| FIX-1223 | closed (`e174cfe3`, local+prod) |
| FIX-950 | closed (`9214414c`, prod-only). No append (rule 70); its blindness is recorded on FIX-1224 |
| FIX-1150 | closed (`a557e3d8`, local+prod) |

**58 open** of 1,211 known ids (57 + 1 filed; the prompt's "58 / 57 / 56" branch is 58). `fixes:check` was clean after every commit. `fixes:sync:dry` shows 0 new rows, because no trailer landed, so there is no sync commit.

**Commits** (all on `origin/main`):

| sha | what |
|---|---|
| `2633980f` | file FIX-1224 |
| `a6986bc9` | env line + key + tests (no trailer) |
| `3425c251` | appends: FIX-1221, FIX-1224 |

**CI:** `tests.yml` run **36199643878** on `a6986bc9`: **success**. `3425c251` is docs-only, so `tests.yml` skips it.

**Local verification** (on `a6986bc9`):

| check | result |
|---|---|
| `turbo run typecheck lint --force` | 19/19 tasks. Warnings only, pre-existing in `@civitics/ai` (12) and `@civitics/graph` (8) |
| unit suites | data **1009** (1002 pass, 7 skipped, 0 fail; was 1004); db **251**; app **321** |
| `check:reads` / `check:render-timeouts` | 0 silent-zero / 0 unwrapped |
| `check:workflow-parity` | 1 group, 17 jobs, 13 workflows, 1 waived, **0 undeclared** |
| `check:proconfig` | 24 migrations, 0 undeclared |
| `scripts/test-*.mjs` | `fixes:test`, `fix:add:test`, `cc:verify:test`, `session:worktree:test`, `drain:test`, `check:proconfig:test` (18/18), all pass |

## 5. What contradicted cc-156 or this prompt

1. **The `fix-1221` worktree slot was not free.** `../civitics-worktrees/fix-1221` exists, but it is not a git worktree. It holds a partial tree with `node_modules` dated 09-24 22:05, left by cc-156's teardown (the EPERM-locked `node_modules` case). `session:worktree 1221` refused: `worktree dir already exists`.
   - Deleting it needs approval, so this run used slot **`1221-cc157`** (`feature/fix-1221-cc157`).
   - **The leftover directory needs a manual delete.** The branch will be torn down with `pnpm session:worktree:done 1221-cc157`.
   - One backup file made during the mutation check sits at `%TEMP%\canary-keys.bak`, outside the repo.
2. **Does any other `@civitics/data` script on GHA fail the same way? No.**
   - Every other job whose script needs a DSN already passes `SUPABASE_DB_PASSWORD` (read 3).
   - `platform-snapshot.yml` runs no `@civitics/data` script at all. Its one job is the curl request-path probe, and the snapshot trigger moved to a Vercel cron under FIX-1127.
   - `crs-summaries` and `refresh-disposable-emails` need no DSN.
   - The canary was the only gap.
3. **Did `check:workflow-parity` want anything beyond the env line? No.**
   - It compares only `NODE_OPTIONS` and `timeout-minutes`, across jobs that run the same package script or a declared envelope group.
   - `data:canary-check:ci` runs in one job, and env keys other than those two are not read. Its output is identical before and after (0 undeclared).
   - So nothing in CI would catch a DSN-less workflow env; the new key is the only guard, and it fires at runtime.
4. **"A `null` from `fetchProdSessionStatus`" is wider than a missing DSN.**
   - Only `buildDbUrl()` throws. `withClient()` (`prod-session.ts:516-538`) returns null on a failed **connection**, including a wrong password or a pooler refusal, after its own `[prod-session] preflight connection failed` warning. `readProdSessionState()` returns null on a failed query.
   - In those cases the canary logs no `read failed (non-fatal)` line at all, only `unknown (read failed)`. That was measured locally with an unreachable DSN.
   - The key is therefore raised on `value === null`, not on a thrown error, and its detail says `QUIET_FAILURE` when nothing was thrown. This is the common blind shape once the DSN exists.
5. **A latent mis-read in `fetchHoldStale` (not fixed; out of scope as "the canary's other detectors").**
   - It classifies holds against `state?.held === true`.
   - If the connection succeeds but `prod_session_state()` fails, `state` is null. A live session's holds would then be reported as `prod_session_hold_stale` (nobody on the box) instead of "held by the live supervised session".
   - This needs the query to fail while the connection holds; not seen on prod. Not filed.
6. **The canary runs twice a day, and both runs are full runs.**
   - The dispatched 05:00 run and the `schedule:` fallback (09:35 on 09-24, 09:52 on 09-25) each ran every detector and wrote a meta row.
   - Unlike the nightly's fallback, the canary's does not stand down on `already_ran`.
   - This is harmless for the receipt: rule 71 names the 05:00 dispatched run, and the fallback at ~09:xx will corroborate it. Worth knowing when counting "runs since".
7. **Two small corrections to cc-156's record.**
   - The `cron_job_budget` note on jobid 54 says "First purge 986 rows". That was the count at 03:15 on 09-25. By 22:58 it was 1,596, and the first purge will be larger again.
   - cc-156 §5.10 named the "prod-session and session-hold detectors" as blind. Confirmed, with the ship dates: 09-11 for the session read and 09-17 for the hold read.
8. **Premise check.** The prompt was written at 05:50 UTC Fri. This run started at 22:55 UTC Fri, before both scheduled receipts, so neither was readable. That matches the prompt's "whichever has not happened is left as a named receipt".
