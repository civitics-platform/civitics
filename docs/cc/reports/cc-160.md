---
cc: 160
prompt: cc-prompt-160-fix1226-rederive-received-totals-fix1224-pattern-b.md
started_at: 2026-09-26T18:34:53Z
finished_at: 2026-09-26T19:51:31Z
head_before: fdf6df8c
head_after: 2bc0259c
commits:
  - {sha: 153eb6f5, subject: "feat(data): FIX-1226 — the received-totals re-derive script and its manifest"}
  - {sha: 51631d5d, subject: "docs(audits): FIX-1224 Pattern B — the 05:00 canary read the prod session"}
  - {sha: 624a1275, subject: "chore(fixes): sync status after FIX-1224"}
  - {sha: 30480222, subject: "chore(fixes): FIX-1226 prod set — re-derived total_received_cents for 5,713 committees from the manifest"}
  - {sha: 2bc0259c, subject: "chore(fixes): sync status after FIX-1226"}
fixes_closed: [FIX-1226, FIX-1224]
fixes_filed: []
fixes_reopened: []
migrations_pushed: []
prod_writes: "One data set, in two runs of the same committed manifest (docs/audits/2026-09-26-fix1226-rederive-manifest.json), each through session:wait-for-gate:prod --census stop --then from the primary checkout under a claimed FIX-950 session (17 holds written and cleared each time). Each chunk was one autocommitted SELECT public.financial_entity_received_totals_rebuild(ARRAY[...]::uuid[]) after SET statement_timeout = 10min, via scripts/fix1226-rederive-received-totals.mjs. Run 1, 19:22:32-19:23:55 UTC: chunks 1-20, 4,000 rows of financial_entities updated, then the script's budget guard stopped it cleanly before chunk 21 (exit 5). Run 2, 19:29:56-19:41:10 UTC: chunks 1-83, chunks 1-20 re-ran as no-ops (0 rows), 1,713 rows updated. Total 5,713 rows: total_received_cents set to its FR-derived sum, and updated_at bumped by the financial_entities_updated_at trigger. The claims made fe-crawl and ec-crawl log skipped at 19:30. Pushes to main (each a Vercel deploy; no app change): 18:55:56, 18:56:56, 18:57:22, 19:47:41, 19:48:03, and this report. No migration, no DDL, no VACUUM, no EXPLAIN ANALYZE, no ad-hoc UPDATE on prod. Every other prod contact was a read: db-query --prod (counts, plain EXPLAIN, pg_stat_user_tables, cron reads, prod_op_gate), the census (Logs API, 3 standalone readings plus the gate's 2), gh, and two HTTPS GETs of the RNC page on the Vercel origin."
ci: green
stopped_items: []
---

# cc-160: 5,713 zeroed committees re-derived on prod — but the function reads the heap, so the set took 12.6 min, not 1–2

**Summary.**
- **FIX-1226 closed** (`30480222`, `Verified: prod`). All **5,713** committees the unscoped PAC upsert zeroed now carry their FR-derived received total. D4 on prod at 19:41:18 UTC finds **0** committees DISTINCT from B's `sum_total`. The RNC, DNC and DSCC equal cc-159 D7 to the cent, and all **41** whales are non-zero.
- **FIX-1224 closed** (`51631d5d`, `Verified: prod`). The 05:00 UTC dispatched canary logged `prod session: clear` and `session holds: no session-set rollup holds`. Its meta row carries both as objects, with no `prod_session_read_failed`.
- **The premise the wall rested on did not hold.** `financial_entity_received_totals_rebuild` filters `to_type`, which `donor_rollup_idx` does not carry. Its aggregate is therefore an `Index Scan using financial_relationships_to` plus a heap Filter, not the index-only read B's bootstrap did.
  - **Wall:** first-pass work was **695 s**, and **758 s** of psql wall across both runs, against the prompt's 1–2 min.
  - **The two changes I made:**
    - a row cap on ordinary chunks;
    - a budget guard. It stopped run 1 cleanly after chunk 20, and that stop was a false positive: its single-rate projection was 3.3× high.
  - **The resume:** the manifest then re-ran gated for 18 min.
- **Census:** 0 × 57014 across both runs; front-door 5xx 0 of 147 and 0 of 294 in the buckets the set ran in. **Renders lost: 0.**
- **Open:** 59 → **57**. Nothing filed. The 232 committees with `total_donated_cents = 0` and outbound rows are reported (§1 read 3(e)) for Craig's filing decision, as the prompt asked.

## 1. Phase 0 reads

### Read 1 — state (18:34–18:36 UTC)
- **Session check:** `session:check` at `fdf6df8c`: **59 open**, as expected; 0 ahead / 0 behind; one unmerged branch (`chore/refresh-disposable-emails`, PR 4, the monthly blocklist refresh, not this run's).
- **CI:** `tests.yml` on main green, 36214354212.
- **Env:** `.env.local` → `http://127.0.0.1:54321` (local).
- **FIX status (`fixes:status`):**
  - FIX-1226 and FIX-1224 OPEN (no row);
  - FIX-1217 and FIX-1225 closed 09-25 `1c7307ef` prod-only;
  - FIX-702 and FIX-726 closed 07-04 `14aaa055` local-only.

### Read 2 — FIX-1224's receipt (prod, 18:36–18:40)
- **Runs:** `gh run list --workflow sync-canary-check.yml --limit 4` (no `--event`):

  | run | event | time |
  |---|---|---|
  | **36219489297** | `workflow_dispatch` | **2026-09-26T05:00:13Z** |
  | 36233243796 | `schedule` fallback | 09:35:31Z |
  | 36096724716 / 36120891482 | dispatch / schedule | 09-25 |

- **Dispatcher stamp:** `pipeline_state.gha_dispatch_sync-canary-check` = `dispatched`, at `2026-09-26T05:00:13.62`, HTTP 204.
- **Run 36219489297's log** (grep `prod session|session holds|read failed`):
  ```
  2026-09-26T05:00:41.9481544Z [canary-check] prod session: clear — no supervised prod session
  2026-09-26T05:00:42.9772139Z [canary-check] session holds: no session-set rollup holds
  ```
  No `read failed` line. The 09:35 fallback logs the same pair (09:35:58 / 09:35:59).
- **Meta rows** (`data_sync_log`, `pipeline='canary_check'`, since 04:30): two rows, both correct.
  - `c2a7b6cb` (05:00:38.38–05:00:44.08) and `1fe8d7d6` (09:35:53–09:36:00).
  - `jsonb_typeof(prod_session)` = **object**: `{"held": false, "state": "clear", "detail": "no supervised prod session", "liveWriters": [], "severity": 0, …}`.
  - `jsonb_typeof(session_holds)` = **object**: `{"detail": "no session-set rollup holds", "pipelines": [], "severity": 0, …}`.
  - `conditions` is an array with **0** `prod_session*` keys.

### Read 3 — the population now (prod)
- **(a) cc-159 read 8's counts, re-read** (18:37:27): **12,317** with a `fec_committee_id` / **8,377** at 0 / **5,713** at 0 with ≥ 1 inbound FE donation row.
  - That last count came both from an EXISTS over FR and from a B row: 5,713 either way.
  - `at_null` 0; `nonzero_no_b_row` 0; B rows with `sum_total = 0`: 0.
  - **Unchanged from cc-159**, and expected: FR `max(updated_at)` is still **2026-09-21 00:18:25.83**, and FE's is 09-22 10:20. Neither the fec phase nor fe-crawl has moved anything since.
- **(b) The manifest query, by class** (18:37:47). `fec_committee_id IS NOT NULL AND total_received_cents IS DISTINCT FROM b.sum_total`, PK join to `financial_entity_inbound_totals`:

  | class | n | Σ tx_total | Σ sum_total (¢) | over 10k | max tx |
  |---|---|---|---|---|---|
  | zeroed | **5,713** | 3,154,766 | 1,470,636,244,400 | 34 | 337,058 |
  | drifted (non-zero) | **0** | — | — | — | — |

  **No non-zero committee had drifted from B.** Rows at 0 with NO totals row: **2,664** (8,377 − 5,713). They have no inbound rows, so they are not in the set.
- **(c) The instruments' one definitional difference** (18:38:08). B does not filter `to_type`; the function does. Of FR rows whose `to_id` is a manifest id, the number with `to_type <> 'financial_entity'` is **0**, and with `to_type IS NULL` also **0**. `pg_stats` lists exactly two `to_type` values. So the two sums are equal by construction for this set.
- **(d)/(e) Out of scope, REPORT only — `total_donated_cents`** (18:38:26 / 18:38:49):
  - **5,771** committees read `total_donated_cents = 0`. **232** of those have outbound FE donation rows, **200** of them touched in the 09-20 22:00 – 09-21 01:00 FEC window.
  - `financial_entity_donation_totals_rebuild`'s own aggregate puts all 232 at a non-zero value: **1,081** outbound rows, **292,205,300 ¢** ($2.92M), max 85 rows per committee.
  - Query: `financial_entities fe WHERE fec_committee_id IS NOT NULL AND total_donated_cents = 0`, aggregated over `financial_relationships WHERE from_type='financial_entity' AND relationship_type='donation' AND from_id = fe.id`. It rides the `donation_size_rollup` index-only.
  - Same writer, same defect, small; it is a filing decision for Craig. **Nothing filed.**

### Read 4 — the function and its neighbours (prod)
- **The body on prod** (`pg_get_functiondef`) is identical to `20260704000000:213-232`:
  - **Predicate:** `to_type = 'financial_entity' AND from_type = 'financial_entity' AND relationship_type = 'donation' AND to_id = ANY(p_ids)`, grouped by `to_id`.
  - **Update:** `UPDATE … WHERE fe.id = agg.to_id AND fe.total_received_cents IS DISTINCT FROM agg.total`.
  - **Return:** `GET DIAGNOSTICS ROW_COUNT`.
  - **Attributes:** owner `postgres`; proconfig `{search_path=public}`; not SECURITY DEFINER; volatile. No COMMIT, so a plain SELECT runs it.
- **Grants:** `has_function_privilege('postgres', …, 'EXECUTE')` **t**; anon f, authenticated f, service_role t. proacl `{postgres=X/postgres, service_role=X/postgres}`.
- **Triggers on `financial_entities`** (prod `pg_trigger`, non-internal): two.
  - `financial_entities_updated_at`, BEFORE UPDATE, `set_updated_at()`. It fired: all 5,713 manifest rows now carry `updated_at` between 19:22:32.32 and 19:40:56.22.
  - `small_dollar_bracket_rollup_cleanup_fe_del`, AFTER DELETE. Not fired.
- **Readers of the column or of FE `updated_at`, and what the set does to each:**
  - **Routines** — `pg_proc.prosrc`: 6 reference `total_received_cents`, 11 reference FE together with `updated_at`, every `updated_at` line read:

    | reader | what the set does to it |
    |---|---|
    | `rebuild_entity_search_index` (via `refresh_derived_mvs('daily')`, jobid 9, 06:00) | A full rebuild reading FE `total_received_cents`; FE rows use `created_at` (FIX-699). **06:00 picks up the corrected totals** for search and typeahead. Cost does not depend on this set. |
    | `rebuild_all_primary_sources` (same job) | Reads FE `updated_at` only in the NULL-only FEC fill (`WHERE primary_source IS NULL`). **0 of the 5,713** have a NULL `primary_source` (19:48:54), so untouched. |
    | fe-crawl (jobid 46) → `refresh_fe_totals_slice` | FR-dirty. FR is unchanged, so nothing to do; its `IS DISTINCT FROM` guard would no-op these rows anyway. |
    | `run_fe_inbound_rollup` (B, jobid 55) | Reads FR, not FE. Unaffected. |
    | `financial_entity_received_totals_window` / `rebuild_financial_entity_received_totals` / `refresh_financial_entity_totals_incremental` | Unscheduled (jobid 13 inactive). |
    | `small_dollar_rebuild_officials` | Its only mention is a comment about `officials.total_received_cents`. |

  - **TypeScript:**
    - `enrichment/seed-backlog.ts` records FE `updated_at` as `entity_updated_at`. That column orders within a priority tier (`20260424020000`); it does not trigger re-enqueue. The seed is manual (`data:enrich-seed`, in no workflow).
    - `receipts-daily.ts` reads FE `n_dead_tup` and VM %. It will show FE dead tuples up **+5,713** (745 → 6,458 at 19:41) and, after the next analyze, a VM dip on up to 5,252 of 245,569 pages. `fe-vacuum-analyze` 04:50 owns that (rule 41); no vacuum is owed.
    - The request-path readers (donor page, entity search, typeahead, treemap, small-dollar routes) now read the corrected value, which is the point.

  **No trigger or job is disturbed, so nothing is filed and nothing is appended to FIX-1194.**
- **jobid 14 on 10-01 — it would have done nothing for these rows.** `reconcile_financial_entity_totals` is an **orphan sweep** (prod `prosrc`). Sweep B is `UPDATE … SET total_received_cents = 0 WHERE fe.total_received_cents > 0 AND NOT EXISTS (inbound donation FR)`. It only ever zeroes a positive total with no rows; it never re-derives upward. The 5,713 at 0 fail `> 0`, so they would have stayed at 0 indefinitely. After this set they have rows and positive totals, so Sweep B leaves them alone too.

### Read 5 — drain-and-wait and gate readings (prod)

| gate | 18:52:04 | 19:22:16 |
|---|---|---|
| (b) EC `n_dead_tup` / trigger | 1,071 / 523,168 | 1,071 / 523,168 |
| (b) FE `n_dead_tup` / trigger | 745 / 261,036 | 745 / 261,036 |
| (b) FR `n_dead_tup` / trigger | 442 / 288,696 | 442 / 288,696 |
| `mods_total` EC / FE / FR | 6,442,890 / 246,089 / 349,950 | unchanged |

- **(b) Rate:** every rate is 0 over 30 min 12 s, so headroom ÷ rate is unbounded. Two readings ≥ 30 min apart, because rule 155 applies (see §5.1).
- **(a) Unguarded vacuums:** the last were the 17:0x series, ending by **17:20:02** (`ecs-vacuum-analyze`, 2.0 s; all ≤ 2.0 s). That is the 10-min class, 122 min before the launch. Tonight's `fr-vacuum-analyze` is at 03:00, after the landing.
- **(c) Crawl arms:** `ec-crawl` and `fe-crawl` 18:00–19:15, all succeeded, max 0.2 s. None running; no `running` `data_sync_log` row.
- **(d) Watchdogs:** 30/30 each per 60 min (`cron-job-budget-watchdog` max 0.076 s, `derived-mvs-unit-watchdog` 0.013 s); 0 startup timeouts.
- **(e) Interlock:** `held=false`, `live_writers` [].
- **(f) Census baseline:** `data:census:cancellations:prod` at **18:52:24**, window 17:52:24–18:52:24.
  - **0** 57014s, rate 0/min against the 0.033/min baseline, ratio 0.00, P99 floor 6: **PASS**.
  - Front door: buckets 17:45 / 18:00 / 18:15 / 18:30 = 169 / 270 / 156 / 309 requests, 0 5xx; last closed bucket 0 of 309: **PASS**.
- **(g) Gate:** `prod_op_gate(480)` ok at 18:52:14, `blocked_by` [], span to 19:08:14.
  - Nightly: last phase `enrichment-tail` complete 09-25 21:12:39; next start **21:00 UTC** dispatch; required 31 min, 128 min away.
  - Blackout 05:45–09:00. Window: **before the 21:00 dispatch**.

### Read 6 — the vehicle
- **Both vehicles autocommit per statement, provably:**
  - `db-query.mjs --prod --call` pushes `--single-transaction` only `if (!call)` (line ~190). psql then sends each `;`-terminated stdin statement as its own simple query, and the prelude (`scripts/lib/db-query-prelude.mjs`) sends `SET statement_timeout` and `SET civitics.prod_session_claimant` as separate statements.
  - The psql-autocommit model of `fix1142-build-treemap-global-index.mjs` (no BEGIN, no `--single-transaction`) does the same.
- **I took the fix1142 model** and fed stdin one chunk at a time. `db-query --call` prints no per-chunk result, cannot write the manifest, and gives no way to stop between chunks.
  - Measured locally first: fed incrementally, psql returns each statement's result before the next arrives (3 × `pg_sleep(1)`, results at +1.1 / +2.1 / +3.1 s).
- **The claimant:** `wait-for-gate --then` sets `CIVITICS_PROD_SESSION_CLAIMANT` (wait-for-gate.ts header). The script SETs it on its session (rule 169: the function is not a guarded procedure, so it only labels the session). Both runs printed `claimant cc-160 FIX-1226 re-derive received totals[ (resume)]`.
- **The plan the vehicle drives** (plain EXPLAIN on prod, RNC alone and the largest ordinary chunk): `GroupAggregate ← Index Scan using financial_relationships_to`. `Index Cond: (to_type = 'financial_entity' AND to_id = ANY(…))`, with a `Filter` on `from_type` and `relationship_type`. **This is a heap read** (§5.1).

## 2. Item 1 — the script and its manifest (`153eb6f5`, pushed 18:55:56, `held=false` 18:55:14)

**The dry run** (prod, read at **18:52:37**) matches read 3(b) row for row:
```
[fix1226] population: 5,713 ids — zeroed 5,713, drifted 0, null 0; excluded (0, no inbound rows): 2,664
[fix1226] inbound rows Σ b_tx_total 3,154,766; Σ b_sum_total 1,470,636,244,400 ¢
[fix1226] chunks: 83 (49 ordinary of ≤ 200 ids / ≤ 50,000 rows, 34 whales of 1, largest last)
[fix1226] expected wall FLOOR (index-only scaling): 62.4 s × 3,154,766 / 4,023,127 B rows (9,656 recipients) = 48.9 s
[fix1226] heap read at the clone's 7.89 rows/page: ~399,749 pages, ~3.3 GB — 65 s at 50 MB/s, 364 s at 9 MB/s
[fix1226] plan of the largest ordinary chunk's aggregate (chunk 26, 119 ids, 49,922 rows):
  GroupAggregate … ->  Index Scan using financial_relationships_to … Filter: ((from_type = 'financial_entity') AND (relationship_type = 'donation'))
```

**Chunking** is D1 plus a row cap: an ordinary chunk also closes at 50,000 rows. The first dry run (18:46) with D1's 200-id cut alone made chunk 28 (200 ids, **394,249** rows) and chunk 29 (79 ids, **442,159** rows). Both were larger than the RNC (337,058), so "largest last" would not have held.

**Rehearsed on local Docker (the clone)** before prod:
- **Dry run:** 4,115 zeroed committees. The clone has the defect too; it predates 09-03.
- **Apply:** 37/37 chunks, 4,115 rows updated, 18.6 s.
- **Re-run:** 0 rows ("already equal") — the function is idempotent.
- **Budget stop:** `--budget-seconds 1` → clean stop between chunks, exit 5.
- **After:** the dry run returns 0 ids, so the function's sums equal B's on the clone for every id.

**Checks:** the seven `tests.yml` checks passed locally before the push:

| check | result |
|---|---|
| `turbo typecheck` + `lint` | 19/19 |
| `check:reads` | 162 scanned, 0 silent-zero |
| `check:render-timeouts` | 161 / 0 |
| `check:workflow-parity` | 0 undeclared |
| `check:proconfig` | 0 undeclared |
| unit suites | data 1011 pass / 0 fail, app 326/326, db 251/251 |

The six `scripts/test-*.mjs` suites passed too. CI on the push: **36264278274 green**.

## 3. The landing (D3)

Both runs were launched from the primary checkout, cwd `packages/data`:

- **Run 1:**
  ```
  pnpm --filter @civitics/data session:wait-for-gate:prod --expected-minutes 8 --max-wait-minutes 900 --census stop --reason "cc-160 FIX-1226 re-derive received totals" --then "node ../../scripts/fix1226-rederive-received-totals.mjs --manifest ../../docs/audits/2026-09-26-fix1226-rederive-manifest.json --env-file ../../.env.local.prod --budget-seconds 960"
  ```
- **Run 2 (the resume):** the same with `--expected-minutes 18 --max-wait-minutes 35 --budget-seconds 2160` and reason `… (resume)`.

**The poll series:** one poll each, no hold.

| run | poll (UTC) | gate | census | span to | claim |
|---|---|---|---|---|---|
| 1 | 19:22:27 | ok, `blocked_by` [] | pass: 0/60 min, ratio 0.00, floor 6; edge 0 of 261 | 19:38Z | 17 holds |
| 2 | 19:29:52 | ok, `blocked_by` [] | pass: 0/60; edge 0 of 261 | 20:05Z | 17 holds |

In run 2's window the claim made `fe-crawl` and `ec-crawl` at 19:30 log `skipped` (`prod session held: cc-160 … (resume)`).

**Run 1:**
- **Wall:** 19:22:32.29 → 19:23:55.54 (server); psql wall **83.6 s**.
- **Work:** chunks 1–20, **4,000 rows updated of 4,000 ids**.
- **The stop:** `STOPPED before chunk 21/83: projected 2298.9 s (3,041,462 rows left at 1,373 rows/s) passes the 960 s budget`.
- **After:** exit 5; claim released; prod at 19:24:31 had 1,713 still distinct, `held=false`, and no lingering `fix1226-rederive` backend.
- **Why it stopped:** the projection is a single cumulative rows/s rate. The first 20 chunks are 200 FE writes each and few FR rows, so their cost is the FE write, not the heap read. A two-term fit of run 1's walls gives **~15 ms per FE row updated** plus **~0.26 ms per FR row read**. For example, chunk 1: 200 rows / 200 FR rows, 3.16 s; chunk 20: 200 / 21,354, 8.69 s. That fit put the whole set at ~14 min, and the guard's single rate put it at 38.
- **The stop was a false positive against its own budget.** The measured first-pass total was **696 s** (83.6 s + 612.7 s of chunks 21–83). That is over the 8 min expected but inside the 960 s budget, so the guard over-projected 3.3×.
- **So the resume was gated for 18 min, on the 14-min fit:** census first (§4, 0 over run 1), then a re-gate.

**Run 2:**
- **Wall:** 19:29:56.35 → 19:41:10.41 (server); psql wall **674.4 s**.
- **Work:** chunks 1–83, **1,713 rows updated**. Chunks 1–20 found all 4,000 rows "already equal" and updated 0.
- **Exit:** 0; claim released.

**Totals:** 4,000 + 1,713 = **5,713 rows updated = the manifest's population**; no chunk updated fewer than its ids on its first pass. Total psql wall **758.0 s** (12.6 min). The first-pass work alone (run 1's 20 chunks + run 2's chunks 21–83) was **695.2 s**; run 2's no-op re-read of chunks 1–20 cost **58.8 s**.

**Against the expectations:**

| expectation | value | actual vs it |
|---|---|---|
| prompt | 1–2 min | ×6–12 |
| dry run's index-only floor | 48.9 s | ×14 |
| clone-projected heap band | 65–364 s | ×1.9 even at the pessimistic 9 MB/s |

**Per-chunk walls (server-side):**

| group | chunks | rows updated | FR rows read | Σ wall | max chunk |
|---|---|---|---|---|---|
| run 1, ordinary | 1–20 | 4,000 | 113,304 | 82.5 s | 8.69 s (ch 20) |
| run 2, re-run no-op | 1–20 | 0 | 113,304 | 58.8 s | 11.27 s (ch 18) |
| run 2, ordinary | 21–49 | 1,679 | 1,345,859 | 444.5 s | 21.84 s (ch 24, 186 ids / 49,773 rows) |
| run 2, **whales** | 50–83 | 34 | 1,695,603 | **168.2 s** | 15.09 s (ch 82, DNC) |

**The 34 whales** (chunk: rows / s):

| chunk | rows / s | chunk | rows / s | chunk | rows / s |
|---|---|---|---|---|---|
| 50 | 10,124 / 2.61 | 62 | 14,067 / 3.59 | 74 | 45,863 / 3.80 |
| 51 | 10,377 / 1.25 | 63 | 14,297 / 2.59 | 75 | 51,573 / 3.81 |
| 52 | 10,607 / 1.33 | 64 | 14,614 / 3.34 | 76 | 57,385 / 9.09 |
| 53 | 10,680 / 3.14 | 65 | 14,690 / 3.93 | 77 | 59,924 / 3.27 |
| 54 | 10,954 / 1.45 | 66 | 15,877 / 4.03 | 78 | 63,307 / 5.95 |
| 55 | 12,068 / 0.44 | 67 | 18,134 / 3.71 | 79 | 93,507 / 10.79 |
| 56 | 12,085 / 0.60 | 68 | 22,978 / 6.79 | 80 | 126,536 / 10.47 |
| 57 | 12,342 / 4.35 | 69 | 23,235 / 4.91 | **81 DSCC** | **199,241 / 9.94** |
| 58 | 13,605 / 1.66 | 70 | 25,161 / 5.81 | **82 DNC** | **259,440 / 15.09** |
| 59 | 13,658 / 4.54 | 71 | 25,751 / 3.45 | **83 RNC** | **337,058 / 14.17** |
| 60 | 13,715 / 3.29 | 72 | 29,216 / 6.62 | | |
| 61 | 13,871 / 4.17 | 73 | 39,663 / 4.18 | | |

Whales ran ~10,000 FR rows/s; ordinary chunks ~3,000. A whale's rows are clustered in its filings' heap pages, while a small recipient's few rows each cost a page. No chunk came near the 10-min `statement_timeout`.

## 4. D4 on prod and the census

**D4** (19:41:18 UTC, `prod_op_gate(60)` ok):

| check | before (18:57:39) | after (19:41:18) |
|---|---|---|
| manifest query: zeroed / drifted DISTINCT from B | 5,713 / 0 | **0 / 0** |
| RNC `total_received_cents` (¢) | 0 | **36,469,039,900** = B = cc-159 D7 |
| DNC | 0 | **33,415,765,200** = B = cc-159 D7 |
| DSCC | 0 | **32,617,811,800** = B = cc-159 D7 |
| committees over 10k inbound rows: non-zero / equal to B | 7 / 7 (of 41) | **41 / 41** |
| `fec_committee_id` rows at 0 with no inbound rows | 2,664 | **2,664** (unchanged) |
| `fec_committee_id` rows at 0 | 8,377 | **2,664** (−5,713) |
| FE `n_dead_tup` / `n_tup_upd` / `n_tup_hot_upd` | 745 / 241,272 / 4,819 | 6,458 / 246,985 / **4,819** (+5,713 updates, all non-HOT: `financial_entities_received_positive` is a partial index on `> 0`) |

- **Two instruments, same definition** (rule 116): the function's result and B's `sum_total` agree for **every** id. That was a zero-mismatch reading, so `Fixes: FIX-1226` was written.
- **The RNC page** (`GET https://civitics-civitics.vercel.app/donors/421c9ec5-…`):
  - before, 18:58:01 → 200 MISS 3.6 s, `Party Committee · $0 donated · $364.7M received`;
  - after, 19:41:55 → 200 MISS 2.9 s, the same.

  It reads `sum_total` (FIX-1217), so it is unaffected, as expected.

**Census** (`data:census:cancellations:prod`, Logs API):

| reading | window | 57014 | front door (15-min buckets, requests / 5xx) |
|---|---|---|---|
| before, 18:52:24 | 17:52–18:52 | 0 — PASS | 17:45 169/0 · 18:00 270/0 · 18:15 156/0 · 18:30 309/0 |
| between runs, 19:29:26 | 18:29–19:29 | 0 — PASS | 18:45 678/0 · 19:00 261/0 |
| after, 19:47:11 | 18:47–19:47 | 0 — PASS | **19:15 147/0 · 19:30 294/0** (the set ran 19:22:32–19:41:10) |

**Renders lost: 0.** Cron over the landing (19:20–19:48): 62 of 62 runs succeeded, 0 startup timeouts.

## 5. What contradicted cc-159 or this prompt

1. **The wall premise.** The prompt sized the set at 1–2 min because "cc-159's bootstrap read the same `donor_rollup_idx` ranges". The function does not read them index-only.
   - **Why:** its `to_type = 'financial_entity'` predicate is not in `donor_rollup_idx`'s key, INCLUDE or partial predicate. On prod the planner takes `financial_relationships_to (to_type, to_id)` with a heap Filter for every inbound row.
   - **Clone:** the set's 1.81 M rows sit on **229,756** heap pages (52 % of the clone's FR heap, 7.89 rows/page), which scales to ~400 k pages (~3.3 GB) on prod.
   - **Measured on prod:** 695 s of first-pass work.
   - **What I did:** stated the wall as minutes, which brought rule 155's two readings ≥ 30 min apart (taken, both 0-rate), and gated the landing for 8 then 18 min instead of 5.
   - **What it cost the front door:** nothing measurable (§4), on a Saturday midday with 147–678 requests per 15 min.

   **The per-whale wall did not match B's unit:** the RNC took **14.17 s** here against **3.2 s** in the bootstrap (`max_recipient_ms 3,202.9`), ×4.4. The DNC took 15.09 s.
2. **D1's 200-id cut did not bound the statements.** It produced two ordinary chunks of 394 k and 442 k rows, both larger than the RNC. The row cap (≤ 50,000 rows) kept every ordinary chunk under 22 s and made the RNC genuinely the largest, last.
3. **The budget guard stopped run 1 early, and the reason was its model, not the set.**
   - **What the projection gets wrong:** a single rows/s rate is dominated by FE write cost on small-recipient chunks (~15 ms per FE row on prod against ~4–11 ms on the clone), so it over-projected 3.3×.
   - **In hindsight the stop was a false positive:** the set would have finished at ~696 s, inside its 960 s budget. The actual wall did exceed the 8 min expected, by 45 %.
   - **What it cost:** a 6-minute pause and 58.8 s of no-op re-reads.
   - **Next time:** a two-term projection (per id + per row) is the right shape. It was not changed in this run; the committed script is what ran.
4. **Sequencing.** Item 3 (FIX-1224) was committed and pushed at 18:56, before the landing rather than after it. It is independent of item 2, and this kept every push out of the landing window.
5. **No non-zero committee had drifted from B.** The drifted class is 0 (read 3(b)), so the old writer only ever wrote 0. The set is exactly cc-159 read 8's 5,713.
6. **fe-crawl and the fec phase did not touch the population** between cc-159's read (01:47 / 03:14) and the apply. FR `max(updated_at)` is still 09-21 00:18:25.83, and every count matches. The claims made fe-crawl and ec-crawl skip once each (19:30).
7. **jobid 14 on 10-01 would have done nothing** (read 4). The FIX-1226 bullet left open whether the monthly reconcile re-derives; it does not. Its received sweep only zeroes positive totals that have no rows. Without this set, the 5,713 would have stayed at 0 until each one's FR rows next changed.
8. **The `total_donated_cents` half of the same defect exists and is small.** 232 committees at 0 have 1,081 outbound donation rows totalling $2.92M (read 3(e)). Reported, not filed, per the prompt's out-of-scope line.
9. **FIX-1226's bullet names one more receipt:** "the RNC non-zero on prod after the next Sunday FEC run". This prompt closes the FIX at the prod set, before that run. The weekend fec phase dispatches at 21:00 UTC tonight, on main with `200a98c2`. The first read after it ends (~22:45 UTC) should confirm the RNC still reads 36,469,039,900 ¢ or more. Not appended (rule 70: the bullet is closed).
10. **Rule 190:** all three standalone census reads (18:52:24, 19:29:26, 19:47:11) were clear of :00/:15/:30/:45 +0–1, and the two gate polls paced themselves. No miss.

## 6. Appends, filings, docs
- **Appends:** none. FIX-1226 and FIX-1224 are closed (rule 70). FIX-1194 needs none: read 4 found no consumer this set disturbs.
- **Filings:** none (rule 106). The donated-total count (§5.8) is for Craig.
- **Local state as left:**
  - The clone's 4,115 zeroed committees are re-derived by the rehearsal; they are local only, and the dry run confirmed they are all equal to local B.
  - The worktree `fix-1226` is torn down after this report lands.

## 7. Push times (UTC), each after a `held=false` read

| push | read at | commit(s) |
|---|---|---|
| 18:55:56 | 18:55:14 | `153eb6f5` |
| 18:56:56 | 18:56:42 | `51631d5d` |
| 18:57:22 | 18:57:09 | `624a1275` |
| 19:47:41 | 19:47:27 | `30480222` |
| 19:48:03 | 19:47:51 | `2bc0259c` |

The report commit is pushed after `pnpm cc:verify 160`.
