---
cc: 133
prompt: (pasted directly — no cc-prompt-133-*.md file in promptsDir)
started_at: 2026-09-18T04:45:00Z
finished_at: 2026-09-18T05:40:00Z
head_before: 782c284f
head_after: d2341e67
commits:
  - {sha: 0b37ebca, subject: "fix(receipts): the cron-jobs table keys on the job's OWN pipeline, not the clock"}
  - {sha: cd81917c, subject: "feat(prod-session): a supervised session HOLDS the guarded pipelines and clears its own holds"}
  - {sha: 1f4159a6, subject: "refactor(kill-switches): delete the dead cron switch, and filter the map at the parse boundary"}
  - {sha: cb239dc6, subject: "feat(db): drop two dead census functions, label the third as deliberately unwired"}
  - {sha: f7e79fe1, subject: "docs(fixes): FIX-1190 / FIX-1173 / FIX-1179 / FIX-1177 / FIX-1172 / FIX-1194 appends"}
  - {sha: d2341e67, subject: "chore(fixes): sync status after FIX-1190 / FIX-1173 / FIX-1179"}
fixes_closed: [FIX-1190, FIX-1173, FIX-1179]
fixes_filed: []
fixes_reopened: []
migrations_pushed: [20260919010000_fix1179_drop_dead_census_functions.sql]
prod_writes: "One DDL migration (20260919010000) applied 2026-09-18 05:22:16 UTC - two DROP FUNCTION IF EXISTS plus one COMMENT ON FUNCTION. No data written, no VACUUM, no cron.job change, no session claim on prod. Everything else against prod was a read via scripts/db-query.mjs --prod (read-only by construction) or receipts-daily.ts --prod --dry-run (SET default_transaction_read_only = on, writes no files)."
ci: green
stopped_items: []
---

# cc-133 — observability smalls

All four items landed. Three FIXes closed (FIX-1190 local, FIX-1173 local,
FIX-1179 prod); FIX-1177 and FIX-1172 have their mechanism built and stay open
for the prod receipt. Open count 73 → 70, as predicted. No STOP fired. Two
premises in the prompt were wrong and are reported in §7.

`head_before` was `782c284f` exactly — nothing had landed past cc-131's report
commit, so there were no bot receipts commits to read.

`.github/workflows/tests.yml` on main carries the seven blocking checks by name:
`Typecheck` (`pnpm turbo run typecheck`), `Lint`, `Silent-read guard`
(`check:reads`), `Render-timeout guard`, `Workflow resource-parity guard`,
`Inert statement_timeout guard` (`check:proconfig`), and the three unit suites
(`@civitics/data`, `@civitics/app-civitics`, `@civitics/db`).

---

## 1. Read 2 — the job → pipeline mapping, and read 3's inherited rows

Derived on the clone by the mechanism in P2 (`cron.job.command` → the procedure
it CALLs → the pipeline literal in that procedure's `prosrc`), comment-only lines
stripped first. All 38 rows:

| jobname | active | proc | pipeline | writes_dsl | guarded |
|---|---|---|---|---|---|
| abuse-events-retention | yes | purge_abuse_events | — | no | no |
| agency-staffing-rollup-refresh | yes | refresh_agency_staffing_rollup | agency_staffing_rollup_refresh | yes | **yes** |
| contract-flow-rollups-refresh | yes | refresh_contract_flow_rollups | contract_flow_rollups_rebuild | yes | **yes** |
| cron-job-budget-watchdog | yes | enforce_cron_job_budgets | — | no | no |
| derived-mvs-unit-watchdog | yes | enforce_derived_mvs_unit_budget | — | no | no |
| donation-edge-orphan-sweep | no | reconcile_donation_edge_orphans | donation_edge_orphan_sweep | yes | **yes** |
| donor-party-rollup-orphan-sweep | no | reconcile_donor_party_rollup_orphans | donor_party_rollup_orphan_sweep | yes | **yes** |
| donor-party-rollup-refresh | no | refresh_donor_party_rollup_incremental | donor_party_rollup_refresh | yes | **yes** |
| donor-rollup-orphan-sweep | no | reconcile_donor_rollup_orphans | donor_rollup_orphan_sweep | yes | **yes** |
| donor-rollup-refresh | yes | donor_rollup_rebuild_bulk | donor_rollup_refresh | yes | **yes** |
| dpr-vacuum-analyze | yes | — | — | no | no |
| ec-crawl | no | run_entity_connections_rebuild | entity_connections_rebuild | yes | **yes** |
| ec-vacuum-analyze | no | — | — | no | no |
| ecs-vacuum-analyze | yes | — | — | no | no |
| entity-connection-stats-orphan-sweep | no | reconcile_entity_connection_stats_orphans | entity_connection_stats_orphan_sweep | yes | **yes** |
| entity-connection-stats-rebuild | no | rebuild_entity_connection_stats | entity_connection_stats_rebuild | yes | **yes** |
| fe-crawl | no | run_fe_totals_crawl | financial_entity_totals_refresh | yes | **yes** |
| fe-vacuum-analyze | yes | — | — | no | no |
| financial-entity-totals-incremental | no | refresh_financial_entity_totals_incremental | financial_entity_totals_refresh | yes | **yes** |
| financial-entity-totals-reconcile | yes | reconcile_financial_entity_totals | financial_entity_totals_reconcile | yes | **yes** |
| fr-vacuum-analyze | yes | — | — | no | no |
| group-donor-rollup-refresh | no | run_group_donor_rollup_refresh | group_donor_rollup_refresh | yes | **yes** |
| odbt-vacuum-analyze | yes | — | — | no | no |
| odr-mv-vacuum-analyze | yes | — | — | no | no |
| odt-vacuum-analyze | yes | — | — | no | no |
| officials-vacuum-analyze | yes | — | — | no | no |
| osar-vacuum-analyze | yes | — | — | no | no |
| osdr-vacuum-analyze | yes | — | — | no | no |
| platform-counts-daily | yes | refresh_platform_counts | platform_counts_daily | yes | no |
| rebuild-ec-incremental | no | run_entity_connections_rebuild | entity_connections_rebuild | yes | **yes** |
| rebuild-ec-incremental-mon | no | run_entity_connections_rebuild | entity_connections_rebuild | yes | **yes** |
| refresh-derived-mvs-daily | no | refresh_derived_mvs | refresh_derived_mvs | yes | **yes** |
| refresh-derived-mvs-weekly | no | refresh_derived_mvs | refresh_derived_mvs | yes | **yes** |
| rule-taggers-daily | no | run_rule_taggers | run_rule_taggers | yes | **yes** |
| rule-taggers-weekly | no | run_rule_taggers | run_rule_taggers | yes | **yes** |
| treemap-individuals-global-refresh | yes | refresh_treemap_individuals_global | treemap_individuals_global_refresh | yes | **yes** |
| treemap-individuals-vacuum-analyze | yes | — | — | no | no |
| vote-stats-refresh | yes | rebuild_official_vote_stats | official_vote_stats_rebuild | yes | **yes** |

**The expectations held exactly.** Every `writes_dsl = true` job resolved a
pipeline — zero exceptions, so nothing to STOP on. All twelve `*-vacuum-analyze`
jobs resolved `proc = NULL` (bare `VACUUM`, no procedure). The two
every-two-minutes watchdogs resolve a proc but no pipeline, because they READ and
UPDATE other pipelines' rows and never INSERT one; `abuse-events-retention` is a
third of that kind (a real procedure that writes no `data_sync_log` row). Every
non-obvious resolution the prompt named checked out, including
`donor_rollup_rebuild_bulk` via the `c_pipeline text := '...'` form.

`active` differs from prod here, exactly as CLAUDE.md / FIX-946 says it will —
the clone's `cron` schema is local history. The mapping itself does not depend on
`active`, and prod's own run of the same query (§1, read-5 diff) returned the
same 38 name→pipeline pairs.

**Guarded pipelines — 17 distinct, item 2's set:**

`agency_staffing_rollup_refresh` · `contract_flow_rollups_rebuild` ·
`donation_edge_orphan_sweep` · `donor_party_rollup_orphan_sweep` ·
`donor_party_rollup_refresh` · `donor_rollup_orphan_sweep` ·
`donor_rollup_refresh` · `entity_connection_stats_orphan_sweep` ·
`entity_connection_stats_rebuild` · `entity_connections_rebuild` ·
`financial_entity_totals_reconcile` · `financial_entity_totals_refresh` ·
`group_donor_rollup_refresh` · `official_vote_stats_rebuild` ·
`refresh_derived_mvs` · `run_rule_taggers` · `treemap_individuals_global_refresh`

`platform-counts-daily` is the one job that writes a `data_sync_log` row and is
NOT guarded, so it is correctly outside the hold set.

**Commented literals: none.** Five comment lines across the cron-called
procedures mention `data_sync_log` and all five are prose (`refresh_derived_mvs`
l.336, `enforce_cron_job_budgets` l.53, `run_entity_connections_rebuild` l.146
and l.149, `refresh_platform_counts` l.118). The regexes have no comment
awareness of their own, so comment-only lines are stripped before they run and a
test pins that.

### Read 3 — the two inherited rows in `docs/receipts/2026-09-15.md`

Exactly two rows in that 38-row table carry `verdict = skipped`, and both resolve
`pipeline = NULL`:

```
| ec-vacuum-analyze | yes | 30 4 * * * | 2026-09-14T04:30:00.253Z | 132.9 s (2m 13s) | succeeded | 0-140 s | **skipped** | peer crawl ec_crawl is backed off until 2026-09-14 06:21:44.694017+00 - the box is throttled for both of us |
| officials-vacuum-analyze | yes | 30 1 * * 1 | 2026-09-14T01:30:00.172Z | 3.8 s | succeeded | - | **skipped** | peer crawl ec_crawl is backed off until 2026-09-14 03:05:29.416168+00 - the box is throttled for both of us |
```

The first is the sharpest form of the bug: a 132.9-second vacuum that ran to
completion **inside its 0–140 s band** rendered as an interlock skip on another
job's reason, with the band it was inside never consulted.

### Read 5's diff — old vs new, measured on prod

The prompt asked for a text diff of a clone run against `2026-09-15.md`. That
instrument does not work and was replaced with a better one. Two reasons: the
clone's `cron.job_run_details` is local history, not prod's (FIX-946), so a clone
run cannot reproduce a prod-produced file; and `Q_CRON_JOBS` takes "the last
firing" relative to `now()`, not to `--date`, so even a prod run three days later
differs from the file by the passage of time rather than by the fix.

So both queries were run in ONE prod read session (read-only, 7-day lookback,
2026-09-18 ~04:5x UTC), which isolates the change from the clock. **12 of 38 jobs
had an inherited correlation, not 2:**

| jobname | derived pipeline | old sync_status | old row came FROM | new sync_status |
|---|---|---|---|---|
| contract-flow-rollups-refresh | contract_flow_rollups_rebuild | complete | financial_entity_totals_refresh | (null) |
| cron-job-budget-watchdog | — | complete | financial_entity_totals_refresh | (null) |
| derived-mvs-unit-watchdog | — | complete | financial_entity_totals_refresh | (null) |
| dpr-vacuum-analyze | — | complete | financial_entity_totals_refresh | (null) |
| ec-crawl | entity_connections_rebuild | complete | financial_entity_totals_refresh | (null) |
| ec-vacuum-analyze | — | complete | financial_entity_totals_refresh | (null) |
| fr-vacuum-analyze | — | partial | financial_entity_totals_refresh | (null) |
| officials-vacuum-analyze | — | **skipped** | financial_entity_totals_refresh | (null) |
| osar-vacuum-analyze | — | **skipped** | entity_connections_rebuild | (null) |
| osdr-vacuum-analyze | — | **skipped** | entity_connections_rebuild | (null) |
| treemap-individuals-global-refresh | treemap_individuals_global_refresh | complete | financial_entity_totals_refresh | (null) |
| vote-stats-refresh | official_vote_stats_rebuild | complete | financial_entity_totals_refresh | (null) |

The other 26 rows are identical between old and new.

Two things to read off that table. First, in **every** changed row the old value
came from a different pipeline than the job's own — so none of these is "the new
key is wrong for that job", which is what the STOP condition was guarding
against. Second, `fe-crawl` runs `*/30` and `ec-crawl` `*/15`, so their rows
blanket the timeline; any firing landing within 90 s of one of those boundaries
borrowed it. That is why four jobs that DO have a pipeline are in the list: their
own writer had not logged inside the window (both crawl arms are gated and log
only sometimes), so the time-first join reached for the nearest row instead of
reporting nothing.

**Verdict-level: exactly three rows move, and all three were inherited.**
`verdictFor` branches on `sync_status === "skipped"` alone, so the nine
`complete`/`partial` corrections are invisible to it. The three that move are
`officials-`, `osar-` and `osdr-vacuum-analyze`, all pipeline-less. **Zero
non-inherited verdicts change.** No STOP.

End-to-end confirmation, `receipts-daily.ts --prod --dry-run`:
`ec-vacuum-analyze` now renders `in-band` (133.4 s inside 0–140 s) where the
09-15 file said `skipped`; `officials-vacuum-analyze` renders `no-band`.

---

## 2. Read 4 — P3 and P4 as written

**`claimProdSession()`'s return statement** (`prod-session.ts`, before this run's
edit):

```ts
  return acquireNamedSessionLock(PROD_SESSION_LOCK_NAME, {
    logTag: "prod-session",
    ref: "FIX-950",
    label: { key: PROD_SESSION_LABEL_KEY, value },
    // The SAME dsn the preflight read. Letting the lock re-resolve buildDbUrl()
    // would let the preflight and the hold land on different databases.
    dbUrl,
  });
```

**`NamedSessionLock`** (`lib/session-lock.ts:35`):

```ts
export interface NamedSessionLock {
  /** True when the caller may proceed - lock held, bypassed, or failed open. */
  readonly acquired: boolean;
  /** Why the lock was not acquired - set only when `acquired` is false. */
  readonly blockedBy?: string;
  /** Release the lock, drop the label, close the connection. Safe to call twice. */
  release(): Promise<void>;
}
```

Confirmed: there is no `releaseProdSession()`, release is the returned object's
`release()`, and `withProdSession`'s `finally` calls it.

**`list_scheduled_rollup_pipelines()`'s two CASE arms** (migration
`20260903000000`, ~line 357):

```sql
               'report_after_hours',
                 CASE WHEN retired IS TRUE OR held IS TRUE OR NOT has_closures THEN NULL
                      ELSE ROUND(cadence_hours * 1.5, 1) END,
               'escalate_after_hours',
                 CASE
                   WHEN retired IS TRUE OR held IS TRUE      THEN NULL
                   ...
```

**`canary-check.ts:565`:**

```ts
    const reports = stale && w.reportAfterHours !== null && !w.retired && !w.held;
```

Both quotes are there, so no migration was needed and item 2 proceeded.

**RLS.** `rollup_watch_overrides` has `relrowsecurity = t`, but
`relforcerowsecurity = f`, its owner is `postgres`, and `postgres` carries
`rolbypassrls = t`. The prod-session client connects as `postgres`
(`buildDbUrl()` → `postgresql://postgres:postgres@127.0.0.1:54322/postgres`
locally, `postgres.<ref>` via the session pooler on prod), so **RLS does not
apply to it** and no policy was needed.

---

## 3. Read 5 — the `cron` classification

`grep -rn '"cron"|CRON_ENABLED|CRON_DISABLED'` over `apps packages scripts docs`
(`.ts/.tsx/.mjs/.md`, excluding `types/database.ts` and pg_cron / `cron.job`
matches):

| file:line | verdict | why |
|---|---|---|
| `packages/db/src/kill-switches.ts:8` | **delete** | header line "(Inverted for cron: CRON_DISABLED...)" |
| `packages/db/src/kill-switches.ts:36` | **delete** | `"cron"` in the `KillSwitchName` union |
| `packages/db/src/kill-switches.ts:70` | **delete** | `ENV_RULES` comment naming CRON_DISABLED |
| `packages/db/src/kill-switches.ts:82` | **delete** | `cron: { envVar: "CRON_DISABLED", off: "true" }` |
| `apps/.../admin/kill-switches/route.ts:19` | **delete** | comment listing CRON_DISABLED among per-feature kills |
| `apps/.../admin/kill-switches/route.ts:41` | **delete** | `"cron"` in `ALLOWED_NAMES` |
| `packages/data/src/feature-flags.ts:5` | **delete** | header "(or 'true' for CRON_DISABLED)" |
| `packages/data/src/feature-flags.ts:28-29` | **delete** | `FLAGS.CRON_ENABLED` itself |
| `packages/data/src/feature-flags.ts:32` | **keep, reworded** | FIX-998's flag comment referenced CRON_ENABLED by name; the flag stays, the dead referent goes |
| `apps/.../api/cron/nightly-sync/route.ts:47-49` | **keep** | the live env control |
| `apps/.../api/cron/notify-followers/route.ts:56-57` | **keep** | the live env control |
| `docs/API.md:491` | **keep, corrected** | documents `CRON_DISABLED` on the nightly-sync route; gained the scope clause |
| `docs/OPERATIONS.md:500` | **keep, corrected** | said "Stops nightly cron from running" - the exact wrong belief; now names the two Vercel routes and states the GHA nightly and all 38 pg_cron jobs are unaffected |
| `docs/ops/prod-holds.md:149-160` | **rewritten** | §3, "dead as of 2026-09-11" → removed |
| `scripts/db-clone-prod.mjs:370,515` | **not in scope** | the `cron` *schema*, not the switch |
| `docs/cc/reports/cc-123.md:264` | **not in scope** | a GHA `on.schedule` cron expression |
| prod `pipeline_state.kill_switches.cron`, `kill_switch_events` | **data** | not migrated; see §7 |

**`FLAGS.CRON_ENABLED` consumers: ZERO.** Grep over `apps packages scripts`
excluding the definition returned nothing, and `pnpm turbo run typecheck` passes
across all 10 packages with the flag deleted — which is the structural proof, not
just the grep's. No STOP.

There was no documented switch-*name* list in `API.md` or `OPERATIONS.md` to
strip (only the env var), so those two files were corrected rather than cut.

---

## 4. Read 6 — the prod census for FIX-1179, and read 7

All via `node scripts/db-query.mjs --prod` (read-only by construction).

**(a) Window and calls.** `pg_stat_statements_info.stats_reset` reads
`2026-09-15 15:09:48.238057+00`; the read ran at `2026-09-18 04:47:47.948181+00`.
Window = **[2026-09-15 15:09:48 UTC, 2026-09-18 04:47:48 UTC]**, about 61.6 h.

| function | calls in window |
|---|---|
| `get_group_donor_totals` | **0** |
| `has_active_official_grant` | **0** |
| `rebuild_financial_entity_donation_totals_full` | **0** |

This confirms **P7**: the bullet's cited baseline (2026-09-08 15:12 UTC) no
longer exists, so the census is stated against the 09-15 reset epoch (D3), and
that epoch is written into the migration's header.

**(b) Grant matrix** (`information_schema.routine_privileges`):

| function | grants |
|---|---|
| `get_group_donor_totals` | `postgres:EXECUTE, service_role:EXECUTE` |
| `has_active_official_grant` | `postgres:EXECUTE, service_role:EXECUTE` |
| `rebuild_financial_entity_donation_totals_full` | `postgres:EXECUTE, service_role:EXECUTE` |

As P6 said — FIX-834/835 had already revoked PUBLIC / anon / authenticated, so
none was anon-reachable. Worth noting because
`20260608030100_qa_lane.sql:48` granted `has_active_official_grant` to
`authenticated, anon` originally; the revoke sweep took it back.

**(c) Reference censuses** — all zero for all three:

| function | pg_policy | pg_views | sibling prosrc | pg_depend | cron.job cmd | as trigger |
|---|---|---|---|---|---|---|
| `get_group_donor_totals` | 0 | 0 | 0 | 0 | 0 | 0 |
| `has_active_official_grant` | 0 | 0 | 0 | 0 | 0 | 0 |
| `rebuild_financial_entity_donation_totals_full` | 0 | 0 | 0 | 0 | 0 | 0 |

Plus zero repo-side callers by grep across `apps packages scripts` (excluding the
generated types). No non-zero anywhere, so no function was skipped and no STOP.

**(d) Pending count before item 4: 0.** `supabase db push --dry-run` reported
"Remote database is up to date", i.e. cc-131's `20260919000000_fix1195_...` is
applied.

**Read 7 — the proconfig checker.** `pnpm check:proconfig` PASSed on the clone
before item 4 ("4 migration(s) scanned after the FIX-1128 baseline; 0 waived, 0
undeclared"). `scripts/check-proconfig-timeouts.mjs` carries **no per-function
list** — it is a lexical scan of migration text for `SET statement_timeout` in
routine definitions plus an `ALTER FUNCTION ... SET statement_timeout` regex — so
nothing there needed the two dropped names removed. Confirmed by reading it.

---

## 5. Item 2 — the four clone readings

**[1] Held, session rows.** `pnpm session:claim --reason "cc-133 item 2 clone
walkthrough" --minutes 5 --hold-seconds 45` printed
`[prod-session] held 16 guarded pipeline(s) (FIX-1177/1172)`, and
`SELECT pipeline, hold_reason FROM rollup_watch_overrides WHERE hold_reason LIKE
'prod session held:%'` returned 16 rows, each
`prod session held: cc-133 item 2 clone walkthrough`:

`agency_staffing_rollup_refresh` · `contract_flow_rollups_rebuild` ·
`donation_edge_orphan_sweep` · `donor_party_rollup_orphan_sweep` ·
`donor_party_rollup_refresh` · `donor_rollup_orphan_sweep` ·
`donor_rollup_refresh` · `entity_connection_stats_orphan_sweep` ·
`entity_connections_rebuild` · `financial_entity_totals_reconcile` ·
`financial_entity_totals_refresh` · `group_donor_rollup_refresh` ·
`official_vote_stats_rebuild` · `refresh_derived_mvs` · `run_rule_taggers` ·
`treemap_individuals_global_refresh`

**Sixteen, not the seventeen of read 2** — and the missing one is the design
working. `entity_connection_stats_rebuild` already carries a
`retired_at = 2026-08-26 16:00:00+00` row (FIX-1161), and the upsert's
`WHERE ... retired_at IS NULL` correctly declined to hold it. The `not_both` CHECK
would have rejected the write anyway; the guard means it is never attempted. This
turned up first as an unexplained `cleared 16 hold(s)` line during the existing
interlock suite, which is how it got chased down.

**[2] The registry's view while held.** All 16 read back from
`list_scheduled_rollup_pipelines(30)->'pipelines'` as `held = true` with
`report_after_hours = NULL` **and** `escalate_after_hours = NULL`. So the
suppression is real and it is the existing read-side mechanism doing it — no SQL
function was changed.

**[3] Release.** `cleared 16 hold(s)` then `interlock released`. Zero rows with
the session prefix and zero rows carrying the FIX-1177 note; the table went back
to exactly its three pre-existing human declarations
(`entity_connection_stats_rebuild` retired, `recipient_count_reconcile` retired,
`front_door_watch` cadence 0.25).

**[4] SIGKILL → canary → self-heal.** A second claim was `taskkill /F`'d. The 16
rows persisted with `prod_session_state()->>'held' = false`. `pnpm
data:canary-check` against the clone then reported:

```
[canary-check] session holds: 16 rollup pipeline(s) still HELD with no supervised
session on the box; oldest held 0h: agency_staffing_rollup_refresh, ... A held
pipeline reports and escalates NOTHING (list_scheduled_rollup_pipelines NULLs
both thresholds), so freshness is unwatched for these until the hold clears.
```

with condition key `prod_session_hold_stale` in the findings (and, separately,
`prod_session_label_stale` for the dead label — the two are distinct keys on
purpose, since one has nobody on the box and the other has somebody). A third
claim then swept the leftovers, re-held its own 16, and cleared on release. Net
zero rows.

**`detector-coverage.test.ts` was NOT red before the key was registered** — and
could not have been. See §7(a).

---

## 6. Item 4 — push time, `migration list`, prod after-check

- `migration list` **before**: pending **1**, exactly
  `20260919010000_fix1179_drop_dead_census_functions.sql`.
- **Pushed 2026-09-18 05:22:16 UTC** via `pnpm db:push:prod`. Outside the
  22:30–01:00 nightly start band, outside cc-132's Saturday 12:00–21:30 window,
  and 38 minutes clear of the 06:00 `refresh-derived-mvs-daily` firing. Prod was
  quiet at the time — `data_sync_log` over the preceding 3 h showed only
  `front_door_watch` and the sub-second `financial_entity_totals_refresh` crawl
  arm.
- `migration list` **after**: "Remote database is up to date" — pending **0**.
- **Prod after-check:**

```
                 proname                  |  comment
------------------------------------------+-----------
 has_active_official_grant                | FIX-1179 (cc-133): KEPT unwired - the answer-side gate mirroring has_active_constituent_grant, which IS live (apps/civitics/app/api/comments/_lib.ts). Zero callers as of 2026-09-18; service_role EXECUTE only. Wire it or drop it when the official-answer lane lands.
 rebuild_financial_entity_donation_totals |
(2 rows)
```

Both drops gone, the comment on the kept function present, and the callee
`rebuild_financial_entity_donation_totals()` untouched. The commit carrying
`Verified: prod` / `Fixes: FIX-1179` was made **after** the push, so the trailer
is honest.

---

## 7. What contradicted the design

**(a) `detector-coverage.test.ts` does not enumerate condition keys.** The prompt
said to "add the new key to the canary's condition census the way
`KEY_LABEL_STALE` is registered (read `detector-coverage.test.ts` — it enumerates
keys; a new key that is not registered fails that test, which is the point)", and
the verification section asked for it "red before it was registered".

There is no such census. `src/__tests__/detector-coverage.test.ts` is a
**DB-backed** suite about the FIX-977 registry ⊇ census property — it has no
condition-key list and cannot go red for an unregistered key. The only
"registration" of `KEY_OVERRUN` / `KEY_LABEL_STALE` anywhere is a three-line
stability assertion in `canary-prod-session.test.ts` ("the condition keys are
stable"). `KEY_HOLD_STALE` got the equivalent — its own
`assert.equal(KEY_HOLD_STALE, "prod_session_hold_stale")`. So the new key never
went red, because nothing in the tree can make it. `detector-coverage.test.ts`
runs green (16/16). **This is a gap worth knowing about rather than a defect
introduced here: a new canary condition key genuinely has nothing enforcing that
it is enumerated anywhere.** Not filed, because the prompt said not to widen
scope on a surprise — but it is the one thing in this run that would justify a
bullet.

**(b) `packages/db` had no kill-switches test file.** Item 3 step 4 said "the
`@civitics/db` suite already covers kill switches (read which file)". It did not
— there is no `kill-switches.test.ts` and no `auto-trip-evaluator.test.ts`; the
28 suites in that package cover billing, platform costs, front-door verdict and
so on. A new `packages/db/src/kill-switches.test.ts` was created with 7 cases,
including the parse-boundary case the prompt asked for.

**(c) A fourth shared pipeline the prompt did not enumerate.** The prompt named
three sets of jobs sharing a pipeline (the three EC jobs; daily/weekly
derived-mvs; daily/weekly rule-taggers). There is a fourth: `fe-crawl` **and**
`financial-entity-totals-incremental` both resolve
`financial_entity_totals_refresh`. So eight jobs share four pipelines, and the
±90 s disambiguator carries one more case than designed. It is documented in the
`Q_CRON_JOBS` header.

**(d) No job resolved a pipeline it does not write.** This is the failure mode the
prompt asked to be reported even if every test is green, and the A/B prod read is
the check: in all 12 changed rows the OLD query had borrowed a foreign pipeline's
row and the NEW query resolved nothing, never a wrong name. The 26 unchanged rows
all matched their own pipeline. Nothing to report beyond that it was looked for.

**(e) `verdictFor` with a NULL `sync_status` does exactly what was predicted.** It
falls through the `=== "skipped"` branch to the cron-status logic. Nothing else in
the matrix reads `sync_status`. No change was needed and none was made; a test
records that by feeding it the old inherited shape and asserting it still says
`skipped` (the wrong-but-green fixture).

**(f) A known limit was accepted rather than engineered away.** An inline comment
*between* the column list and `VALUES` breaks the literal regex, because the
pattern's inter-token gaps are `\s*`. No cron procedure has that shape. The
alternative — a blanket comment strip — would silently truncate a body at a `--`
inside a string literal and could resolve the **wrong** pipeline. A visible
missing correlation (renders `—`, verdict falls to cron status) beats an invisible
wrong one, which is FIX-1190's own lesson, so the limit is asserted as a test with
the reasoning in its docstring. Checked first that no cron procedure has `--`
inside a string literal today; the five candidate lines are all prose apostrophes
("unit's", "chunk's").

**(g) The session-created `note` rows survive in no state this prompt did not
enumerate.** After every walkthrough the table held only its three human rows. The
one path worth naming: if a human adds a `cadence_hours` or a `retired_at` to a
row the session created, the release's DELETE declines it (all four `IS NULL`
predicates must hold) and the row survives as a human declaration with the
FIX-1177 note still on it. That is the right outcome — the human's assertion
outlives the machinery — but the note then reads slightly oddly. Not worth a
migration; worth knowing.

**(h) The read-5 instrument was replaced, not skipped.** Stated in §1: a clone
diff against a prod-produced receipts file is not a valid instrument (FIX-946 for
the cron history, `now()`-relative lookback for the drift), so the A/B prod read
was used instead. It answers the same question more precisely.

**(i) One addition beyond the prompt.** The pre-claim leftover sweep now logs when
it actually clears something (`swept N leftover hold(s) from a session that did
not release`). The prompt specified only `held N` and `cleared N`. A silent sweep
would delete the only evidence that a previous session died without releasing,
and that evidence is exactly what an operator wants after the fact.

**Also noted, no action:** `packages/db/CLAUDE.md`'s FIX-1059 section repeats the
`rollup_watch_overrides` table COMMENT's claim that it is "read only by
`list_scheduled_rollup_pipelines()`". That is now stale in two ways —
`canary-prod-session.ts` reads it and `claimProdSession()` writes it. The
CLAUDE.md prose was corrected; the table COMMENT is migration text and was left as
written, with the drift named in the same paragraph. `session:check` also reports
one unmerged branch and open PR #3 (`chore/refresh-disposable-emails`, a bot PR
from 2026-08-03) — pre-existing and unrelated.

---

## 8. FIX status after `fixes:sync`

```
FIX-1172  OPEN    (no done.log row)
FIX-1173  closed  2026-09-17 1f4159a6 local-only
FIX-1177  OPEN    (no done.log row)
FIX-1179  closed  2026-09-17 cb239dc6 prod-only
FIX-1190  closed  2026-09-17 0b37ebca local-only

5 shown - 70 open of 1183 known ids
```

**Open count 73 → 70**, exactly as predicted. `fixes:sync` appended three rows and
touched no bullet; `fixes:check` is clean.

FIX-1177 and FIX-1172 close on the receipt named in their appends: **a hold longer
than 48 minutes on prod with no `stale rollup: financial_entity_totals_refresh`
report and no escalation from the nightly canary** — Monday or later, recorded
Pattern B with `Verified: prod`. And the first `prod_session_hold_stale`, if one
ever fires.

## CI

Four code commits, four green `tests.yml` runs: **35309357450** (0b37ebca),
**35310144186** (cd81917c), **35310393557** (1f4159a6), **35310574680**
(cb239dc6). The two docs-only commits (f7e79fe1, d2341e67) do not trigger
`tests.yml` — it carries `paths-ignore: docs/**` — and both are green on
`fixes-integrity`.

Test counts: `@civitics/data` 699 → **733** (730 pass, 3 pre-existing skips, 0
fail); `@civitics/db` 198 → **205**; `@civitics/app-civitics` **275**, unchanged.
`check:reads`, `check:render-timeouts`, `check:workflow-parity`,
`check:proconfig` + `check:proconfig:test`, `session:worktree:test` and
`drain:test` all pass locally.
