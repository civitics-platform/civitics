---
cc: 159
prompt: cc-prompt-159-fix1217-1225-landing-partial-spending-index-fe-inbound-rollup-fix1226-writer-pattern-bs.md
started_at: 2026-09-26T01:38:15Z
finished_at: 2026-09-26T04:00:21Z
head_before: ff51c3ed
head_after: 581bc3cb
commits:
  - {sha: 200a98c2, subject: "fix(data): FIX-1226 — the unscoped PAC entity upsert no longer overwrites received totals on conflict"}
  - {sha: e321504d, subject: "docs(audits): FIX-1221 Pattern B — the retention's first firing"}
  - {sha: a3a62cf6, subject: "chore(fixes): sync status after FIX-1221"}
  - {sha: 7240c1a7, subject: "feat(data): FIX-1217 — wait-for-gate composes the census (stop|report|off); the FR spending index build script"}
  - {sha: 66cd1394, subject: "feat(db): FIX-1217 — the FE inbound rollup migration"}
  - {sha: 8a4a326d, subject: "feat(app): FIX-1217, FIX-1225 — the donor page reads the FE inbound rollup; the spending read is bounded"}
  - {sha: cd3c1437, subject: "chore(fixes): sync status after FIX-1217, FIX-1225"}
  - {sha: 1c7307ef, subject: "chore(fixes): record prod verification of FIX-1217, FIX-1225"}
  - {sha: ee961c98, subject: "chore(fixes): sync status after the FIX-1217, FIX-1225 prod record"}
  - {sha: 581bc3cb, subject: "docs(fixes): FIX-1226 / 1194 appends; file FIX-1227, FIX-1228; gate (g)'s fr-vacuum band; prod-holds B paragraph (cc-159)"}
fixes_closed: [FIX-1221, FIX-1217, FIX-1225]
fixes_filed: [FIX-1227, FIX-1228]
fixes_reopened: []
migrations_pushed: [supabase/migrations/20260926000000_fix1217_fe_inbound_rollup.sql]
prod_writes: "Three landings, each through session:wait-for-gate:prod --census stop --then, from the primary checkout, each under a claimed FIX-950 session released afterwards. (i) 02:11:11-02:12:44 UTC: CREATE INDEX CONCURRENTLY financial_relationships_fe_spending_idx (to_id, amount_cents DESC) WHERE relationship_type IN (contract, grant) AND to_type = financial_entity, plus its COMMENT, via scripts/fix1217-build-fr-spending-index.mjs: valid, 114,360,320 B. (ii) ~02:51:35 UTC: migration 20260926000000 via db-push-prod.mjs: the index IF NOT EXISTS skipped (already there); tables financial_entity_inbound_rollup and financial_entity_inbound_totals with RLS, a SELECT policy and grants; refresh_fe_inbound_rollup_unit(uuid[]) and run_fe_inbound_rollup(integer) with REVOKE/GRANT; cron.schedule fe-inbound-rollup-refresh (prod jobid 55, 13 2 * * *, active, first scheduled firing 2026-09-27 02:13 UTC); its cron_job_budget row (1,800 s); its cron_job_first_seen row (02:51:37); the pipeline_state bootstrap cursor row. (iii) 03:12:34-03:13:36 UTC: CALL public.run_fe_inbound_rollup(p_max_units := 1000) via db-query --prod --call --yes-i-mean-prod under the claimant GUC: complete and caught_up, 49 units, 9,656 recipients, 561,499 rollup rows + 9,656 totals rows, one data_sync_log row (pipeline financial_entity_inbound_rollup), the cursor row replaced by financial_entity_inbound_rollup_watermark {last_indexed_at 2026-09-26 02:12:34.316}. Each claim wrote and cleared its rollup_watch_overrides holds (16 at (i) and (ii), 17 at (iii)). Pushes to main, each a Vercel deploy (the app changed only at 8a4a326d, Vercel success before 03:21:30): 01:55:47, 01:56:39, 01:57:11, 02:09:47, 02:23:03, 03:17:36, 03:27:05, 04:00:02, and this report. No VACUUM, no EXPLAIN ANALYZE and no ad-hoc DELETE on prod. Every other prod contact was a read: db-query --prod (counts, EXPLAIN, pg_stat_user_tables, cron reads, prod_op_gate), the census (Logs API, 7 readings), gh, and HTTPS GETs of three donor pages on the Vercel origin."
ci: green
stopped_items:
  - "Item 2b, FIX-1224's Pattern B: the dispatched 05:00 UTC canary had not fired when read 2 ran (01:55 UTC, and this session ended before 05:00), so there was no receipt to quote. No commit; FIX-1224 stays open with its receipt named on the bullet."
  - "Item 6's function-log check: the Vercel CLI on this machine has no stored credentials (vercel logs starts an interactive device login), so the function log for the three renders was not read. What stands in for it: every section of the three renders is populated (a withDbTimeout timeout renders as empty lists and 0), and the census read 0 57014s over 03:11:34-03:26:34, which covers the renders."
  - "Item 6's curls went to https://civitics-civitics.vercel.app, not https://civitics.com: Cloudflare answers every scripted client on civitics.com (home, robots.txt, a browser user-agent) with a 403 managed challenge (cf-mitigated: challenge), as docs/CLOUDFLARE.md section 5 records. The Vercel origin serves the same deployment."
---

# cc-159: the whale donor page lands — a 109 MB partial spending index, the FE inbound rollup bootstrapped in 62 s, and the page reads true top-50s and donor counts

**Summary.**
- **FIX-1226's writer fix** (item 1) was pushed at **01:55:47 UTC**, 19 h before the Sat 21:00 UTC deadline. An unscoped `upsertPacEntitiesBatch` now sets neither aggregate on conflict. No trailer: the re-derive of the committees already at 0 is cc-160, and read 8 sizes it at **5,713 committees / 3,154,766 inbound rows**.
- **FIX-1221 closed** on its first firing (item 2a): 01:51:00 UTC, `DELETE 1682`, 35 ms.
- **FIX-1217 and FIX-1225 closed**, `local + prod`, in three landings, each opened by `wait-for-gate` with the new census half (D2):
  - **(i) The spending index CIC:** **93.8 s** on prod, **114,360,320 B**, valid. Against the 6.4 min projection that is 4× faster; against the ≤ 19 min bracket, 12×. As anon, EXPLAIN is now `Limit ← Index Scan` for the RNC and AmerisourceBergen.
  - **(ii) The rollup migration:** prod jobid 55, `13 2 * * *`, budget 1,800 s.
  - **(iii) The bootstrap:** **62.4 s**, complete and caught_up. 49 units, **9,656** recipients (cc-158 read 4's exact count), **561,499** rollup rows.
  - **D7:** the RNC, DNC and DSCC are **0 missing / 0 extra / sums and ranks equal** against a live aggregate.
- **The page on prod:** the RNC renders **Donors 283,715 (337,058 transactions)**, **Total received $364.7M** and 50 true top donors. Before, it showed a sampled top 50, a sample's donor count, and $0 received.
- **The census read 0 × 57014 before and after every landing**, so the measured render cost of all three was **0**.
- **Filed:** FIX-1227 (ISR caches a degraded render) and FIX-1228 (the from-side Recipients sample: 57 prod donors over 1,000 outbound rows, max 2,074).
- **Open:** 60 − 3 (1221, 1217, 1225) + 2 filed = **59**.

## 1. Phase 0 reads

### Read 1 — state (01:38–01:40 UTC)
- **Session check:** `session:check` at `ff51c3ed`: **60 open**, as expected.
- **Branches and PRs:** one unmerged branch, `chore/refresh-disposable-emails` (PR 4, the monthly blocklist refresh, not this run's).
- **CI and env:** `tests.yml` on main was green, run 36199643878 at 23:06:03Z. `.env.local` points at local.
- **FIX status:**
  - Closed: FIX-704 (07-03), FIX-1009 (08-11), FIX-1018 (08-12), FIX-1140 (09-02), FIX-1150 (09-04).
  - Open: FIX-1217, 1221, 1224, 1225 and 1226.
- **Clone freshness:** FR `max(updated_at)` is 2026-09-03 01:34:43, reltuples 10,412,646.

### Read 2 — FIX-1221's first firing and FIX-1224's receipt (prod)
- **FIX-1221:** jobid 54, runid **68317**.
  - The run: `succeeded`, 2026-09-26 01:51:00.025 → 01:51:00.060 (**35 ms**), `return_message DELETE 1682`.
  - Rows older than 42 d: **1,682 of 68,161** at 01:50:17, then **2 of 66,489** at 01:55:52. The 2 crossed the line after 01:51.
  - Oldest row: moved from 06-29 08:00 to 08-15 01:52.
- **FIX-1224:** the 05:00 canary had not fired when this was read (01:55), so there is no receipt and no commit.

### Read 3 — B's population (rule 116)
- **Prod** (01:39:30, 62 ms): FR rows with `to_type='financial_entity' AND relationship_type='donation' AND from_type <> 'financial_entity'` = **0**. Read as two index ranges, `from_type < …` and `from_type > …`, plus `IS NULL`: 0 / 0 / 0.
- **Clone:** 0 / 0 / 0.
- **Other `from_type` values on prod:** `pg_stats` lists exactly two, `financial_entity` and `agency`.

### Read 4 — B's unit shapes (clone, EXPLAIN (ANALYZE, BUFFERS), prod planner GUCs, `work_mem 64MB`)
- **(a) The prompt's keyset** (`… GROUP BY to_id ORDER BY to_id LIMIT 200`) **early-terminates**: `Limit ← Group ← Index Scan using financial_relationships_to`, with a `relationship_type` Filter. But it walks every inbound row of every type for those 200 recipients through the heap:
  - **16,172 ms** for the first 200 from the zero cursor (150,874 reads; 55,732 rows kept, 288,030 filtered);
  - 4,259 ms from a cursor just below the RNC;
  - adding `from_type='financial_entity'` moves it onto `donor_rollup_idx`: 8,337 ms.

  Summed over the bootstrap, that is a walk of the whole FE-inbound heap. **Used instead:** a loose-index-scan keyset (a recursive CTE, one `ORDER BY to_id LIMIT 1` descent per distinct `to_id` on `donor_rollup_idx`, index-only, 0 heap fetches, then a `financial_entities` PK probe):
  - **119 ms** for the first 200 (439 reads);
  - **4,216 ms** for all 15,325 donation `to_id`s.

  It is still a keyset, and still early-terminating.
- **(b) The RNC unit** (125,567 donors, 151,406 rows on the clone), cold: **600 ms**, 1,630 reads.
  - The plan is `GroupAggregate ← Index Only Scan using financial_relationships_donor_rollup_idx`, which is `from_id`-ordered, so there is no hash table.
  - The CTE is materialized in memory (no temp I/O). The top 200 is a top-N heapsort using 48 kB.
- **(c) The watermark dirty read.** It takes `financial_relationships_updated_at` (Index Scan plus Filter) for a one-day window: 52,201 donation→FE rows → 1,640 recipients, 584 ms. For a 1.1 M-row window it becomes `BitmapAnd(updated_at, _to, _type)`: 496,798 rows → 4,898 recipients, 3,957 ms. `refresh_fe_totals_slice()`'s to-side read (`20260903010000:149`) takes the same shape.

### Read 5 — the daily unit's size (prod, 01:44–01:45 UTC)
- **Dirty since the last drop:** **660** recipients with an inbound FE donation row written since 2026-09-20 00:00, **7** of them over 10k rows (24.9 s read; the whale test was a `LIMIT 10001` probe per recipient).
- **FR rows by `updated_at::date`, last 14 d:**

  | date | rows |
  |---|---|
  | 09-12 | 125 |
  | 09-13 | 19,776 |
  | 09-14 | 41,406 |
  | 09-17 | 46,308 |
  | 09-19 | 47,774 |
  | 09-20 | 55,807 |
  | 09-21 | 20,811 |

  Nothing has been written since; FR `max(updated_at)` is 2026-09-21 00:18:25.83.
- **Sizing:** a post-drop day is 4 units of 200, well inside `p_max_units := 10` (2,000 recipients).

### Read 6 — the spending predicate (prod, 01:45:58–01:46:02, 3.7 s, gate(120) ok)
FR rows with `relationship_type IN ('contract','grant') AND to_type <> 'financial_entity'`: **0**.

### Read 7 — co-tenancy (prod `cron.job` ⟕ `cron_job_budget`, 30-d walls, 01:46 UTC)
**`13 2 * * *` confirmed.** It passes every placement rule:
- hour 2 is inside the 18–05 band;
- minute 13 is odd and not a multiple of 15;
- no active job holds that schedule;
- the command is a single statement;
- the budget row lands in the same migration.

**Neighbours:**

| job | schedule | 30-d max wall |
|---|---|---|
| `cron-job-run-details-retention` | 01:51 | — (not yet fired) |
| `dpr-vacuum-analyze` | 02:00 Sun/Wed | 9 s |
| `vm-probe-pre-fr` | 02:55 | 0 s |
| `fr-vacuum-analyze` | 03:00 | 227 s |
| `fe-crawl` | :00 / :30 | 1,579 s |
| `ec-crawl` | :15 multiples | 2,114 s |

The worst-case end (the 1,800 s budget) is 02:43.

### Read 8 — FIX-1226 (b)'s size, for cc-160 (prod)
- **The population** (01:47:27–01:47:38):
  - **12,317** `financial_entities` carry a `fec_committee_id`;
  - **8,377** of them read `total_received_cents = 0`;
  - **5,713** of those have ≥ 1 inbound FE donation row. That is the population `financial_entity_received_totals_rebuild(uuid[])` would change.
- **Their inbound rows:** **3,154,766** in total, summing to 1,470,636,244,400 ¢, with **34** recipients over 10k rows (the same 34 cc-158 counted). Read at 03:14:56 from B's `tx_total` after the bootstrap, a PK join instead of a ~100 s walk of the whale ranges.
- **`total_donated_cents` has an owner too:** `financial_entity_donation_totals_rebuild(uuid[])` (`20260704000000`), which fe-crawl drives from its from-side dirty set and the monthly reconcile also covers. So item 1 excludes **both** `ENTITY_AGGREGATE_COLUMNS`.

### Read 9 — the ISR evidence
- **`withDbTimeout` never throws:** it resolves `{data: null, error}` on timeout (`supabase-check.ts:66-90`).
- **The page caches:** `revalidate = 300` (`page.tsx:21`), and every read folds `.data ?? []` / `.count ?? 0`.
- **Nothing opts a degraded render out.** No `unstable_noStore` or `noStore`, no `dynamic` export, no throw on `error`, and `createPublicClient()` passes no fetch options. The only `no-store` in `supabase-check.ts` is `unavailableResponse()`, an API-route helper.
- **Filed as FIX-1227.**

### Read 10 — drain-and-wait and gate readings (prod)

| reading | 02:10 UTC | 02:40:59 UTC | 02:55:39 UTC |
|---|---|---|---|
| FR `n_dead_tup` / trigger / headroom | 884 / 289,208 / 288,324 | 884 / 289,129 / 288,245 | 884 |
| EC `n_dead_tup` / trigger | 822 / 521,453 | 822 / 521,453 | 822 |
| FE `n_dead_tup` / trigger | 688 / 260,965 | 688 / 260,965 | 688 |
| `mods_total` FR / EC / FE | 349,950 / 6,442,890 / 246,089 | unchanged | unchanged |

- **Rates:** every rate is **0 over 45 min**. FR also had 884 dead at cc-158's 23:38:51 reading, so it has not moved in 3 h 17 min. Headroom ÷ rate is unbounded.
- **(a) Unguarded vacuums:**
  - Last ends: `fr-vacuum-analyze` 09-25 03:00:10 (10.3 s), `ec-` 04:30:09 (9.3 s), `fe-` 04:50:11 (11.6 s), the 17:xx series by 17:20:07 (≤ 7.2 s).
  - Tonight's `fr-vacuum-analyze`: **03:00:00 → 03:00:09.93, 9.9 s**. That is in the 10-min class, not the 90-min one (see item 9).
- **(c) Crawl arms:** `ec-crawl` units 01:15–02:00 all succeeded, max 25.5 s (01:45); `fe-crawl` 0.1 s. None running.
- **(d) Watchdogs:** 30/30 runs each in 60 min, max 0.038 / 0.010 s. No startup timeouts, no budget overruns.
- **(e) Interlock:** `held=false`, no live writers.
- **(f) Census baseline**, `data:census:cancellations:prod` at **02:10:22** (window 01:10:22–02:10:22):
  - **0** 57014s, rate 0/min against the 0.033/min baseline, P99 floor 6: **PASS**;
  - front door: last closed bucket 01:45, **0 of 779** = 0.00 %: **PASS**.
- **Gate:** `prod_op_gate(1200)` ok at 02:10:39, span to 02:50:39; `prod_op_gate(120)` ok; nightly 1,129 min away.

## 2. D1 — the spending index (clone, cold = container stop, VM page cache dropped, start)

| candidate | CIC wall cold | size | RNC spending as anon (cold) | AmerisourceBergen as anon (cold) |
|---|---|---|---|---|
| none (baseline) | — | — | BitmapAnd(`_to`, `contract_grant_updated_at`) + Sort, **5,953 ms** | BitmapAnd + Sort, **8,255 ms** |
| partial-A `(to_type, to_id, amount_cents DESC) WHERE rt IN (contract, grant)` | 57.5 s | 178,782,208 B | `Limit ← Index Scan`, no Sort, 0.215 ms | `Limit ← Index Scan`, 32.6 ms (58 buffers) |
| **narrower `(to_id, amount_cents DESC) WHERE rt IN (contract, grant) AND to_type = 'financial_entity'`** | **12.1 s** | **111,951,872 B** | `Limit ← Index Scan`, no Sort, **0.145 ms** | `Limit ← Index Scan`, **20.6 ms** (57 buffers; 0.358 ms warm) |

- **Winner: the narrower index.** It is 37 % smaller, the plan flips for both entities, and read 6 = 0 means it is complete.
- **Build settings:** `max_parallel_maintenance_workers 1`, `maintenance_work_mem 64MB`, both builds `CONCURRENTLY` through `db-query --local --call`. The first attempt failed with `CREATE INDEX CONCURRENTLY cannot run inside a transaction block`, because plain `db-query --local` wraps the file in `--single-transaction`. The two baseline plans above come from that aborted attempt.
- **Clean-up:** both scratches were dropped `CONCURRENTLY`. The FR index inventory is 18 names, md5 `eb6e8b5c…`, before and after, with 0 `fix1217_%` relations.

## 3. The three landings

### Landing (i) — the CIC (from the primary at `7240c1a7`)
- **Command:** `session:wait-for-gate:prod --expected-minutes 20 --max-wait-minutes 900 --census stop --reason "cc-159 FIX-1217 spending index CIC" --then "node ../../scripts/fix1217-build-fr-spending-index.mjs --env-file ../../.env.local.prod"`.
- **Polls:** **1**. 02:11:06Z `ok=true`, census pass (0/60 min, ratio 0.00, floor 6; edge 0 of 779 = 0.00 %), span to 02:51Z. Claimed, holding 16 guarded pipelines.
- **Build:** start **02:11:11**, end **02:12:44** UTC, **93.8 s** (1.56 min). Before: absent. After: `indisvalid=true indisready=true`, **114,360,320 B**, ×1.021 the clone's size.
- **Against the projection:** 6.4 min (clone cold × 1.389) and ≤ 19 min (bracket). The narrower index's own clone-cold projection was 12.1 s × 1.389 = 16.8 s, so prod ran 5.6× the clone-cold projection. There were no old transactions for CONCURRENTLY to wait on (pre-read: 0 rows).
- **Prod EXPLAIN as anon** (02:13:37, PostgREST CTE shape): RNC and AmerisourceBergen are both `Aggregate ← Limit ← Index Scan using financial_relationships_fe_spending_idx`, `Index Cond: (to_id = …)`, no Sort.
- **Census:** before 02:10:22 (0; edge 0/779); after 02:16:07, window 01:16–02:16 (0; edge 02:00 0/595). **Renders lost: 0.** Rule 192 projected 93.8 / 240 = 0.4.

### Landing (ii) — the migration
- **Before:** item 4 was pushed to main at 02:23:03 (`held=false` 02:22:45). The read-only dry-run listed exactly one pending migration, `20260926000000`, above the remote head `20260920160000`.
- **Launch:** held until 02:51:30, the end of the CIC's 2 × 20 min span, then `session:wait-for-gate:prod --expected-minutes 2 --census stop --reason "cc-159 FIX-1217 migration" --then "node ../../scripts/db-push-prod.mjs --workdir ../../../civitics-worktrees/fix-1217"`.
- **Polls:** **1**. 02:51:33Z ok, census pass (0/60; edge 0 of 424), span to 02:55Z. Claim held 16.
- **Applied** at ~02:51:35. `financial_relationships_fe_spending_idx already exists, skipping`; `scheduled fe-inbound-rollup-refresh (jobid 55)`; the placement guard passed.
- **Verified at 02:51:52:**
  - job 55 `13 2 * * *`, active, `CALL public.run_fe_inbound_rollup(p_max_units := 10)`;
  - budget 1,800; first-seen 02:51:37; cursor row present;
  - EXECUTE on the procedure: anon `f`, authenticated `f`, service_role `t`; EXECUTE on the unit: anon `f`; anon SELECT on both tables `t`;
  - `schema_migrations` head `20260926000000`.

### Drain-and-wait before (iii) (rule 136, read 02:55:39)
- **Watchdogs:** two distinct runs each since landing (ii) ended — `cron-job-budget-watchdog` 68445 at 02:52:00 (0.033 s) and 68449 at 02:54:00 (0.020 s); `derived-mvs-unit-watchdog` 68447 (0.009 s) and 68451 (0.004 s).
- **Cron and counters:** nothing running, 0 startup timeouts, autovacuum counters unchanged, `held=false`.
- **Gate:** `prod_op_gate(120)` ok. `prod_op_gate(1200)` was blocked **only** by `fr-vacuum-analyze (mean 87.6 s) is scheduled 03:00 UTC, inside the span` — a clock condition the wait loop resolves.

### Landing (iii) — the bootstrap
- **Command:** `… --expected-minutes 20 --max-wait-minutes 900 --census stop --reason "cc-159 FIX-1217 fe inbound rollup bootstrap" --then "node ../../scripts/db-query.mjs --prod --call --yes-i-mean-prod --file <…>/bootstrap_call.sql"`. The file is `CALL public.run_fe_inbound_rollup(p_max_units := 1000);`. `db-query --call` sent `SET statement_timeout = '60min'` and `SET civitics.prod_session_claimant = …` as their own statements ahead of the CALL, so the 2-min interactive-pooler timeout did not govern it (rule 109).
- **Polls: 4, 3 held.**

  | poll (UTC) | result | held by |
  |---|---|---|
  | 02:55:55 | held | `b:fr-vacuum-analyze(→04:31)`, the slot inside the span |
  | 03:02:25 | held | `b:fr-vacuum-analyze(→03:10)`, +10 min after its 9.9 s run |
  | 03:07:25 | held | the same |
  | **03:12:25** | **ok** | — census pass (0/60; edge 0 of 165), span to 03:52Z |

  The 03:02:25 poll is paced: 03:00:55 fell inside :00 +0–1 and moved 90 s. That is D2's rule-190 pacing, live.
- **Claim:** held **17** guarded pipelines (B included).
- **CALL:** **03:12:34.30 → 03:13:36.69, 62.4 s**, `complete`, `caught_up`:
  - `units_run 49`, `recipients 9,656`, `rows_written 571,155` (561,499 + 9,656);
  - `max_recipient_ms 3,202.9`;
  - target and watermark 02:12:34.316.

  The source recorded is `Supavisor`, the pooler's `application_name`. **The interlock exempted the claimant's CALL** (rule 169): it ran rather than skipped.
- **Against the floor and bracket (2.5 / 15 min):** it beat the floor because the floor was the 101.6 s GROUP BY enumeration, which the loose keyset replaced.
- **Size:** **561,499 rows**, inside the 0.50–0.75 M projection; 70,279,168 B with indexes, plus the totals at 1,015,808 B.
- **Census:** after 03:17:10 (window 02:17–03:17: 0; front door 02:15–03:00 buckets 541 / 424 / 165 / 434, 0 5xx), and 15 min at 03:26:32 (03:11:34–03:26:34: 0). **Renders lost: 0.** Rule 192 projected 0.26.

## 4. D7 and the page on prod

**D7 (prod, 03:14:26–03:14:46, `prod_op_gate(60)` ok).** B against a live aggregate over `donor_rollup_idx` (cc-158 option C). Read 3 makes it the page's population.

| | missing | extra | sum or rank diff | top-50 Σ truth / B (¢) | donors truth / B | tx truth / B | sum truth / B (¢) |
|---|---|---|---|---|---|---|---|
| RNC | **0** | **0** | 0 | 1,558,669,200 / 1,558,669,200 | 283,715 / 283,715 | 337,058 / 337,058 | 36,469,039,900 / = |
| DNC | **0** | **0** | 0 | 1,223,918,100 / = | 204,872 / = | 259,440 / = | 33,415,765,200 / = |
| DSCC | **0** | **0** | 0 | 1,379,859,800 / = | 157,889 / = | 199,241 / = | 32,617,811,800 / = |

The RNC's 283,715 / 337,058 and the DSCC's 157,889 / 199,241 equal cc-158 §4.3's timed reads. The same check on the clone used an independent **heap-path** truth on the page's predicate (no `from_type`) and was 0 / 0 for all three.

**The page** — item 6 pushed at 03:17:36 (`held=false` 03:17:20); `tests.yml` 36214354212 green; Vercel success before 03:21:30. GETs at 03:22:21 of `https://civitics-civitics.vercel.app/donors/<id>`:

| page | HTTP | Donors | Total received | Top donors | Spending |
|---|---|---|---|---|---|
| RNC `421c9ec5…` | 200 (HIT, 425 ms) | **283,715 (337,058 transactions)** | **$364.7M** (sum_total; the FE column is still $0) | 50 rows, "Showing top 50 of 283,715 unique" | — |
| TeamHealth PAC `0e988eec…` (ordinary, 126 inbound) | 200 (MISS, 1.9 s) | 126 (126 transactions) | $431K | 50 rows, "top 50 of 126" | — |
| AmerisourceBergen `01ffa686…` | 200 (MISS, 1.2 s) | 0 | $0 | none | 50 rows ($4,123.2M federal spending) |

The meta description matches, e.g. the RNC's `Party Committee · $0 donated · $364.7M received`. **Function log: not read** (see stopped_items). The census over the renders read 0.

## 5. What contradicted cc-158 or this prompt

1. **The keyset enumeration early-terminated, but not cheaply.** The prompt's `GROUP BY to_id … LIMIT 200` takes `_to` with a `relationship_type` Filter, so each batch reads every inbound row of every type for its 200 recipients through the heap: 16.2 s cold for the first batch on the clone. Summed, it is the whole FE-inbound heap. Not a STOP, because the plan is `Limit ← Group ← Index Scan`. The build used a loose-index-scan keyset over `donor_rollup_idx` instead (119 ms / 200; 4.2 s for every recipient). That is why the prod bootstrap took 62 s against a 2.5 min floor that assumed read 4's 101.6 s scan.
2. **`work_mem 64MB` was never the constraint.** The per-recipient aggregate is a GroupAggregate over `from_id`-ordered index-only tuples, with no hash. The RNC's CTE tuplestore stayed in memory, and the top 200 is a 48 kB heapsort.
3. **The planner took the narrower spending index**, as anon, on the clone and on prod. On the clone its CIC cost a fifth of partial-A's: 12.1 s against 57.5 s. cc-158's 13.75 s for partial-A was not a CONCURRENTLY build.
4. **Builds are far below every projection.** The CIC took 93.8 s on prod against 6.4 min projected and ≤ 19 min bracketed. The ×20 prod/clone-warm bracket and the 9 MB/s heap-rate bound were both pessimistic by an order of magnitude tonight. The clone-cold × 1.389 figure was optimistic by 5.6×.
5. **The daily dirty set after a drop is not whale-heavy by count:** 660 recipients, 7 over 10k. The whales dominate its wall, though: the prod bootstrap's slowest recipient took 3.2 s.
6. **The interlock exempted the claimant** (rule 169). The bootstrap CALL ran under the claim's GUC, `complete` rather than `skipped`. The claim's held set grew 16 → 17 when the migration added B.
7. **The census during the CIC** (for the design note, candidates 182–184) read 0 57014s and 0 5xx over the window containing it. On a quiet evening a ~94 s CIC on FR cost nothing measurable, below rule 192's projected 0.4 renders. CIC cost is apparently bursty, not proportional; one reading is not a model.
8. **B cannot see a deleted FR row.** This is the FIX-372/704 hard-delete limitation the FE totals family shares. A remediation that deletes donation rows must call `refresh_fe_inbound_rollup_unit(<recipients>)` in its tail. It is written into the migration header and `docs/ops/prod-holds.md`, not filed ("nothing else is filed from this prompt").
9. **D4 grew one mechanism: a watermark window that resumes.** In the prompt's design a capped (`partial`) watermark run restarts its whole window next time, so a dirty set larger than 2,000 recipients would never finish. The run now records `resume_target` / `resume_after` in the watermark row, inside each unit's transaction, and the next firing finishes that window. On the clone: 250 scratch rows, `p_max_units 1` → `partial` at 200 with the watermark unmoved → the next CALL finished the 50 and advanced to that window's target.
10. **FIX-1226's column is still 0 on prod.** The RNC's `total_received_cents` still reads 0 until cc-160; the page no longer shows it because `sum_total` wins. `sum_total` and the FE received total are the same definition (cc-158 §3; clone RNC 21,031,139,400 on both).
11. **D7 on prod shares B's access path.** The truth there is a live computation over the same `donor_rollup_idx`: independent in time, not in path. The clone check used the heap path.
12. **My own slips:**
    - One census read ran at **02:16:05**, inside :15 +0–1. That is a rule-190 miss: I read the clock and did not wait.
    - The held value for the **01:56:39** push was cut off my screen by a `tail`. A re-read 5 s after the push said `false`.
    - The FIX-700 DB test I ran alongside FIX-1226's runs a full `rebuild_financial_entity_donation_totals()` on the clone (135 s). It is an idempotent recompute, so clone totals may have moved to their FR-derived values.
13. **Template gate (g)'s `fr-vacuum-analyze` figure** is now a band (item 9). Tonight's run was 9.9 s, in the 10-min class, and the gate's spacing rule used that last wall (`→03:10`). Its "scheduled inside the span" rule still classes the job by the 14-d mean (87.6 s > 60 s), so a 20-min op cannot straddle 03:00 even on a 10 s night. That is correct, since it cannot know the wall in advance.

## 6. Appends, filings, docs

- **FIX-1226 append:** the writer half (`200a98c2`, 01:55:47), read 8's numbers, and the recommended vehicle for cc-160.
- **FIX-1194 append:** `run_fe_inbound_rollup` is the **21st** routine that consults `prod_session_state()`. Two counts moved together, so the gap is unchanged: `prod_op_gate`'s `guarded_pipelines` 17 → **18**, and `claimProdSession`'s held set 16 → **17**.
- **FIX-1178:** no append; read 5 does not touch its premise.
- **Filed FIX-1227** (🟡 S, INFRASTRUCTURE & PERFORMANCE): a `revalidate = 300` page caches a render whose reads degraded to `{data: null}`.
- **Filed FIX-1228** (🟢 S, GENERAL / CROSS-CUTTING). Prod has **57** donors over 1,000 outbound donation rows, max **2,074**, 0 over 10k. Read 03:58:29–03:59:07 UTC with gate(120) ok and `platform-counts-daily` finished at 03:53:27, index-only on `donation_size_rollup`, ~38 s. So the from-side Recipients stat and top-50 have the sample shape FIX-1225 fixed inbound. It is not a whale class.
- **`PROMPT_TEMPLATE.md` gate (g):** `fr-vacuum-analyze` is a band ("~10 s between drops, 161–212 s after a weekend drop … read the LAST wall").
- **`docs/ops/prod-holds.md`:** B's row in the guarded-set table (jobid 55) and one paragraph on its owner, its two `pipeline_state` keys, and how a cancelled or deferred bootstrap resumes from the cursor.

## 7. Local and clone state as left

- **Migration:** `20260926000000` is applied locally. The job is local jobid 131 and **parked** (`active=false`; the clone is not a live scheduler).
- **Local rollup:** 441,378 rows / 8,483 totals, with the watermark at the last local CALL.
- **Removed:** the scratch FR rows (0 left), and their 250 recipients restored and re-checked.
- **Unchanged:** the FR index inventory, apart from the migration's own index.

## 8. Push times (UTC), each after a `held=false` read

| push | read at | commit(s) |
|---|---|---|
| 01:55:47 | 01:55:30 | `200a98c2` |
| 01:56:39 | — (re-read 01:56:44) | `e321504d` |
| 01:57:11 | 01:56:57 | `a3a62cf6` |
| 02:09:47 | 02:09:31 | `7240c1a7` |
| 02:23:03 | 02:22:45 | `66cd1394` |
| 03:17:36 | 03:17:20 | `8a4a326d` |
| 03:27:05 | 03:26:50 | `cd3c1437`, `1c7307ef` |

| 04:00:02 | 03:59:46 | `ee961c98`, `581bc3cb` |

The report commit is pushed after `pnpm cc:verify 159`.
