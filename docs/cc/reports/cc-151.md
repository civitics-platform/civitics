---
cc: 151
prompt: cc-prompt-151-fix1212-census-floor-p99-census-inside-gate-wait-fr-front-door-read-filing-launch-tonight-long-wait.md
started_at: 2026-09-24T00:40:10Z
finished_at: 2026-09-24T09:39:18Z
head_before: 82841454
head_after: f6014452
commits:
  - {sha: a769d630, subject: "fix(data): FIX-1212 — the census verdict floors the ratio at the Poisson 99th percentile of the stated baseline"}
  - {sha: 2fc50727, subject: "fix(data): FIX-1212 — the runner waits on the census inside the gate wait; census_fail retired"}
  - {sha: 87cdd6cc, subject: "docs(fixes): file FIX-1217 (the donor profile page's inbound financial_relationships fan-out, from the evidence)"}
  - {sha: aed3a09c, subject: "docs(receipts): FIX-1212 prod bootstrap runner, 2026-09-24 — stopped before CALL 6 on the front door; windows 3-12 banked"}
  - {sha: f6014452, subject: "docs(fixes): FIX-1212 / 1125 / 1194 appends (cc-151: floored census, census inside the wait; the prod run banked windows 3-12 and stopped on the front door)"}
fixes_closed: []
fixes_filed: [FIX-1217]
fixes_reopened: []
migrations_pushed: []
prod_writes: "The FIX-1212 bootstrap runner, launched once from the primary checkout at 01:04:47 UTC (node pid 17080; the pnpm wrapper at 01:04:44). It claimed the FIX-950 session 09:04:54-09:34:12 (label written and deleted; 16 guarded holds set and cleared). It upserted pipeline_state.donor_party_crawl = {max_units: 2} before each of 5 CALLs and DELETED it after each (absent before and after). Five CALLs of public.refresh_donor_party_rollup_incremental() ran 09:04:59-09:32:36 and wrote data_sync_log 22d56234, 4a554538, 626e8d1b, b516361d and 2bb9f584. They replaced donor_party_rollup_mv windows 3-12 (1,728,538 -> 2,618,055 rows) and advanced the procedure-owned cursor pipeline_state.donor_party_full_rebuild from windows_done [1,2] to [1..12]; the watermark was not moved. Pushes to main at 01:01:40 (87cdd6cc) and 09:37 UTC (f6014452) deploy to Vercel; no app-route code changed. Every other prod contact was a read: db-query.mjs --prod, the Logs API and edge_logs. No cron.alter_job, no VACUUM, no --force, no second runner, no hand CALL."
ci: green
stopped_items:
  - "Item 5, the prod run, did not reach caught_up. It stopped before CALL 6 on stop rule (3): the pre-CALL census at 09:34:11 read 8 x 57014 in 15 min (ratio 16.16, over the P99 floor of 3) and edge 2 of 183 = 1.09 %. That is exit 4 (stopped). Windows 3-12 are banked; windows 13-16 remain. Per the runner's contract and the prompt (never a second runner, never a hand CALL) there was no retry."
  - "Item 6 (the Pattern B record) did not apply: there was no caught_up. FIX-1212 stays open with no trailer, and the receipt was committed as docs(receipts) (aed3a09c)."
  - "Item 8's chore(fixes) sync was not run: no Fixes or Closes trailer landed, so fixes:sync has nothing to append."
  - "Item 7 wrote appends for FIX-1212, FIX-1125 and FIX-1194 only. FIX-1216: the run kept the cursor's started_at, exactly as its bullet says. FIX-969: jobid 17's condition (a caught_up) was not met."
  - "Read 3's premise did not hold: receipts-daily.ts has no daily census line (it only points to the script from NOT_CAPTURABLE at :651), so nothing there shows the new verdict. It was not added; that is out of D1's scope."
  - "D4 could not exercise the census-held path on the clone (no Logs API; the half reads skipped — local), and on prod the census held 0 of 97 polls. Only the unit tests (prod-op-gate 12, runner 23) exercise the path where the census holds the window."
---

# cc-151: the census is floored and waited on in code; the prod run banked windows 3–12, then stopped on its own front-door cost

**FIX-1212 is still open, but 12 of 16 windows are now done.** Five paced
CALLs ran 09:04:59–09:32:36 UTC and banked windows 3–12:
- each window took 117–145 s;
- the watchdogs never went above 1.24 s;
- there were 0 startup timeouts.

**Why it stopped.** Before CALL 6, the 15-min census read 8 × 57014 and a
1.09 % edge 5xx rate, so the runner stopped.
- **This time the 57014s were the op's own cost.** All nine front-door
  57014s in the run fell inside CALLs, and none fell in the 60 min before
  the claim or in the breathers.
- The reads that timed out were official-page reads. The largest burst was
  6 in one second at 09:30:26, and the watchdog walls read 0.19 s at that
  moment.
- None of them was the donor-page read that dominated the 24-h baseline.

**What remains:** 4 windows, about two CALLs.
- **If nobody relaunches,** Tue 09-29 15:00 resumes the cursor for those 4
  windows unsupervised, about 8–9 min of stage load. The cursor will be
  6 d 6 h old, under the 7-day limit.
- **A relaunch** will meet the same front-door cost. **Your decision:** is
  8 official-page timeouts in 15 min a stop, or the price of this op?

**What was built:**
- **D1:** a census reading now fails only when the ratio is over 2 AND the
  count is over the Poisson 99th-percentile floor. In the field, that
  floor let CALLs 2–5 proceed on 1–2 stray 57014s, each of which trips
  the old rule.
- **D2:** the runner now waits on the census inside the gate wait, and
  `census_fail` is retired.
- **D3:** filed as FIX-1217, the donor profile page's three inbound reads
  for the largest party committees. **The prompt's condition was one query
  TEXT at ≥ 50 %.** The largest is 25.5 %; the call site is 72.3 %. I filed
  on the call site (§5).

## 1. Reads 2–6

**Read 1 (00:40 UTC).**
- `git pull` found nothing new; head 82841454.
- `session:check`: 56 open, as expected (cc-148 filed FIX-1216 and closed nothing). The stranded-branch and open-PR warnings are the known fix-1178 / fix-1213 worktrees.
- `tests.yml` has seven checks: Typecheck · Lint · Silent-read guard (`check:reads`) · Render-timeout guard · Workflow resource-parity guard · Inert statement_timeout guard (`check:proconfig`) · the three unit suites (data, app, db).
- `.env.local` points at local (127.0.0.1:54321) in the primary checkout and the worktree.

**Read 2 — FIX-1212 on Pro at 00:41:01 UTC: every expectation held.**
- `donor_party_full_rebuild` = `{mode full, target 2026-09-21T00:18:25.831727, started_at 2026-09-23T09:02:48.434717, windows_done [1,2]}`.
- `donor_party_crawl` absent.
- Watermark 2026-07-28 05:54:28.48494.
- MV 1,728,538 rows / SUM 732,520,894,800.
- `prod_session_state()`: held=false, reason_text clear, no label, live_writers [].
- 0 `civitics_dp_bootstrap` backends.
- Re-read at 01:04:22 before launch: identical.

**Read 3 — `verdictFor` and its callers, by grep.**
- The tree has it at `cancellation-census.ts:58-84` (the prompt said :53-82).
- It has exactly ONE call site: `scripts/cancellation-census.ts:321`. Every consumer reads that script's exit code.
- The script's consumers:
  - `data:census:cancellations:prod` (gate (f), read by hand);
  - the runner's `runCensus`, called pre-launch at 60 min (`:1150`), pre-CALL at 15 min (`:980`) and on the 15-min cadence (`:945`).
- **`receipts-daily.ts` does NOT call it.** Its NOT_CAPTURABLE entry at `:651` is a prose pointer to the script. So there is no "daily census line" to show the new verdict, and the prompt's premise that one exists does not hold. No receipt change was made.
- `receipts-format.ts` has an unrelated function also called `verdictFor`, the cron-band verdict.
- What changed for each caller:
  - a 60-min reading now tolerates up to 6 (it was 3);
  - a 15-min reading tolerates up to 3 (it was 0);
  - the edge (5xx) half is unchanged.
- That is wanted for the runner and for gate (f).

**Read 4 — the runner's gate wait before this run.**
- `run()` at `:1123-1159` called `waitForProdOpGate({pollSeconds 300, maxWaitSeconds = the remainder of --max-wait-minutes})`.
- That function polls immediately, then every `pollSeconds`, and fails closed on a read error. Each poll records `{at, ok, blocked[, error]}` and logs one `[gate]` line.
- The one-shot census came after it (`:1148-1158`): `runCensus(60)`, then exit 3 `census_fail` on any non-zero exit, with no retry.
- The vocabulary was `:128-133` (seven outcomes, including census_fail 3). The receipt printed the census rows at `:725-728` and the outcome in the table.
- `--max-wait-minutes` is declared at `:191` with min 1 and **no max**, so 600 is accepted (now asserted by a test).

**Read 5 — the 57014 attribution (Logs API, 24 h to 00:42 UTC, walked in 6-h slices).**
The census's `--by query` timed out twice at its 20 s fetch timeout over 1440 min. `--by application_name` returned postgrest 47 / 47.

| group | n | share |
|---|---|---|
| financial_relationships | 34 | 72.3 % |
| enrichment_queue | 10 | 21.3 % |
| check_senate_reference_cohort | 2 | 4.3 % |
| list_scheduled_rollup_pipelines | 1 | 2.1 % |

The 34 `financial_relationships` events are **three query texts**, not one. Quoted verbatim from the Logs API, with the `"public"."financial_relationships".` column prefix dropped for length:
- **11 (23.4 %)**: `WITH pgrst_source AS ( SELECT "id", "from_type", "from_id", "to_type", "to_id", "amount_cents", "occurred_at", "cycle_year", "relationship_type", "metadata" FROM "public"."financial_relationships" WHERE "to_type" = $1 AND "to_id" = $2 AND "relationship_type" = $3 ORDER BY "amount_cents" DESC LIMIT $4 OFFSET $5 ) SELECT null::bigint AS total_result_set, …` This is `donors:inbound`, `donors/[id]/page.tsx:232-242` (limit 1000).
- **11 (23.4 %)**: the same with `"relationship_type" = ANY ($3)`. This is `donors:spending`, `:243-253` (contract/grant, limit 50).
- **12 (25.5 %)**: `WITH pgrst_source AS ( SELECT "from_id" FROM … WHERE "to_type" = $1 AND "to_id" = $2 AND "relationship_type" = $3 LIMIT $4 OFFSET $5 ) , pgrst_source_count AS (SELECT 1 FROM … same filter) SELECT (SELECT pg_catalog.count(*) FROM pgrst_source_count) …, ''::text AS body …`. This is `donors:donor-count`, `:273-281` (`count: "exact", head: true`).

All three come from one `Promise.all` in `apps/civitics/app/donors/[id]/page.tsx:219-297`:
- The page is a server component, with `createPublicClient()` at `:208`. That is the anon role, `statement_timeout=3s` per `pg_roles` on prod; authenticated is 8s and authenticator 8s.
- Each read also sits inside `withDbTimeout(…, 3000)`.

edge_logs name the requests: GET/HEAD `/rest/v1/financial_relationships?…&to_type=eq.financial_entity&to_id=eq.<id>…`, user-agent `node`, `supabase-js-node/2.99.1`. The entities, by burst:

| burst (UTC) | entity |
|---|---|
| 07:08 | DNC Services Corp / Democratic National Committee `34a4772a`, and RNC `421c9ec5` |
| 13:00 | Republican Party of Texas `d598c07a` |
| 23:41 | DSCC `7d632d3f` |
| 00:22 | RNC `421c9ec5` again |

The 00:43 (09-23) burst fell outside the edge window I read. Every burst is one page rendered twice about 3 s apart, with all three reads failing each time.

- Clone plan (an estimate, not a prod measurement): a bitmap scan on `financial_relationships_to (to_type, to_id)` of about 94,576 rows for the RNC, then a filter and a sort. No FR index covers `(to_type, to_id, relationship_type, amount_cents)`.
- **D3's condition as written — ONE query text ≥ 50 % — does not hold (max 25.5 %). The call site is 72.3 %.** I filed on the call site; see §5.

**Read 6 — the instruments at 00:44:46 UTC.**
- Watchdogs, last 30 completed walls:
  - cron-job-budget-watchdog 0.015 / 0.017 / 0.033 s (min / median / max);
  - derived-mvs-unit-watchdog 0.001 / 0.004 / 0.009 s.
- `job startup timeout`, last 6 h: 0.
- `prod_op_gate(3600)` read **ok=true, blocked_by []**, span to 02:44:47. The prompt expected it blocked. It was open because a 2-h span from 00:44 ends before `fr-vacuum-analyze` at 03:00. It closed as the span crossed 03:00: at 01:04:22 it read `b:fr-vacuum-analyze`, retry 04:31:58. The window was unusable, because items 1–2 were not built yet.
- The "before" census (60 min, 23:41:43–00:41:43): 6 × 57014, ratio 3.03, and the edge's last closed bucket 00:15–00:30 at 6 of 583 = **1.03 %**, so exit 1.

| rule | 57014 half | edge half | overall |
|---|---|---|---|
| old | FAIL (ratio 3.03 > 2) | FAIL | FAIL |
| new | PASS (6 ≤ P99 floor 6 at λ = 1.98) | FAIL | FAIL |

## 2. D1 / D2: the changed lines, the tests, the rehearsal

**D1** (`lib/cancellation-census.ts`; a769d630).
- `poissonP99(λ)` is exported beside `verdictFor`. It returns the smallest k with CDF ≥ 0.99, summed in log space so a large λ does not underflow.
- `verdictFor` fails iff `ratio > 2 && total > floor`, where `lambda = baseline × minutes` and `floor = poissonP99(lambda)`. Both are in the verdict and the JSON; the JSON `gate` restates the rule.
- The note names both, e.g. `6 over 60 min — ratio 3.03 > 2 but <= P99 floor 6 at λ=2.0`.
- Zero-baseline behaviour is unchanged.
- The text output prints λ and the floor, and the note.
- Pinned values:
  - `poissonP99(2.0) = 6`, `poissonP99(0.5) = 3`, `poissonP99(0) = 0`;
  - and at the λ actually used: `poissonP99(1.98) = 6`, `poissonP99(0.495) = 3`.

Fixtures at baseline 0.033:

| reading | old | new |
|---|---|---|
| {6, 60 min} (cc-148 23:55) | FAIL | **PASS** |
| {12, 60} (cc-148 00:23) | FAIL | FAIL |
| {6, 15} (cc-148 00:23) | FAIL | FAIL |
| {4, 60} | FAIL | **PASS** |
| {7, 60} | FAIL | FAIL |
| {3, 15} | FAIL | **PASS** |
| {4, 15} | FAIL | FAIL |
| {40, 60} | FAIL | FAIL |
| {2, 60} | PASS | PASS |
| {0, 15} | PASS | PASS |

- The old test "just past 2x fails" (5 at λ = 2.0) now passes under the floor. It was rewritten in two parts: 121 at λ = 60 fails, which is where the ratio binds, and the small-count form is asserted as a pass.
- Census suite 22 → 28.

**D2** (`lib/prod-op-gate.ts`, the runner; 2fc50727).
- `waitForProdOpGate` takes an optional `andAlso: {name, read}`. It is read only on polls where the gate reads ok, and the window opens when both halves pass on the same poll.
- A half that throws counts as blocked; fail closed.
- Poll rows gain `gate_ok` and `also`, and each poll writes one log line with both halves: `[gate] HH:MM:SSZ ok=… blocked_by=[…] census=pass|FAIL|dark (N/60 min, ratio r, floor f; edge …)`.
- `GateTimeout.lastBlockedBy` names the half that held the last poll.
- Without `andAlso` the rows and lines are FIX-1215's, byte for byte (asserted), so `session:wait-for-gate` is unchanged.

In the runner:
- The one-shot census is gone. `censusHalf()` runs `runCensus(60)` on a gate-ok poll; exit 2 means `dark`, which means not open. On a local target it returns `skipped — local (no Logs API)`.
- **census_fail is retired**: removed from `Outcome`, `EXIT` and the header, and exit 3 is never reused.
- The final vocabulary is caught_up 0 · error 1 · stopped 4 · skipped 5 · max_calls 6 · gate_timeout 7. Every receipt carries it as a generated `vocabulary` row.
- The receipt's gate row tallies the polls held by the gate, by the census, and dark, with the census at opening and, on a timeout, the last blocking half.
- Each census row is labelled gate poll / before CALL n / cadence.
- The pre-CALL and cadence censuses keep cc-148's 15-min shape and now print the floor.

**Tests.**
- Runner 19 → 23; prod-op-gate 7 → 12.
- `@civitics/data` 914 → 920 (item 1) → 929 (item 2): 922 pass, 7 skipped, 0 fail.
- Mid-rehearsal, 5 `prod-session` DB tests failed because the rehearsal held the local claim ("already held by … FIX-1212 bootstrap"). They passed on the re-run after the release.
- Turbo typecheck and lint: 19/19.
- The four checks: reads 158/0, render-timeouts 161/0, workflow-parity 0 undeclared, proconfig 20 / 0.
- Script suites all pass: fixes, fix-add, cc-verify, session-worktree, drain, check-proconfig 18/0, db-query 12/0.
- `fixes:check` is clean.

**D4 rehearsal — clone, resume path** (receipt `docs/audits/2026-09-24-fix1212-bootstrap-runner-local.md`).

Setup:
- Watermark set to 2026-06-21 00:13:37.208343.
- Cursor seeded as `{full, target 2026-09-03T01:34:43.542642 (LEAST(max FR.updated_at, horizon)), started_at 00:53:28, [1,2]}`.
- No scratch nightly row was needed: the clone's next nightly read 22:35 on 09-24. cc-148's "22:35 to 01:35" is really "22:35 to 00:00 UTC".
- The watchdogs had 30 runs in 60 min.
- I passed `--expected-minutes 20`, because the clone's fr-vacuum-analyze is active at 03:00 and a 2-h span from 01:0x would reach it.

The run:
- Command: `--units-per-call 2 --breather-max-s 20 --poll-seconds 5 --max-wait-minutes 45 --expected-minutes 20`.
- The gate opened on poll 1 at 00:53:38 with `census=skipped — local (no Logs API)`.
- It resumed at window 3.

| CALL | wall s | windows | stage s |
|---|---|---|---|
| 1 | 90.2 | 3, 4 | 64.75, 23.39 (cold) |
| 2 | 36.6 | 5, 6 | 12.55, 21.61 |
| 3 | 55.8 | 7, 8 | 32.39, 18.84 |
| 4 | 12.4 | 9, 10 | 5.24, 4.59 |
| 5 | 11.8 | 11, 12 | 5.78, 4.17 |
| 6 | 23.1 | 13, 14 | 4.28, 14.68 |
| 7 | 13.6 | 15, 16 | 5.54, 4.49 |

- Six breathers ran; the one before CALL 5 released on walls, and the other five were `breather_timeout` at 20 s (cc-148 §5.6 explains why).
- `caught_up`: cursor gone, watermark 2026-09-03 01:34:43.542642 = target.
- The crawl row was upserted 7 times and restored 7 times, and is absent.
- MV SUM ratio 1.000000. Session clear, 0 backends, exit 0.
- The receipt's vocabulary row and gate tally are the D2 shape.
- The census-held path cannot run on the clone (no Logs API). Only the unit tests exercise it.

**Clone restored:** watermark 2026-08-20 00:13:37.208343, no cursor, no crawl row, no scratch nightly row, session clear, 0 backends.

**Items 1–3 landed on `origin/main` at 01:01:40 UTC** (a769d630, 2fc50727, 87cdd6cc). `tests.yml` run 35941087663 was green on 87cdd6cc, with all seven checks passing as their own steps.

## 3. Item 5 — the prod run

**Launch gates at 01:04:22 UTC — all true:**
- (i) items 1–2 on `origin/main`, CI green (all seven checks);
- (ii) D4 passed;
- (iii) cursor `[1,2]`, crawl row absent;
- (iv) session clear, live_writers [], 0 bootstrap backends;
- (v) `--max-wait-minutes 600` accepted.

**Launch.**
- 01:04:44 UTC from the primary checkout at 87cdd6cc.
- The pnpm wrapper started at 01:04:44 and node pid 17080 logged its first line at 01:04:47.
- The command ran with no `--`.
- Log at `scratchpad/cc151-runner.log`, outside the repo. Receipt at `docs/audits/2026-09-24-fix1212-bootstrap-runner.md`, a fresh name for 09-24.
- The wait deadline was 11:04:47.
- I polled the log every ≤ 9 min, and nothing else touched prod meanwhile.

**Poll series: 97 polls at 5 min. The gate held 96 of them; the census held 0.**

| UTC | polls | held by |
|---|---|---|
| 01:04–02:29 | 18 | `b:fr-vacuum-analyze` (→04:31, then →03:10 once it ran) |
| 02:34–03:09 | 8 | `b:ec-vacuum-analyze`, `b:fr-vacuum-analyze` |
| 03:14–03:44 | 7 | `b:ec-vacuum-analyze` |
| 03:49–04:39 | 11 | `b:ec-vacuum-analyze`, `c:blackout` |
| 04:44–04:49 | 2 | `c:blackout` |
| 04:54–04:59 | 2 | `b:fe-vacuum-analyze`, `c:blackout` |
| 05:04–05:59 | 12 | `c:blackout` |
| 06:04 | 1 | `c:blackout`, `d:cron-job-budget-watchdog`, `e:live_writers` (the 06:0x stack) |
| 06:09 | 1 | `c:blackout`, `d:cron-job-budget-watchdog` |
| 06:14–08:59 | 34 | `c:blackout` |
| **09:04:48** | 1 | **OK**, census pass (0/60 min, ratio 0.00, floor 6; edge 0 of 136) |

- The census was read once, on the one gate-ok poll, and passed.
- The prompt expected "~09:00"; it opened on the first poll after 09:00.
- There were no census FAILs in the wait, so there is nothing to set against the deploy clock there. The run's one census FAIL (09:34:11) came 6 h 29 min after the last push to main before it: 994b8d65 at 03:05:24 UTC, cc-150's report. There was no deploy adjacency.

**Claim.**
- Claimed at 09:04:54. Arm on pid 165967: held=true, defer=false, claimant_bypass=true, label_pid 17080.
- Before: watermark 2026-07-28 05:54:28.48494, cursor `[1,2]`, crawl null, MV 1,728,538 / 732,520,894,800.
- The runner logged "resuming at window 3".

**Every CALL (max_units 2):**

| CALL | started | wall s | windows | stage s | apply s | watchdog budget / derived max s | elevated |
|---|---|---|---|---|---|---|---|
| 1 | 09:04:59 | 247.6 | 3, 4 | 119.06, 123.01 | 2.80, 2.53 | 1.194 / 1.015 | 2 |
| 2 | 09:10:12 | 270.6 | 5, 6 | 144.83, 120.71 | 2.89, 1.96 | 1.240 / 1.099 | 3 |
| 3 | 09:16:17 | 246.9 | 7, 8 | 118.26, 117.00 | 2.58, 8.84 | 1.190 / 0.839 | 1 |
| 4 | 09:22:29 | 255.9 | 9, 10 | 122.93, 122.33 | 7.82, 2.64 | 1.155 / 1.035 | 3 |
| 5 | 09:28:20 | 256.1 | 11, 12 | 117.45, 126.77 | 5.52, 3.11 | 0.993 / 0.744 | 0 |

- **Watchdog series per CALL** (pre-CALL plus two ticks, n = 3 each), min / median / max in seconds, budget and derived:

  | CALL | cron-job-budget-watchdog | derived-mvs-unit-watchdog |
  |---|---|---|
  | 1 | 0.020 / 0.796 / 1.194 | 0.006 / 0.517 / 1.015 |
  | 2 | 0.049 / 1.224 / 1.240 | 0.022 / 0.975 / 1.099 |
  | 3 | 0.028 / 0.548 / 1.190 | 0.010 / 0.274 / 0.839 |
  | 4 | 0.018 / 1.129 / 1.155 | 0.007 / 0.998 / 1.035 |
  | 5 | 0.033 / 0.186 / 0.993 | 0.025 / 0.132 / 0.744 |

  12 readings fell in the elevated [1, 3) s band. No reading reached 3.0 s, and there were 0 startup timeouts, 09:00 onward.
- **Breathers:**

  | before CALL | waited s | released by | walls at release (budget / derived) |
  |---|---|---|---|
  | 2 | 60.8 | walls | 0.049 / 0.022 s |
  | 3 | 90.8 | walls | 0.028 / 0.010 s |
  | 4 | 120.9 | walls | 0.018 / 0.007 s |
  | 5 | 90.9 | walls | 0.033 / 0.025 s |
  | 6 | 90.8 | walls | 0.031 / 0.018 s |

- **Census rows (with the floor):**

  | UTC | when | window | reading | edge |
  |---|---|---|---|---|
  | 09:04:53 | gate poll | 60 min | pass: 0, floor 6 | 0/136 |
  | 09:04:59 | before CALL 1 | 15 min | pass: 0, floor 3 | 0/136 |
  | 09:10:12 | before CALL 2 | 15 min | pass: **1, ratio 2.02**, floor 3 | 0/136 |
  | 09:16:17 | before CALL 3 | 15 min | pass: **1, ratio 2.02**, floor 3 | 1/279 = 0.36 % |
  | 09:22:29 | before CALL 4 | 15 min | pass: **2, ratio 4.04**, floor 3 | 1/279 |
  | 09:28:20 | before CALL 5 | 15 min | pass: **2, ratio 4.04**, floor 3 | 1/279 |
  | 09:34:11 | before CALL 6 | 15 min | **FAIL: 8, ratio 16.16**, floor 3 | **2/183 = 1.09 %**, FAIL |

  The four bold passes would each have tripped under cc-148's rule. The 15-min cadence census never fired, because each pre-CALL census reset its clock.

**Outcome.**
- `stopped`, exit 4: "stop rule before CALL 6: (3) census pass=false".
- The CALL backend was verified no longer running the CALL (Supavisor, idle, DISCARD ALL). The session was released at 09:34:12.

**Before and after, read on Pro at 09:35:39:**

| | before | after |
|---|---|---|
| cursor | `[1,2]` | `{mode full, target 2026-09-21T00:18:25.831727, started_at 2026-09-23T09:02:48.434717, windows_done [1..12]}` (started_at kept) |
| watermark | 2026-07-28 05:54:28.48494 | 2026-07-28 05:54:28.48494 (correctly unmoved: not caught_up) |
| crawl row | absent | absent (5 upserts / 5 restores) |
| MV | 1,728,538 / 732,520,894,800 | 2,618,055 / 867,716,229,900 (**1.184562×**) |
| session | clear | clear, no label |
| bootstrap backends | 0 | 0 |

## 4. FIX status, commits, CI

- **Status:**
  - FIX-1212 open (no row).
  - **FIX-1217 filed**, open.
  - FIX-1216, 1125, 1194 and 969 open.
  - FIX-1218 was filed by cc-150, which ran concurrently.
- **Open: 56 → 58** (+1217 here, +1218 by cc-150). The prompt's arithmetic for this run is 56 → 57: D3 filed and 1212 stayed open.
- **Commits:** 82841454 → f6014452, all on `origin/main`:
  - a769d630 (D1);
  - 2fc50727 (D2 + D4 receipt);
  - 87cdd6cc (FIX-1217);
  - aed3a09c (prod receipt);
  - f6014452 (appends).
  - cc-149's and cc-150's commits (07c0181c … 994b8d65) landed between 87cdd6cc and aed3a09c. They are not this run's.
- **CI:**
  - `tests.yml` 35941087663 green on 87cdd6cc: Typecheck, Lint, Silent-read guard, Render-timeout guard, Workflow resource-parity guard, Inert statement_timeout guard, and the three unit suites.
  - The latest `tests.yml` on main is green (a478880e, cc-150's).
  - aed3a09c and f6014452 are docs-only, so paths-ignored.

## 5. What contradicted cc-148, the receipt, or this prompt

**1. Does ~130 s per window hold across windows 3–12? Yes, a little under.**
- Stage times were 117.0–144.8 s, mean 123.2 s, with no drift from window 3 to 12.
- The per-CALL wall is 247–271 s for two windows plus 2–9 s of apply.
- Windows 13–16 were not measured.

**2. Do the walls recover in the breathers? Yes, every time, and fast.**
- Every breather released on walls in 60.8–120.9 s. The wait is the */2 cadence, not the box: release needs a watchdog run that started after the CALL returned.
- Walls at release were 0.018–0.049 s.
- No breather timed out. The box recovers between CALLs.

**3. The floored census DID read FAIL, once, and there was load behind it — this op's.**
This is the finding of the run.
- All 9 front-door 57014s between 08:34 and 09:34 fell inside CALLs. None was the donor-page read (FIX-1217); that read did not fire at all during the run.

  | UTC (edge time) | CALL | read |
  |---|---|---|
  | 09:07:53 (09:07:48) | 1 | `proposals` (`order=introduced_at.desc…limit=1000`) |
  | 09:19:59 | 3 | `rpc/get_official_page` |
  | 09:24:21 | 4 | `officials` (`id=in.(…)`) |
  | 09:30:26 ×6 (09:30:20) | 5, at +126 s | `entity_tags` ×4 (`entity_type=eq.official`), `entity_engagement_rollup_mv`, `officials` with a jurisdictions embed |

  - All nine were user-agent `node`, i.e. Vercel server renders.
  - 0 fell in the 60 min before the claim, and 0 in the ~7.6 min of breathers.
- So the stop rule did the job it exists for: the census saw the visitor-facing cost of the op.
- **The watchdog wall did not see it.** At CALL 5 +121 s, the tick nearest the 6-event burst, the walls read 0.186 / 0.132 s.
- The prompt asks whether the stop was load or another burst of the same read. It was load. The reads are ordinary page reads (indexed, normally fast), timing out at anon's 3 s while the stage build held the box in `IO:DataFilePrefetch` and `LWLock:WALWrite`.
- **For Craig:**
  - a relaunch under this rule will likely stop again around the same point;
  - Tuesday's unsupervised 4 windows will cost the front door the same kind of burst, at 15:00 UTC;
  - the choice is whether ~8 official-page timeouts per 15 min is a stop or the price.

**4. The edge half has the same small-count problem D1 fixed, and D1 did not touch it.**
- The edge gate fails the last closed 15-min bucket at > 1 % 5xx, with a median of 198 requests per bucket (24-h read). So 3 events fail it.
- 4 of 96 buckets in the 24 h before this run were over 1 %: 06:00, 07:00, 13:00, 00:15. Three of those four are FIX-1217's donor-page bursts; 06:00 is enrichment_queue.
- Read 6's "before" census failed only on the edge half (1.03 %), after the 57014 half had passed under the new floor.
- At the 09:34 stop, both halves failed, so the edge half did not decide the outcome.
- Not changed, because D1 scoped the floor to `verdictFor`. A Poisson or binomial floor on the edge half is the same fix.

**5. `prod_op_gate(3600)` was OPEN at read 6 (00:44:47), not blocked.**
- The 2-h span ended 02:44, before `fr-vacuum-analyze` at 03:00. It closed as the span crossed 03:00, and read `b:fr-vacuum-analyze` at 01:04:22.
- It was unusable, because items 1–2 were not built.
- Also, cc-148 §5.5's "22:35–01:35 a clone reads nightly_due" is really 22:35–00:00: after midnight UTC the next start rolls to tonight. D4 needed no scratch nightly row.

**6. D3's filing condition as written did not hold, and I filed anyway, on the call site.**
- The condition asked for ONE query text at ≥ 50 %. The 34 FR events are three texts at 12 / 11 / 11, the largest 25.5 %.
- They are one `Promise.all` in one page (`donors/[id]/page.tsx:219-297`), always failing together, for one entity per render. As one read they are 72.3 %.
- The prompt's own premise was "one `financial_relationships` read", and its bullet asks for one call site. I read "the read" as the call site.
- If that is the wrong reading of rule 156, FIX-1217 closes `closes-as-no-op` with nothing else touched.
- Separately, the bullet's mechanism is a CLONE plan, not a prod measurement. It is labelled as an estimate.

**7. `receipts-daily.ts` is not a census caller** (read 3). The prompt expected "the receipts' daily census line" to show the new verdict. There is no such line, only a prose pointer at `:651`.

**8. Smaller things.**
- **Clone rehearsal flags.** The rehearsal used `--expected-minutes 20`, because the clone's `fr-vacuum-analyze` is active at 03:00 and a 2-h span from 01:0x reaches it. The 60 applies to prod only.
- **Test collision.** The 5 `prod-session` DB tests fail when run while a rehearsal holds the local claim. They are not flaky; they share the one local interlock. Run them before or after a rehearsal, not during.
- **Label.** The claim label still reads `FIX-1212 bootstrap (cc-147 runner)`.
- **Concurrency.** cc-149 and cc-150 ran and pushed while this run's runner waited: 02:xx–03:05 UTC, all before the gate opened and outside the run window. Their FIX-1218 dispatcher and gate (g) changes did not reach this runner, which was launched at 87cdd6cc.
- **Log noise.** `DEP0190` warnings appear once per census child process (`spawn` with `shell: true` on Windows). They are harmless and not changed.
