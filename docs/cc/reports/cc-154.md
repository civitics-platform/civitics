---
cc: 154
prompt: cc-prompt-154-addendum-to-152-census-unavailable-is-not-dark-launch-the-1212-tail-front-door-watch-loud-when-blind.md
started_at: 2026-09-25T00:34:53Z
finished_at: 2026-09-25T01:09:49Z
head_before: edb81f48
head_after: 6a3ee437
commits:
  - {sha: 715d9dae, subject: "fix(data): FIX-1219 — the census distinguishes an endpoint that is gone (410 → exit 8, unavailable) from a Logs API that is dark (exit 2); the FIX-1212 runner does not hold or trip on unavailable"}
  - {sha: 47428fd1, subject: "fix(app,db,data): FIX-1219 — the front-door watch reports its corroborator unavailable instead of ok; report-only canary"}
  - {sha: de0ba039, subject: "fix(data): FIX-1212 Pattern B — the paced runner banked windows 13-16 on Pro and the donor-party full rebuild is caught up; watermark = 2026-09-21 00:18:25.831727"}
  - {sha: 4142f7d2, subject: "chore(fixes): sync status after FIX-1212"}
  - {sha: 6a3ee437, subject: "docs(fixes): FIX-1212 / 1219 / 969 appends (cc-154: the tail caught up under the paced runner; exit 8 and the blind front-door watch; jobid 17 takes the crawl branch on 09-29)"}
fixes_closed: [FIX-1212]
fixes_filed: []
fixes_reopened: []
migrations_pushed: []
prod_writes: "The FIX-1212 bootstrap runner, launched once from the primary checkout at 00:45:27 UTC (node pid 7828, at 715d9dae). It claimed the FIX-950 session 00:45:29-00:54:09 (label written and deleted). It upserted pipeline_state.donor_party_crawl = {max_units: 2} before each of 2 CALLs and deleted it after each (absent before and after). Two CALLs of public.refresh_donor_party_rollup_incremental() wrote data_sync_log a6e947dc and 337b8512. They replaced donor_party_rollup_mv windows 13-16 (2,618,055 -> 2,972,870 rows), completed the cycle (the procedure deleted the cursor pipeline_state.donor_party_full_rebuild), and advanced the watermark to 2026-09-21 00:18:25.831727. Pushes to main at 00:42:38, 00:55:20 and 00:57:06 UTC deploy to Vercel. de0ba039's deploy (live 00:57:05) changed /api/cron/front-door-watch, which now writes its data_sync_log rows with state corroborator_unavailable and a logs_api field. Every other prod contact was a read: db-query.mjs --prod, and census invocations against the Management API Logs endpoint (all 410)."
ci: green
stopped_items:
  - "Item 5's canary half is not verified by a canary run. sync-canary-check next fires at 05:00 UTC (the Vercel dispatcher), after this session. The key is verified by the unit tests and by running the new classifier over the two real prod front_door_watch rows (tier report, detail quoted in §4). No canary was run by hand: it writes a meta row on prod."
  - "cc-152 item 7 (the visitor cost of a paced CALL) stays owed. Every census call returned exit 8, so the tail's front-door cost is not measured. It waits on FIX-1219's port."
  - "FIX-1216 got no append: the run kept the cursor's started_at and the procedure deleted the cursor on caught_up, exactly as its bullet expects."
---

# cc-154: the FIX-1212 tail is caught up on Pro, the census says "unavailable" instead of "dark", and the front-door watch no longer reports `ok` while blind

**FIX-1212 is closed.** The paced runner banked windows 13–16 in two CALLs,
00:45–00:54 UTC. It then asserted `caught_up`:
- the cursor is gone;
- the watermark is 2026-09-21 00:18:25.831727, the cycle's target.

The donor-party rollup's watermark had not moved since 2026-07-28. On Tue
09-29 at 15:00, jobid 17 takes the **crawl** branch: today's lag is 0,
against a 14-day full-rebuild threshold. jobid 17's own first `caught_up`
there is FIX-969's receipt. The FIX-1216 treadmill no longer applies to
this cycle.

**The two FIX-1219 halves that landed tonight:**
- **The census.** An endpoint that answers 410 is now exit **8,
  unavailable**. It is no longer exit 2, dark. The runner opens its gate on
  the gate alone and never trips on an 8. The dark-twice rule is untouched
  for a real dark.
- **The front-door watch.** Since 09-24 10:15 UTC it had reported `ok` on
  every firing while its Logs corroborator saw nothing. It now writes
  `corroborator_unavailable`, and the first firing after the deploy
  (01:00:28) did. The canary gains a report-only key for that state.

**Still owed:**
- the port to the new `logs` endpoint (FIX-1219);
- the tail's visitor cost, which nothing could measure.

## 1. Reads 2–5

**Read 1 (00:35 UTC).**
- `git pull` found edb81f48 (cc-153's report).
- `session:check`: 60 open, as expected.
- `fixes:status`:
  - FIX-1212 and 1219 open;
  - **FIX-1130 closed** (2026-09-03, f706f301), so its append folded into
    1219's.

**Read 2 — the census's exit paths** (`cancellation-census.ts`, tree at
edb81f48). Four paths reached `process.exit(2)`:
- `:236` no `SUPABASE_MANAGEMENT_API_KEY`;
- `:251` attribution got `null`;
- `:307` either gate half got `null`;
- `:389` fatal.

Details:
- Both gate halves (`postgres_logs`, `edge_logs`) and `--by` go through the
  one `query()` helper against `LOGS_URL` = `…/analytics/endpoints/logs.all`.
  That is why both printed `[census] Logs API 410 Gone`, from `:213`.
- `--help` (`:175-179`) printed usage only, with no exit vocabulary.
- **A trap in the exit path itself.** `process.exit(2)` after two
  fast-failing fetches hit the Windows `UV_HANDLE_CLOSING` abort. cc-152
  read 5 saw exit 127 / ELIFECYCLE 3221226505, not 2. The attribution path
  already documents this, and returns on `process.exitCode` for it. A
  `process.exit(8)` would likely have reached the runner as 127, i.e. dark.

**Read 3 — the runner's census consumers, as they were** (cc-152 quoted the
lines):
- the gate half `:1204-1209` `ok: cen.code === 0`;
- `evaluateCensus` (exit 2 → the dark count);
- `censusSummary`, where anything not 0/1 → `dark (exit N — the Logs API did not answer)`;
- `gateTally`, where a `dark` poll is `/^dark/` on the census half's summary.
- `AlsoReading` is `{name, ok, summary}`. There is no `note` field, so the
  prompt's `note` went into `summary` (rule 133).

**Read 4 — the front-door watch's dark path.**
- `fetchBuckets` returned `null` on any non-OK. `alignBuckets(rows ?? [])`
  zero-filled four buckets, and `decideFrontDoorVerdict` fell through to
  `{state: "ok", reason: "no sustained 52x, front door answering"}`.
- The route writes `data_sync_log` `front_door_watch` with metadata
  `{source, state, reason, probe_answered, emailed, buckets}`. Its status is
  `partial` on `down` and `complete` otherwise.
- **Readers of `state`:**
  - the route's own JSON body;
  - `shouldSend` (sends on `down`/`recovered` only);
  - `renderFrontDoorEmail` (`down` or else recovered);
  - the tests.
- **No canary key read `front_door_watch`.** A grep of every `canary-*.ts`
  finds nothing. The only other reader is the rollup registry: it reads
  cadence via `rollup_watch_overrides` (0.25 h) and counts only
  `status='complete'` rows. So the prompt's "the canary key(s) that read it"
  did not exist, and I added one (§2).
- **The probe pages independently.** `if (!probe.answered) return {state: "down", isDownEdge: true}` comes before any bucket logic. That
  is the premise that makes D4 report-only, and it holds.

**Read 5 — Pro at 00:36:51 UTC, exactly cc-152 read 2:**
- cursor `[1..12]`, `started_at` 2026-09-23 09:02:48;
- crawl row absent;
- watermark 2026-07-28 05:54:28.48494;
- MV 2,618,055 / 867,716,229,900;
- session clear, 0 backends.

`prod_op_gate(1200)` at 00:36:55 read `ok=true`, `blocked_by []`, span to
01:16:55. The clock was as the prompt wrote it: the push was on main well
before ~01:45.

## 2. D1 / D2 / D4 — what changed, the fixtures, the counts

**D1 — census exit 8** (715d9dae). In `lib/cancellation-census.ts`:
- `fetchLogs()` classifies each answer as one of three:
  - `rows`;
  - `dark`: a 5xx, a throw, or a 200 without a result array;
  - `unavailable`: 410, or 404 on the same path.
- `answersExit()` picks 8 if either half is unavailable (8 wins over a dark
  sibling, because both halves share one endpoint), else 2 if either half
  is dark.
- `unavailableSummary()` and `unavailableJson()` build the exit-8 output.

The script:
- exits 8 with `unavailable — Logs API endpoint removed (410; FIX-1219)`, or
  `{window, unavailable: true, http_status: 410, fix: "FIX-1219", summary}`
  under `--json`, in place of the verdict halves;
- `--by` exits 8 the same way;
- the header and `--help` name 8, and 3 stays unused;
- **exit 8 and both exit-2 paths now end on `process.exitCode` and a
  return.**

The exit-2 change was not asked for. It is the same lines and the same
Windows bug. The runner is unaffected, because it read 127 as dark too.

Measured against prod from this box: exit 8 in ~2 s, in text, `--json` and
`--by` modes, and through the primary's `data:census:cancellations:prod` at
00:42:52.

**D2 — the runner** (715d9dae):
- `censusHalfReading(code, summary)` opens on 0 or 8, and holds on 1, 2 and
  anything else, including 127.
- `evaluateCensus(state, 8, mode)` returns `null` in both modes and touches
  nothing. **8 is transparent**: dark, 8, dark still trips. I chose `null`
  over a fourth shape, because the row's `code: 8` already carries it.
- `censusSummary(8)` prints `unavailable (FIX-1219 — Logs API endpoint removed, HTTP 410)`.
- `gateTally` appends `; N read the census unavailable (FIX-1219) and opened on the gate alone`
  only when N > 0, so cc-151's lines are byte-identical.
- The census-mode row counts unavailable calls apart from would-trips.
- The header doc gains the sentence.

**D4 — the front-door watch** (47428fd1). This half had to go wider than
"smallest honest form", because no reader existed to be report-only.
- **`packages/db` `front-door-verdict.ts`:**
  - `FrontDoorCorroborator` (`ok` with a bucket count, `unavailable` with a
    status, `dark` with a detail), `isLogsEndpointGone`, `corroboratorLabel`;
  - `decideFrontDoorVerdict(buckets, probe, corroborator?)` gains the state
    **`corroborator_unavailable`**. It applies when the probe answered and
    the corroborator did not.
  - A probe with no answer is still `down`.
  - `shouldSend` never sends the new state.
  - With no third argument, or an `ok` one, verdicts are unchanged.
- **The state vocabulary took a new value with no parser change.** Every
  reader of `state` either switches on `down`/`recovered` or passes it
  through.
- **The route:**
  - passes the corroborator;
  - its body and its row's metadata carry `logs_api`:
    `"4 bucket(s)"` | `"unavailable (410, FIX-1219)"` | `"dark (…)"`;
  - **a blind tick's row stays `complete`**, so the rollup registry's
    freshness clock does not freeze on a watch that did run.
- **The canary key `front_door_corroborator_unavailable`** (report-only,
  severity constant 1) is raised when the newest `front_door_watch` row in
  the last 60 min is blind. It is registered in `canary-keys.ts` and the
  census test's `CONSTANTS`.
- Staleness stays with `rollup:front_door_watch`.

**Fixtures and counts:**

| suite | before → after | added |
|---|---|---|
| census (`lib/cancellation-census.test.ts`) | 28 → 33 | 410 and 404 → unavailable; 503, a throw, and the new endpoint's 200-with-`{error}` → dark; a 200 with rows unchanged; the 8-over-2 precedence and exit table; the exit-8 JSON shape; the `process.exitCode` source guard |
| runner | 29 → 36 | **{8, 8, 8}** → no trip, 0 would-trips, dark count 0 (both modes); **{2, 8, 2}** → trips (8 transparent), with a pass as the contrast; **{8} at the gate** → opens, while 1, 2 and 127 hold; the summary, tally and census-mode rows; a source guard |
| db `front-door-verdict` | 22 → 27 | a 410 corroborator is not `ok` and not sent; dark reads the same; a probe with no answer is still `down`; the old shapes are unchanged and the 08-31 replay still pages; the labels |
| data `canary-front-door` (new) | → 4 | |

**Totals:** `@civitics/data` 976 → 995 (988 pass, 7 skipped); app 321;
db 227 → 232.

**Checks:**
- typecheck and lint clean;
- reads 162/0, render-timeouts 161/0, workflow-parity 0 undeclared,
  proconfig 23/0;
- the script suites all pass, and `fixes:check` is clean.

**CI:**
- 36078781028 green on 715d9dae;
- 36079728113 green on de0ba039;
- all seven checks as their own steps.

## 3. Item 3 — the prod run

**Launch gates, re-read at 00:45:08–12 UTC. All true:**
- (i) 715d9dae on `origin/main`, CI green;
- (ii) the census exits **8**. Through the primary's prod script at
  00:42:52: `"summary": "unavailable — Logs API endpoint removed (410; FIX-1219)"`,
  `ELIFECYCLE … exit code 8`;
- (iii) cursor `[1..12]`, crawl absent, watermark unmoved;
- (iv) interlock clear, live_writers [], 0 bootstrap backends;
- (v) held=false;
- `prod_op_gate(1200)` `ok=true`, `blocked_by []`, span to 01:25:12.

**Launch.**
- 00:45:27 UTC from the primary checkout at 715d9dae; node pid 7828.
- The command was cc-152 D3's, verbatim, with no `--`.
- Log at `scratchpad/cc154-runner.log`, outside the repo.
- Receipt `docs/audits/2026-09-25-fix1212-bootstrap-runner.md`, a fresh UTC
  day.

**The polls: one.**
- At 00:45:27 the gate read OK, with census
  `unavailable (FIX-1219 — Logs API endpoint removed, HTTP 410)`.
- The runner opened on the gate alone: `1 poll(s): 0 held by the gate, 0 by the census (0 dark); 1 read the census unavailable (FIX-1219) and opened on the gate alone`.

**The claim.**
- Claimed 00:45:29. The arm on pid 243007 read held=true, defer=false,
  claimant_bypass=true, label_pid 7828.
- `[resume] resuming at window 13`, cycle started 2026-09-23 09:02:48.

| CALL | started | wall s | windows | stage s | apply s | status | watchdog max budget / derived | elevated |
|---|---|---|---|---|---|---|---|---|
| 1 | 00:45:31 | 244.5 | 13, 14 | 124.06, 113.12 | 3.18, 3.82 | partial (unit cap) | 1.365 / 0.826 | 1 |
| 2 | 00:50:07 | 239.3 | 15, 16 | 115.28, 117.99 | 2.84, 3.01 | **complete, caught_up** | 1.365 / 1.027 | 2 |

- **Watchdog series (min / median / max, s):**
  - CALL 1: budget 0.027 / 0.979 / 1.365 (n=3), derived 0.005 / 0.658 /
    0.826;
  - CALL 2: budget 0.063 / 0.714 / 1.365 (n=2), derived 0.044 / 0.535 /
    1.027.
  - Both +120 s ticks read the backend in `IO:DataFilePrefetch`.
  - 0 startup timeouts, and no reading ≥ 3.0 s.
- **The breather** before CALL 2 was released by **walls after 30.4 s**
  (0.063 / 0.044 s, runs 65137 / 65139).
- **Census rows, all exit 8 `unavailable`:**
  - gate poll 00:45:28 (60 min);
  - before CALL 1 00:45:32;
  - before CALL 2 00:50:08.
  - 0 would-trips, 0 dark. No cadence census fired: each pre-CALL census
    reset its 15-min clock.
- **Outcome:** `caught_up`, exit 0, "caught_up (complete) after 2 CALL(s)".
- The CALL backend was verified no longer running the CALL (Supavisor,
  idle, `DISCARD ALL`). Released at 00:54:09.

**The `caught_up` assertion's two halves:**
- `cursor_gone=true`;
- `watermark 2026-09-21 00:18:25.831727+00 = target 2026-09-21T00:18:25.831727+00:00: true`.

**Before and after, the after re-read independently on Pro at 00:54:20:**

| | before (00:45:31) | after |
|---|---|---|
| cursor | `[1..12]` | absent |
| watermark | 2026-07-28 05:54:28.48494 | **2026-09-21 00:18:25.831727** |
| crawl row | absent | absent (2 upserts / 2 restores) |
| MV rows / SUM | 2,618,055 / 867,716,229,900 | **2,972,870 / 921,676,324,900** (1.062186×) |
| session | clear | clear, no label |
| bootstrap backends | 0 | 0 |

**Item 4.**
- de0ba039 carries the receipt, `Fixes: FIX-1212` and `Verified: prod`.
- `fixes:sync` appended `2026-09-24 | FIX-1212 | de0ba039 | prod-only`
  (4142f7d2).
- The runner wrote its receipt into the PRIMARY checkout's `docs/audits/`,
  because its `REPO_ROOT` is its own tree. It was moved into the worktree to
  commit, and the primary is clean.

## 4. Item 5 — the first post-deploy watch row and the canary key

- **The deploy.** de0ba039's Vercel status went `success` at 00:57:05 UTC.
- **The rows.** `front_door_watch` on Pro:

  | started_at | status | state | logs_api | emailed |
  |---|---|---|---|---|
  | 00:45:28 (old code) | complete | `ok` | — | — |
  | **01:00:28** (new code) | complete | **`corroborator_unavailable`** | **`unavailable (410, FIX-1219)`** | — (not sent) |

  The 01:00:28 reason reads: "direct probe answered; the Logs corroborator
  is unavailable (410, FIX-1219) — the 52x wedge rule could not be
  evaluated this tick".
- **The canary key.** The next `sync-canary-check` fires at 05:00 UTC
  (Vercel dispatcher `0 5 * * *`; last stamp 09-24 05:00:13), after this
  session.
- The new classifier over those two real rows returns `tier: "report"`,
  `severity: 1`:

  > front-door watch corroborator unavailable (410, FIX-1219) on 1 of 2 firing(s) in the last 60 min (newest 2026-09-25 01:00:28.349+00): the 52x wedge rule is blind; the direct probe still pages — FIX-1219

  The 05:00 run should carry `front_door_corroborator_unavailable` in
  `report_only`. Not observed; see `stopped_items`.

## 5. FIX status, commits, CI

- **Status:**
  - **FIX-1212 closed** (de0ba039, prod-only);
  - FIX-1219 open, with the appends;
  - FIX-969 open, its receipt now being Tue 09-29's own `caught_up`;
  - FIX-1216 open;
  - FIX-1130 closed (2026-09-03).
- **Open: 60 → 59**, matching the prompt's arithmetic.
- **Commits**, edb81f48 → 6a3ee437. Every push had a `held=false` read,
  seen, first:
  - **00:42:38** (read 00:42:19): 715d9dae (D1 + D2);
  - **00:55:20** (read 00:54:57, after the 00:54:09 release): 47428fd1
    (D4) and de0ba039 (Pattern B);
  - **00:57:06** (read 00:56:48): 4142f7d2 (sync) and 6a3ee437 (appends).
  - No push happened while the claim was held.
- **Order.** D4 (47428fd1) sits before the Pattern B commit in history
  because it was committed locally while the runner ran. Both landed in
  the same post-release push.
- **CI:**
  - `tests.yml` 36078781028 green on 715d9dae;
  - 36079728113 green on de0ba039 (the latest on main);
  - 6a3ee437 is docs-only.
- **Concurrency.** cc-153 was finished before this run; there were no
  other pushes in the window.

## 6. What contradicted cc-152 or this prompt

**1. Did windows 13–16 cost what 3–12 did on prod? Yes, and slightly less.**

| | windows 13–16 (this run) | windows 3–12 (cc-151) |
|---|---|---|
| stage per window | 113.1–124.1 s (mean 117.6) | 117.0–144.8 s (mean 123.2) |
| CALL wall | 239.3–244.5 s | 246.9–270.6 s |
| apply | 2.8–3.8 s | |

cc-152's clone numbers (13–55 s, cold cache) were the clone, not prod.

**2. Did the breather release on walls? Yes, in 30.4 s**, faster than
cc-151's 60.8–120.9 s. Release needs a watchdog run that STARTED after the
CALL returned. CALL 1 returned at 00:49:37, and the */2 runs fired at
00:50:00. The wait is the cron phase, not the box.

**3. Did anything other than the op show in the watchdog series? No.**
- Three elevated readings, all at the +120 s tick of a CALL, all with the
  backend in `IO:DataFilePrefetch`: the stage build's own load, cc-151's
  signature.
- Breather and pre-CALL walls were 0.027–0.063 s.
- 0 startup timeouts.
- The front door itself was unmeasured. The census was unavailable
  throughout, and the watch's corroborator was blind until 01:00. So "what
  did visitors see" has no instrument tonight, and cc-152 item 7 stays
  owed.

**4. What the watch's state vocabulary could and could not take.**
- **Could:** a new `state` value, with no parser change. No reader
  exhaustively matches `state`. `shouldSend` and the email switch on
  `down`/`recovered`; the body and metadata pass it through.
- **Could not:** be surfaced by an existing canary key. None read the
  watch's rows. The prompt assumed one ("the canary key(s) that read it");
  there was only the rollup registry's cadence. So D4 added the key.
- **The trap that would have paged instead:** writing `status: "partial"`
  for a blind tick. The registry counts only `complete`, so
  `rollup:front_door_watch` would have gone stale and escalated.

**5. Two premises that were off:**
- `AlsoReading` has no `note` (read 3).
- The exit-8 code path needed `process.exitCode`, not `process.exit()`. The
  prompt did not anticipate that (read 2). Without it, the whole of D2
  would likely have read 127 → dark on this box.

**6. For FIX-1219's prompt.** max `financial_relationships.updated_at` on
Pro reads **2026-09-21 00:18:25.831727** (read 00:55:46). That is the
cycle's target, so no FR row has been written or updated in ~4 days. I
checked only one of the nightlies since then: 09-24's fec phase completed
(240 s). It may be expected (no FEC drop, nothing to write). It is also
exactly what a stalled ingest looks like. I did not investigate; it is out of scope here. It is also why
jobid 17's lag reads 0.
