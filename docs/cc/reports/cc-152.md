---
cc: 152
prompt: cc-prompt-152-fix1212-tail-windows-13-16-census-report-only-relaunch-plus-fix1218-pattern-b.md
started_at: 2026-09-24T23:39:59Z
finished_at: 2026-09-25T00:21:17Z
head_before: 9f7462ab
head_after: b6fda36f
commits:
  - {sha: 7ee63e55, subject: "feat(data): FIX-1212 — the runner's census can run in report mode; rule (3) pass=false is recorded, not a stop; the dark-twice trip stays armed"}
  - {sha: 0d96d726, subject: "docs(fixes): file FIX-1219 (Supabase removed the Logs API logs.all endpoint; the census and the front-door watch's corroborator are dark)"}
  - {sha: 23adb556, subject: "chore(nightly): FIX-1218 Pattern B record — the dispatched nightly did nominal day 2026-09-25 at its slot; the schedule fallback stood down"}
  - {sha: 56a0fad5, subject: "chore(fixes): sync status after FIX-1218"}
  - {sha: 6e7d106b, subject: "docs(receipts): FIX-1212 clone rehearsal of --census-mode report, 2026-09-25 — caught_up in 2 CALLs from a [1..12] cursor"}
  - {sha: b6fda36f, subject: "docs(fixes): FIX-1212 / 1218 / 969 appends (cc-152: report mode built and rehearsed; the tail not launched on a dark census; the 1218 Pattern B record)"}
fixes_closed: [FIX-1218]
fixes_filed: [FIX-1219]
fixes_reopened: []
migrations_pushed: []
prod_writes: "None to the database. The FIX-1212 runner was NOT launched, so there was no claim, no pipeline_state write, no CALL and no label. Pushes to main at 23:53:53 UTC (7ee63e55..23adb556) and 00:19:09 UTC (56a0fad5..b6fda36f) deploy to Vercel; no app-route code changed. Every other prod contact was a read: db-query.mjs --prod (read-only by construction), gh, and eight Supabase Management API Logs calls (the census attempt's two, a direct 410 probe, a re-probe at 00:17:40, and four probes of the replacement endpoint)."
ci: green
stopped_items:
  - "Item 5 (the prod launch) did not run. Supabase removed the Management API endpoint the census reads (analytics/endpoints/logs.all answers 410 Gone, confirmed at 23:42:49, 23:43:14 and 00:17:40 UTC), so every census reading is dark (exit 2). The runner's gate wait treats dark as not-open, so it would hold until gate_timeout, and its pre-CALL dark-twice rule would trip if it ever opened, in either census mode. Launch gates (i)-(v) were all true at 00:17:44 and prod_op_gate(1200) was ok at 00:17:47; the instrument the whole op rests on is gone. Filed as FIX-1219. The census script is out of this prompt's scope, so it was not ported."
  - "Item 6 (the FIX-1212 Pattern B record) did not apply: no caught_up. FIX-1212 stays open with no trailer; the only receipt is the clone rehearsal, committed as docs(receipts) (6e7d106b)."
  - "Item 7 (attribution of the tail's cost) did not run: no tail ran, and the attribution mode (--by) reads the same removed endpoint."
  - "Read 5's census before-reading has no value: the Logs API answered 410 Gone and the script exited 2 (dark)."
  - "Item 8 wrote appends for FIX-1212, FIX-1218 and FIX-969 only. FIX-1216, FIX-1125 and FIX-1194 got none: nothing ran on prod, so there is no started_at handling or watchdog series to add."
  - "Rule 180's held=false read before the 00:19:09 push ran, but my output filter printed '(1 row)' instead of the value, so the push went out on an unseen reading. The re-read at 00:19:17 (8 s later) was held=false, claimed_at null, live_writers []. No claim existed this session: the runner never launched, and cc-153 does not claim."
---

# cc-152: report mode is built and rehearsed, but the tail was not launched, because Supabase removed the Logs API endpoint the census reads

**FIX-1212 is still open, and windows 13–16 did not run.** Prod is exactly
as cc-151 left it: cursor `[1..12]`, watermark unmoved, MV 2,618,055 /
867,716,229,900.

**What stopped it.** Supabase retired the Management API's
`analytics/endpoints/logs.all`. It now answers `410 Gone`: "Use GET
/v1/projects/{ref}/analytics/endpoints/logs instead". That endpoint is the
one the cancellation census reads, so every census reading is now **dark**.
The runner cannot run on a dark census, in either mode:
- its gate wait holds on dark;
- its pre-CALL dark-twice rule trips.

That rule stays armed by design (rule 164). A launch would have waited 12 h
and exited `gate_timeout`. Filed as **FIX-1219**.

**When the endpoint went.** Prod's `front_door_watch` rows show the
cutover between **10:00 and 10:15 UTC on 09-24**, about 40 minutes after
cc-151's last census. The front-door watchdog lost its Logs corroborator at
that moment. It has reported `ok` every 15 minutes since, with zero requests
in every bucket. It fails silent.

**What landed:**
- `--census-mode stop|report`, rehearsed on the clone: 2 CALLs,
  `caught_up`;
- the FIX-1218 record: the 21:00 dispatched nightly did the day, and the
  fallback stood down. **FIX-1218 is closed.**

**Your decision: how windows 13–16 get done before the cursor ages out on
09-30 09:02.**
1. **Port the census to the new endpoint (FIX-1219), then relaunch.** This
   is my recommendation if the port can land by Monday night. The runner and
   report mode are ready. The port is more than a URL swap: the new
   endpoint takes ClickHouse SQL over a different, still-unmapped schema.
2. **Let Tuesday do it.** jobid 17 runs windows 13–16 unsupervised at
   Tue 09-29 15:00 UTC. The cursor will be 6 d 6 h old, under the 7-day
   limit. It has no stop rule, and today nothing would measure its
   front-door cost.
3. **Relaunch without the census.** That needs a runner change to disarm
   the dark-twice rule, which this prompt kept armed on purpose.

## 1. Reads 2–5

**Read 1 (23:40 UTC).**
- `git pull` found 27e66239: the 09-25 receipts, written by the dispatched
  nightly.
- `session:check`: 58 open, as expected.
- The pre-existing warnings are the known fix-1178 / fix-1213 worktrees and
  an open PR.
- `tests.yml` has seven checks: Typecheck · Lint · Silent-read guard ·
  Render-timeout guard · Workflow resource-parity guard · Inert
  statement_timeout guard · the three unit suites (data, app, db).
- `.env.local` points at local (127.0.0.1:54321) in the primary checkout,
  the worktree, and `apps/civitics`.
- `fixes:status`:
  - FIX-1212, 1218, 969 and 1216 are open;
  - FIX-1163 is closed (2026-09-13, bd60f873), so it got no trailer.

**Read 2 — FIX-1212 on Pro at 23:40:55 UTC. Every expectation held.**
- `donor_party_full_rebuild` = `{mode full, target 2026-09-21T00:18:25.831727, started_at 2026-09-23T09:02:48.434717, windows_done [1..12]}`.
- `donor_party_crawl` absent.
- Watermark 2026-07-28 05:54:28.48494.
- MV 2,618,055 / 867,716,229,900.
- `prod_session_state()`: held=false, reason_text clear, no label, live_writers [].
- 0 `civitics_dp_bootstrap%` backends.
- Re-read at 00:17:44: identical.

**Read 3 — the census path, quoted from the tree.** The line numbers
matched the prompt's.
- `evaluateCensus` (`:406-412`): exit 0 resets the dark count; exit 1
  resets it and returns `{rule 3, "(3) census pass=false …"}`; dark
  increments it and trips at 2.
- `StopState` `:346-355`.
- The args parser `:181-245`, with `--receipt-tag` the one string flag at
  `:229-230`.
- The receipt rows at `:749-753`, the census row type at `:668`, and the
  rule (3) doc paragraph at `:80-86`.
- **One premise was off.** `evaluateCensus` has **two** call sites, not
  three: the cadence census (`:1024`) and the pre-CALL census (`:1059`).
- The gate half (`:1204-1209`) never calls it. It reads `ok: cen.code === 0`
  directly and is a WAIT, not rule (3). D1 therefore left it unchanged in
  both modes, and the receipt says so.
- Exactly one test read `evaluateCensus`, at `:164-172`, and it runs
  unchanged.

**Read 4 — FIX-1218's two runs. All four points held, with one designed
exception (§6).** Read 23:42–23:49 UTC.
- **Dispatched run 36058610529**:
  - `workflow_dispatch`, `dispatched_by vercel-cron`;
  - createdAt 2026-09-24T21:00:12Z, offset 0.00 h, conclusion success;
  - `docs/receipts/2026-09-25.md` §1 has its four phases `complete`: fec
    240.3 s, heavy 5.3 s, light 9.6 s, tail 327.5 s;
  - `data_sync_log` has 4 `nightly_cron` rows for it, all complete.
- **Fallback run 36073983772**:
  - `schedule`, createdAt 23:41:27Z (2.69 h after the slot);
  - in fec-phase, "Already ran for this nominal day? (FIX-1218)" =
    success, and "Run FEC phase" = **skipped**;
  - enrichment-heavy, enrichment-light and enrichment-tail were
    **skipped**;
  - **0** `nightly_cron` / `nightly_killed` rows carry its id.
- **Stamp**: `gha_dispatch_nightly` = `{status dispatched, http_status 204, at 2026-09-24T21:00:12.468677}`.

**Read 5 — the instruments at 23:42:38 UTC.**
- Watchdogs, last 30 walls (min / median / max):
  - cron-job-budget-watchdog 0.016 / 0.023 / 0.039 s;
  - derived-mvs-unit-watchdog 0.003 / 0.006 / 0.012 s.
- `job startup timeout`, last 6 h: 0.
- `prod_op_gate(1200)`: **ok=true, blocked_by []**, span to 00:22:38. The
  nightly's next start is 21:00 on 09-25; `fr-vacuum-analyze` is next at
  03:00.
- **The census `--minutes 60 --json` exited 2**, with
  `[census] Logs API 410 Gone` twice (both halves).
- A direct request returned the 410 body quoted above.
- The replacement endpoint is not a drop-in:
  - with the census's own edge query it answers **HTTP 200** with
    `{"error":"Table \"edge_logs\" does not exist."}`;
  - `source_name` "does not exist";
  - `select * from logs limit 1` returns "Backend error".
- I stopped probing there. Mapping the new schema is FIX-1219's work.

## 2. D1 / D2

**D1** (7ee63e55, `donor-party-bootstrap-runner.ts`):
- **The flag.** `--census-mode stop|report` defaults to `stop`. Any other
  value is refused: `--census-mode must be stop|report (got "foo")`.
- **`evaluateCensus`.** The signature is now
  `evaluateCensus(state, exitCode, mode = "stop")`, and it stays pure.
- **The third return shape.** I chose a third return shape over a second
  field, and it is `WouldTrip {would_trip: true, rule: 3, reason}`.
  - `Trip` gains `would_trip?: never`, so a `WouldTrip` does not typecheck
    as a `Trip`: it cannot reach `recordTrip` or `stopping` by accident.
  - Both call sites stop only on `isTrip(v)`.
- **The receipt.**
  - `args` carries `censusMode`.
  - A new `census mode` header row counts the would-trips in report mode.
  - Each census row gains `would_trip`: a boolean on rule (3) rows, and
    `null` on gate rows.
  - A would-trip row is marked in the census section. The log suffix is
    `— would have tripped rule (3); census mode report`.
- **The header doc's rule (3) paragraph** gains the two sentences.

**Report-mode semantics in one paragraph.** A `pass=false` census, before
a CALL or on the 15-min cadence, is recorded and logged but does not stop
the run. Everything else still stops or holds:
- two consecutive dark readings (rule 164);
- the forker (1);
- the watchdog wall (2);
- a gone CALL backend (4).

The gate wait's census half still holds the window on a FAIL or a dark
reading. Report mode demotes exactly one thing: rule (3)'s pass=false stop.
Stop mode's two-argument call is today's behaviour byte for byte.

**Tests.** The runner suite went 23 → 29. The existing `:164-172` test
runs unchanged; the args-defaults test gained `censusMode: "stop"`. Added:
- the report twin: exit 1 → a would_trip, not a trip; stop is the
  default;
- dark twice trips in both modes, a pass resets the count in both, and a
  FAIL resets it;
- **the rule 105 fixture**: cc-151's 09:34:11 reading, {8 in 15 min, edge
  2/183}. Run through `verdictFor` / `edgeVerdictFor`, it gives exit 1:
  a would_trip in report mode and a trip in stop mode;
- the census-mode row and the census line, for both modes;
- parse accepts `stop` / `report` (and the `=` form), refuses `foo` and
  `Report`, and accepts the cc-152 launch command;
- a source guard that both call sites pass `args.censusMode`.

**Verification:**
- `@civitics/data` 970 → 976 (969 pass, 7 skipped, 0 fail); app 321/321;
  db 226/226.
- Turbo typecheck (10 tasks) and lint (9 tasks): clean.
- The four checks: reads 161/0, render-timeouts 161/0, workflow-parity 0
  undeclared, proconfig 22 scanned / 0.
- The script suites all pass: fixes, fix-add, session-worktree, drain,
  check-proconfig 18/0, db-query 12/0.
- `fixes:check` is clean.
- **CI:** `tests.yml` 36074988778 was green on 23adb556, with all seven
  checks as their own steps.

**D2 — the clone rehearsal in report mode.** Receipt
`docs/audits/2026-09-24-fix1212-bootstrap-runner-local-235148Z.md`; the
suffix is there because cc-151's `-local` exists.
- **Seed.** I mirrored prod rather than cc-151's `[1,2]`:
  - watermark −60 d (2026-06-21 00:13:37.208343);
  - cursor `{full, target 2026-09-03T01:34:43.542642, started_at 23:51:40, windows_done [1..12]}`;
  - no scratch nightly row.
- **Command.** `--units-per-call 2 --breather-max-s 20 --poll-seconds 30 --max-wait-minutes 45 --expected-minutes 20 --census-mode report`.
- **The wait.** The gate held 45 polls:
  - `a:nightly_due` until 00:00 UTC, exactly as cc-151 §5.5 said;
  - then both watchdogs under the 28-runs-in-60-min floor, because the
    local DB container had come up at ~23:20.
- It opened at 00:14:18 with `census skipped — local`.

| CALL | wall s | windows | stage s | apply s | status |
|---|---|---|---|---|---|
| 1 | 96.8 | 13, 14 | 54.86, 39.50 | 1.38, 0.98 | partial (unit cap) |
| 2 | 43.0 | 15, 16 | 27.80, 12.86 | 0.89, 1.19 | complete, caught_up |

- One breather, released on walls at 20.0 s (0.015 / 0.007 s).
- `caught_up` was asserted: cursor gone, watermark = target.
- SUM ratio 1.000000.
- The args row shows `"censusMode":"report"`, and the census-mode header
  row renders (0 of 0 rule (3) readings, as expected on a local target).
- Exit 0.
- **Clone restored:** watermark 2026-08-20 00:13:37.208343, no cursor, no
  crawl row, session clear, 0 backends.

## 3. Item 5 — not launched

**Launch gates, re-read at 00:17:44 UTC. All true:**
- (i) 7ee63e55 on `origin/main`, CI green;
- (ii) D2 `caught_up`, with `censusMode: report` in the args row;
- (iii) cursor `[1..12]`, crawl row absent, watermark unmoved;
- (iv) interlock clear, live_writers [], 0 bootstrap backends;
- (v) held=false;
- and `prod_op_gate(1200)` was ok at 00:17:47.

**Why not launched anyway.** The census the runner needs was still dark:
`logs.all` answered 410 at 00:17:40. The runner reads that census in three
places:
- **On a gate-ok poll**, `censusHalf()` runs `runCensus(60)`. Exit 2 means
  `ok: false`, so the window never opens. With `--max-wait-minutes 720` the
  result is a 12-hour wait, one dead Logs API call on every poll where the
  gate is ok, and exit 7.
- **Before CALL 1**, exit 2 is dark #1.
- **Before CALL 2**, it is dark #2, which trips rule (3) in both modes.

So there is no path to `caught_up` under this prompt's rules. The runner
was not launched: no pid, no log, no prod receipt. Item 6 (Pattern B) and
item 7 (attribution) follow from that and did not run. See
`stopped_items`.

## 4. Item 7 — the visitor cost of one paced CALL

**Not measured.** No CALL ran. The instrument the measurement depends on,
the census and its `--by query` / `--by application_name` attribution, is
the removed endpoint. The number Craig asked for waits on FIX-1219.

**A substitute exists but was out of scope.** Cloudflare analytics
(`cf-analytics.mjs`) still sees the website edge. It cannot see 57014s,
and 08-31 showed it cannot see a data-front-door wedge either.

## 5. FIX status, commits, CI

- **Status:**
  - FIX-1218 **closed** (23adb556, prod-only);
  - FIX-1219 **filed**, open;
  - FIX-1212, 969 and 1216 open.
- **Open: 58 → 61.** That is +1219 −1218 here, and +1220, 1221, 1222 from
  cc-153's 347db426. The prompt expected 57 for this run (one close); the
  unplanned FIX-1219 filing makes it 58.
- **Commits:** 9f7462ab → b6fda36f.
  - Push 1 at **23:53:53 UTC**: 7ee63e55, 0d96d726, 23adb556.
  - Push 2 at **00:19:09 UTC**: 56a0fad5, 6e7d106b, b6fda36f.
- **cc-153's pushes as seen from `git log`** (worktree `fix-1194`):
  - 347db426 (FIX-1220/1221/1222 filings), committed 00:01:07 UTC, landed
    between my two pushes;
  - f9b9b43c (FIX-1220 / FIX-1194, `feat(db)`) and 5a4e4072 (FIX-1125),
    committed 00:19:32–33 UTC, landed after my push 2.
  - No claim existed at any point in this session, so none of them
    overlapped one.
- **CI:**
  - `tests.yml` 36074988778 green on 23adb556 (all seven checks);
  - push 2 is docs-only, so paths-ignored by `tests.yml`;
  - the run `cc:verify` found in progress (36077145989, on 5a4e4072) is
    cc-153's, not this run's;
  - the pre-push `fixes:check:trunk` was clean both times.

## 6. What contradicted cc-151, the receipt, or this prompt

**1. The instrument, not the box.** The prompt's design protects the
census on three fronts:
- report mode for its FAIL;
- rule 164 for its dark;
- the gate half for the window.

All three assume the census reads the Logs API. Supabase removed the
endpoint between 10:00 and 10:15 UTC on 09-24, about 40 min after cc-151's
last census answered (09:34:11). Nobody noticed for 13.5 h, because:
- the census runs only when a session runs it;
- the only scheduled consumer, front-door-watch, turns "no rows" into
  zeros and then into `ok`.

That silent `ok` is the second half of FIX-1219, and arguably the more
urgent one. It is a paging watchdog that has been reporting healthy while
its corroborator is blind. Its direct probe still pages on a front door
with no answer. It no longer sees the 08-31 wedge shape.

**2. Did windows 13–16 cost what 3–12 did? Unknown on prod.** On the
clone, the same four windows cost several times what cc-151's clone
rehearsal measured:

| window | stage s, this run | stage s, cc-151 clone |
|---|---|---|
| 13 | 54.9 | 4.3 |
| 14 | 39.5 | 14.7 |
| 15 | 27.8 | 5.5 |
| 16 | 12.9 | 4.5 |

That is most likely a cold cache after the container restart (item 6).
Neither column is prod's number (cf. `reference_clone_understates_scan_bound_costs`).

**3. Did the breather release on walls?** On the clone, yes, at 20.0 s. On
prod, not measured.

**4. Did the fallback nightly stand down cleanly? Yes.** One wording in the
prompt did not hold: "every downstream job `skipped`". The fallback's
`receipts` job **ran**, and pushed 99a18085. FIX-1218 designs that
(nightly.yml:604-612): the fallback's receipt is "the only file that sees
BOTH runs". It lists 36073983772 as `already_ran (stood down)`. Every
downstream PHASE job skipped and no phase re-ran (0 `nightly_cron` rows),
so I treated the point as holding and filed nothing. If Cowork meant
"every job", it is the prompt that is wrong, not the workflow.

**5. `evaluateCensus` has two call sites, not three** (read 3). The gate
half is a separate verdict. D1 kept it a hold in both modes. The alternative
reading — report mode also opens the window on a failing 60-min census —
would launch into a front door already worse than baseline, and that cost
is not this op's.

**6. The clone needed ~55 min of watchdog history.** The local DB
container had been restarted at ~23:20 UTC (not by this session), so the
gate's (d) floor of 28 runs in 60 min held the rehearsal until 00:14.
- Harmless: the runner waited in code (rule 170).
- A rehearsal right after a Docker restart should expect it.
- The rehearsal held the LOCAL claim 00:14:19–00:16:59. cc-151 §5.8 found
  that any cc-153 `prod-session` DB test run in that window would have
  failed on it. I cannot see whether cc-153 ran one; if its data suite shows
  5 `prod-session` failures stamped in that window, this is why.

**7. The FIX-1216 clock is now the binding constraint.** The cursor's
`started_at` 2026-09-23 09:02:48 dies at 09-30 09:02. Tuesday 15:00 is
inside that window; the Tuesday after (10-06) is not. If Tuesday's firing
fails or is stopped, the cycle restarts from window 1.
