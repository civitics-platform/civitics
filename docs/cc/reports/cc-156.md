---
cc: 156
prompt: cc-prompt-156-fix1221-retention-in-order-fix1222-burst-weight-logs-schema-removal-reads-unavailable.md
started_at: 2026-09-25T03:06:32Z
finished_at: 2026-09-25T05:10:00Z
head_before: 4188273b
head_after: ad106814
commits:
  - {sha: 245a4022, subject: "docs(fixes): file FIX-1223 (cc-156: the canary's derived rollup registry has timed out on every run since 2026-09-08 and watched a 1-entry fallback)"}
  - {sha: 173a301f, subject: "feat(db): FIX-1221 — list_scheduled_rollup_pipelines takes its era boundary from cron_job_first_seen and correlates over the retention window; cron-job-run-details-retention (daily 01:51, 42 d) + budget row; FIX-1222 — the startup-timeout burst weighs each failure at the */2 cadence; FIX-1223 — the registry's correlation joins through a shared minute (35.7 s -> 0.43 s)"}
  - {sha: 5dab7e84, subject: 'fix(db,data,app): FIX-1219 hardening — a Logs answer of 200 + "does not exist" is unavailable, not dark; the detail names the field'}
  - {sha: f1b185bb, subject: "docs(fixes): FIX-1221 / 1219 / 1194 appends (cc-156: the retention is 42 days at 01:51 and closes on its first firing; a removed Logs column reads unavailable; the retention answers cc-149's finding 2)"}
  - {sha: 3c6c3503, subject: 'chore(fixes): sync status after FIX-1222'}
  - {sha: e174cfe3, subject: 'chore(canary): FIX-1223 receipt — the 05:00 canary read the derived rollup registry again (52 pipelines) for the first time since 2026-09-08'}
  - {sha: ad106814, subject: 'chore(fixes): sync status after FIX-1223'}
fixes_closed: [FIX-1222, FIX-1223]
fixes_filed: [FIX-1223]
fixes_reopened: []
migrations_pushed: [supabase/migrations/20260920160000_fix1221_1222_1223_registry_era_retention_burst_weight.sql]
prod_writes: "One migration, 20260920160000, applied to Pro at 03:47 UTC through wait-for-gate (gate OK on its first poll at 03:47:03, held=false at 03:46:55, census PASS at 03:46:33). wait-for-gate claimed the FIX-950 session, pushed and released. The migration redefined list_scheduled_rollup_pipelines() (+45/-8, adding proconfig enable_nestloop=off) and check_cron_job_health() (+15/-0), re-asserted both functions' grants and COMMENTs, scheduled cron-job-run-details-retention (prod jobid 54, 51 1 * * *, DELETE FROM cron.job_run_details WHERE start_time < now() - 42 days; it has not fired yet, first firing 2026-09-26 01:51 UTC), and upserted its cron_job_budget row (300 s). No DELETE ran by hand. Pushes to main, each a Vercel deploy: ~03:54, ~03:59 (5dab7e84 changed /api/cron/front-door-watch; Vercel success 04:01:20), ~04:00, ~04:01, ~05:02 (x2) and this report. The 05:00 canary's own record_cron_jobs_seen() wrote the new job's cron_job_first_seen row (05:00:43); this session wrote nothing there. Every other prod contact was a read: db-query --prod (EXPLAIN ANALYZE, pg_get_functiondef, inline reads of both registry bodies), prod_op_gate(), PostgREST RPC calls of list_scheduled_rollup_pipelines as service_role (x2), Logs API calls (7 census readings of 2 calls each, and 1 scratch call naming a removed column), and gh."
ci: green
stopped_items:
  - "Item 3, FIX-1221's first-firing receipt: the job first fires at 2026-09-26 01:51 UTC, outside this session. FIX-1221 stays open, and its bullet names the slot and what the receipt reads (rows older than 42 d before and after, and the job's job_run_details wall)."
---

# cc-156: retention lands in the stated order, the burst weighs the probe at half, a removed Logs column reads unavailable — and the registry this was protecting had been dead since 09-08

**Summary.**
- **Built and live on Pro** (migration `20260920160000`, 03:47 UTC, behind the gate):
  - **FIX-1221 (a)+(b)+(c).** The FIX-1135 era boundary now comes from `cron_job_first_seen`. Correlation reads both sides over 42 days. `cron-job-run-details-retention` (prod jobid 54) deletes rows older than 42 d daily at **01:51** UTC, with a 300 s budget row. FIX-1221 stays **open** until the job's first firing, **2026-09-26 01:51 UTC**.
  - **FIX-1222.** The startup-timeout burst now weighs each failure by `least(1, interval/2)`. Replayed on prod: **64 of 64** paging buckets identical. **Closed** (`local + prod`).
  - **FIX-1223 (new, found here).** The canary's derived rollup registry had timed out on every run since **2026-09-08 09:11 UTC**, at 35.7 s against the 8 s role timeout. For 17 days it silently fell back to a 1-entry literal. The correlation join now goes through a shared minute: **35.7 s → 0.35 s**, with byte-identical output. **Closed** on its receipt: the 05:00 canary (run 36096724716) logged `rollup registry: 52 pipeline(s)`, escalated nothing and reported 7 (the 6 predicted plus the known orphan).
- **Item 2 (FIX-1219 hardening, deployed 04:01 UTC).**
  - A Logs answer of 200 + `Field/Table "…" does not exist.` is now `unavailable` (status 200, detail names the field), not `dark`.
  - Checked live: one scratch call naming `source_name` → census exit 8, `unavailable — Logs API schema changed: Field "source_name" does not exist. (FIX-1219)`.
  - Also fixed: the census verdict path's `process.exit`, which exited **127** on a PASS at 03:55:12 (a consumer reads that as dark).
- **Filed 1 (FIX-1223). Closed FIX-1222 and FIX-1223. Open: 58 → 57** (+1 filed, −2 closed; FIX-1221 open until its first firing).

**Push times (UTC):**

| event | UTC | `held` read just before |
|---|---|---|
| DDL `20260920160000` | gate OK 03:47:03 (first poll), claimed, pushed, released | false @ 03:46:55.343 |
| push #1 (`245a4022` + `173a301f`) | ~03:54:20 | false @ 03:54:00.765 |
| push #2 (`5dab7e84`, item 2 — Vercel success 04:01:20) | ~03:59:40 | false @ 03:59:28.961 |
| push #3 (`f1b185bb`, appends) | ~04:00:50 | false @ 04:00:40.286 |
| push #4 (`3c6c3503`, sync) | ~04:01:10 | false @ 04:01:07.918 |
| push #5 (`e174cfe3`, FIX-1223 receipt) | ~05:02:00 | false @ 05:01:51.269 |
| push #6 (`ad106814`, sync) | ~05:02:25 | false @ 05:02:18.803 |
| push #7 (this report) | after 05:10 (a file cannot carry its own push time) | read before the push |

---

## 1. Reads

**Read 1** (03:06–03:08 UTC).
- `git pull`: `4188273b`, as expected.
- `session:check`: **58 open**. The standing `chore/refresh-disposable-emails` PR and branch, 3 reopened (pre-existing). CLI 2.78.1 matches the pin.
- `tests.yml` has seven checks: `typecheck` · `lint` · `check:reads` · `check:render-timeouts` · `check:workflow-parity` · `check:proconfig` · unit tests (`@civitics/data`, `@civitics/app-civitics`, `@civitics/db`). Latest on main green.
- `.env.local` → local, in both the primary and the worktree.
- `fixes:status`: 1221 open, 1222 open, 1219 closed (`7e153bec`), 1135 closed, 1150 closed, **1073 closed** (so no 1073 append; 1222 carries it), 1194 open.
- Prod `schema_migrations` head: **`20260920150000`**. The dry run later listed exactly one pending migration.

**Read 2 — `list_scheduled_rollup_pipelines()` on prod** (`pg_get_functiondef`, 03:08; 224 lines, identical to `20260903000000` and to local).
- `v_since := now() - make_interval(days => p_lookback_days)` (90). It bounds `all_runs` (data_sync_log), `job_counts` and `by_time`.
- `by_time`: `JOIN cron.job_run_details d ON r.started_at BETWEEN d.start_time - interval '90 seconds' AND COALESCE(d.end_time, d.start_time) + interval '90 seconds' WHERE d.start_time >= v_since`.
- `driver_start`: `min(rd.start_time) AS first_firing FROM drivers d JOIN cron.job j ON j.jobid = d.jobid AND j.active JOIN cron.job_run_details rd ON rd.jobid = d.jobid` — all history.
- `v_min_support int := 4`. It applies **only** when `cadence_source = 'observed_median'`.
- The cadence vocabulary is `override` | `cron_schedule` | `observed_median` | `default`.
- Run with no argument (03:09): **52 pipelines, 1 orphan** (`official_vote_stats_rebuild`). Timed **35,709 ms**, 3.27M shared hits (EXPLAIN ANALYZE, 03:11) — see §5.1.

The 15 era boundaries (`driver_start`) are in read 3. The weekly members, and their closures per window (`data_sync_log`, `status='complete'`, gaps ≥ 0.5 h — the rows the function itself counts):

| pipeline | driver · cadence source | 30 d | 35 d | 42 d | 45 d |
|---|---|---|---|---|---|
| agencies_hierarchy, agency_leadership, congress_committees, courtlistener, edgar, elections, littlesis, openstates, opm_fte, plum_book, tag_ai | GHA · observed (~167 h) | 4 | 5 | 6 | 6 |
| usaspending_bulk, usaspending_bulk_assistance | GHA · observed (~168 h) | 5 | 5 | 6 | 7 |
| contract_flow_rollups_rebuild | unknown · observed (168 h) | 5 | 5 | 6 | 7 |
| irs990 | GHA · observed (190.8 h) | 3 | **4** | **5** | 5 |
| agency_staffing_rollup_refresh | pg_cron · cron_schedule 168 | 3 | 3 | 3 | 3 |
| donor_party_rollup_refresh | pg_cron · cron_schedule 168 | 1 | 1 | 1 | 1 |
| group_donor_rollup_refresh | pg_cron · cron_schedule 168 | 3 | 3 | 3 | 3 |
| treemap_individuals_global_refresh | unknown · cron_schedule 168 | 3 | 3 | 3 | 3 |

**Read 3 — `cron_job_first_seen` on prod** (03:13). All **42** `cron.job` rows (active and inactive) have a ledger row, and the ledger has no row a job lacks.

| ledger vs `min(start_time)` | jobs |
|---|---|
| equal (the 09-05 backfill) | 36 |
| ledger **earlier** (created before first firing) | `group-donor-rollup-refresh` 09-07 23:55:34 vs 09-09 03:10; `platform-counts-daily` 09-05 01:30:53 vs 03:53 |
| ledger **later** (the canary recorded it after the first firing) | `box-health-probe` (+53 min), `vm-probe-pre-ec/fe/fr` (09-21 10:15:47 vs 02:55–04:45). None is a registry driver. |

- The recorder is `record_cron_jobs_seen()`. The canary calls it right before `check_cron_job_health`, and it inserts every active job it hasn't seen yet. So "no row" exists only between a job's creation and the next canary run.
- A job with no row gets its oldest *retained* firing (see §2).

Era boundaries, before → after, from the same inline read on prod at 03:30:

| pipeline | before (min over all history) | after | |
|---|---|---|---|
| 13 pipelines (agency_staffing, donation_edge_orphan_sweep, donor_party_orphan_sweep, donor_party_rollup_refresh, donor_rollup_orphan_sweep, donor_rollup_refresh, ecs_orphan_sweep, entity_connections_rebuild, fe_totals_reconcile, fe_totals_refresh, refresh_derived_mvs, run_rule_taggers, treemap_global) | — | **identical to the microsecond** | |
| group_donor_rollup_refresh | 09-09 03:10:00 | 09-07 23:55:33 | earlier |
| platform_counts_daily | 09-05 03:53:00 | 09-05 01:30:52 | earlier |

**No verdict flipped.** The old body and the new returned **byte-identical JSON** on prod (§2). Both moved pipelines are `cron_schedule`-cadenced, and no closure falls in either moved interval. Read 3 predicted exactly these two moves and no verdict change.

**Read 4 — `cron.job_run_details` now** (03:15 UTC, prod).
- **65,290 rows**; oldest `2026-06-29 08:00:00`.
- Heap 10,551,296 B (total 12,124,160).
- `n_dead_tup` 0; last autovacuum 09-22 22:33.
- Rows older than: 30 d **16,475** · 35 d **9,180** · 42 d **986** · 45 d **224**.
- Rows/day: 09-22 1,486 · 09-23 1,613 · 09-24 2,930 (probe live from 02:02, 1,318 probe rows) · 09-25 415 in 195 min (196 probe) → **~3,065/day**.
- The probe's one scan (`record_box_health`'s 1-day `MATERIALIZED` read): **20.1–20.3 ms**, 1,288 buffers, three runs.
- `cron.job` around 01:50 (rule 143, re-read 03:13; unchanged since cc-153 read 8):
  - `officials-vacuum-analyze` Mon 01:30;
  - `dpr-vacuum-analyze` Sun/Wed 02:00;
  - `refresh-derived-mvs-weekly` Tue 00:47;
  - the `*/1` probe, both `*/2` watchdogs, `ec-crawl` `*/15`, `fe-crawl` `*/30`.
- `check_cron_job_escalations` reads 336 h, inside any N. Left alone.

**Read 5 — the burst and FIX-1220's interval** (`20260920150000:260-277` and `:221-231`, identical to prod):

```sql
cadence AS (
  SELECT jobid,
         ROUND((percentile_cont(0.5) WITHIN GROUP (ORDER BY gap_min))::numeric, 1) AS interval_minutes
  FROM (SELECT jobid, EXTRACT(epoch FROM (start_time - lag(start_time) OVER (PARTITION BY jobid ORDER BY start_time))) / 60.0 AS gap_min
        FROM hist) g
  WHERE gap_min > 0 GROUP BY jobid),
...
buckets AS (SELECT date_trunc('hour', d.start_time) AS bucket, count(*) AS n, … WHERE … 'startup timeout' GROUP BY 1),
burst AS (SELECT … FROM buckets WHERE n >= v_burst_m)
```

cc-153's replay query was not saved anywhere in the repo, so it was rebuilt. The rebuild reproduces cc-153's baseline exactly (64 buckets on 13 days), which is the check that it is the same replay.
- For every hourly bucket with a startup timeout, it judges the bucket as the canary would at `T = bucket + 1 h`.
- Each job's interval is the median gap over the trailing 52 h, the same expression as above.
- Run on prod 03:17 over all retained history (06-29 →).

| | buckets ≥ 10 | paging days |
|---|---|---|
| runs (today) | 64 | 13 (08-17, 08-18, 08-19, 08-24, 08-25, 08-26, 08-29, 08-31, 09-01, 09-07, 09-14, 09-17, 09-22) |
| weighted `least(1, interval/2)` | **64** | **13** |

- The weighted count equals the run count **in every one of the 64 buckets**.
- The minimum interval of any failing job in any bucket was 2.0.
- No bucket held a sub-2-minute failing job: **the probe has had zero startup timeouts**.
- **64/64 identical.**

**Read 6 — the classifier and its consumers** (`supabase-logs.ts:255-285` before the change).
- `queryLogs` sent a 200 with `error` to `dark` ("Logs API 200 with an error: …").
- Only 410/404 were `unavailable`.

The places that print the unavailable wording:
- **census:** `unavailableSummary(status)` → `unavailable — Logs API endpoint removed (${status}; FIX-1219)`. It printed the status, but "endpoint removed" was fixed text. The help text read `8 unavailable (the endpoint is gone — 410; FIX-1219)`.
- **runner:** `censusSummary(8, j)` → `unavailable (FIX-1219 — Logs API endpoint removed, HTTP ${http_status})`. The wording was fixed at the runner side; a 200 would have printed "endpoint removed, HTTP 200".
- **watch:** `corroboratorLabel` → `unavailable (${c.status}, FIX-1219)`; the route built `{kind, status}` with no detail.

Grep for other hard-coded "410" printers found only comments and the tests that pin cc-154's wording (`canary-front-door.test.ts`, `cancellation-census.test.ts`, `donor-party-bootstrap-runner.test.ts`, `front-door-verdict.test.ts`). `canary-front-door.ts` prints the stored `logs_api` string, so it inherits whatever the watch writes.

## 2. Item 1 — the migration

**N = 42 days.**
- **Lower bound.** 42 is the smallest window where every weekly pipeline on an observed cadence has `v_min_support` = 4 gaps. `irs990` (190.8 h) binds it: 4 closures at 35 d, 5 at 42 d. At 42 d every ~168 h weekly also keeps 4 gaps after one lost firing (6 closures).
- The four pg_cron weeklies can't reach 4 gaps inside any N ≤ 45 d (1–3 closures, from holds and Tuesday losses). They are on `cron_schedule`, where support isn't tested.
- **Upper bound.** The probe's scan is 20.2 ms at 65,290 rows. At ~3,050/day, 42 d holds ~128k rows: **~40 ms** for the probe and **~46 ms** for `prod_op_gate` (d) (23.3–26.2 ms at 64.9k, cc-153). Both are under ~50 ms. At 45 d the gate reaches ~50–52 ms.

**`list_scheduled_rollup_pipelines()`: +45 / −8** against prod's `pg_get_functiondef`:
- `v_corr_since := GREATEST(v_since, now() - make_interval(days => 42))`. `runs`, `job_counts` and the firings all read from it.
- `firings AS MATERIALIZED` (each firing's `[start − 90 s, end + 90 s]`) → `firing_minutes AS MATERIALIZED` (one row per minute it touches) → `by_time` joins `ON d.minute = date_trunc('minute', r.started_at) AND r.started_at BETWEEN d.lo AND d.hi`.
- `driver_start` → `LEAST(min(fs.first_seen_at), min(rd.start_time))`, with LEFT JOINs to the ledger and the run rows.
- Proconfig `SET enable_nestloop TO 'off'` added. STABLE, SECURITY DEFINER and `search_path` are unchanged; grants and COMMENT re-asserted.

**`check_cron_job_health()`: +15 / −0**:
- `sum(LEAST(1, COALESCE(c.interval_minutes, 2) / 2.0)) AS weighted_n` in `buckets`, with `LEFT JOIN cadence c`.
- `AND weighted_n >= v_burst_m` added under the unchanged `WHERE n >= v_burst_m`. Since `weighted_n ≤ n`, this equals `weighted_n >= 10` alone.
- `'weighted_count'` on each burst element.

**Equivalence on prod** (inline reads of both bodies; a read-only transaction cannot create a `pg_temp` function):

| read (UTC) | result |
|---|---|
| 03:28–03:30 old body vs new body | **byte-identical** JSON: 52 pipelines, same `drivers`, same orphans |
| new body with `cron.job_run_details` cut to 42 d | identical again (purge-invariant) |
| after the push, 03:47–03:48: deployed function, old inline body, deployed function | all three identical |

Timings of the registry:

| form | ms (prod) |
|---|---|
| old body (the function as it was) | **35,709** (EXPLAIN ANALYZE 03:11); the old inline read took ~65 s at 03:28 |
| the prompt's form: a 42-day window, the same range join | **89,583** |
| minute join, planner's choice, no GUC | 447 / 475 |
| minute join + `enable_nestloop off` (built) | 422 / 427 / 431 inline; **deployed 642 (cold) / 356 / 346** |
| **through PostgREST as service_role (the canary's path)** | **1,470 / 648 ms, HTTP 200, 52 pipelines** |

**The push.**
- Dry run from the worktree: one pending migration.
- Census 03:46:33 over 60 min: 57014 = 1 (03:03), rate 0.0167/min vs baseline 0.033/min, ratio 0.51×, P99 floor 6 → PASS. Edge: last closed bucket 03:30, 0 of 514 = 0.00 % → PASS. Exit 0.
- `held=false` @ 03:46:55.343, no live writers.
- Then `wait-for-gate.ts --expected-minutes 2 --reason "cc-156 …" --then "node <primary>/scripts/db-push-prod.mjs --workdir <worktree>"`, from the primary's `packages/data` with its `.env.local.prod`.
- Gate reading `03:47:03Z OK (expected 120 s, span to 03:51Z) watchdogs max 0.089/0.038 s`. It held 16 guarded pipelines, applied, cleared and released; `--then` exited 0.
- Pro head is now `20260920160000`.

**On Pro after:**
- `cron.job` jobid **54** `cron-job-run-details-retention`, `51 1 * * *`, active, `postgres`, command `DELETE FROM cron.job_run_details WHERE start_time < now() - make_interval(days => 42)`.
- `cron_job_budget` 300 s.
- Proconfig: `{search_path=…, enable_nestloop=off}` / `{search_path=…}`. ACLs unchanged (`postgres=X`, `service_role=X`).
- Both bodies are 0 lines from local.
- `check_cron_job_health()`: the same 8 top keys and the same 5 tier keys, burst `[]`, 257.5 ms.
- Rule 126: the 300 s budget sits far under the 9,836 s a budget may reach below the 3 h `postgres` ceiling. The largest active budget is still 9,000.

**The first firing** is 2026-09-26 01:51 UTC, outside this session. FIX-1221 is open and its append names the slot and the receipt.

## 3. Item 2 — the classifier

| Logs answer | kind | detail |
|---|---|---|
| 410 / 404 | `unavailable` | `Logs API 410 Gone` (unchanged) |
| 200 + `Field "source_name" does not exist.` | **`unavailable`, status 200** | `Logs API schema changed: Field "source_name" does not exist.` |
| 200 + `Table "edge_logs" does not exist.` | **`unavailable`, status 200** | `Logs API schema changed: Table "edge_logs" does not exist.` |
| 200 + `Backend error` / `Backend error! Retry…` / any other `error` | `dark` | `Logs API 200 with an error: …` (unchanged) |
| 429 | `dark` | `… (rate limit 10/min; retry-after Ns)` (unchanged) |
| 5xx, throw, no `result`, the 1000-row cap | `dark` | unchanged |

`isLogsSchemaRemoval` matches `(field|table|column) "…" does not exist`. "Project does not exist" stays dark.

Consumers, new wording for a 200. The 410/404 wording is unchanged everywhere:
- census exit 8: `unavailable — Logs API schema changed: Field "source_name" does not exist. (FIX-1219)`. `--json` gains `detail`.
- runner `censusSummary(8)`: `unavailable (FIX-1219 — Logs API schema changed: Field "source_name" does not exist.)`. It still opens the gate half like any exit 8.
- watch `corroboratorLabel`: `unavailable (Logs API schema changed: Field "source_name" does not exist., FIX-1219)`. The route passes `detail` through, and the verdict is still `corroborator_unavailable`.

**Checked against prod, 03:55:04 UTC:** one real call `SELECT source_name FROM logs WHERE source = 'edge_logs' AND {{time_filter}} LIMIT 1`, through the worktree's code, printed each line above verbatim. Census exit 8.

A normal reading, 03:55:12, from the worktree's census: VERDICT PASS, then `Assertion failed: !(handle->flags & UV_HANDLE_CLOSING)`, **exit 127**.
- This is pre-existing. The verdict path was the one path cc-154/155 left on `process.exit(...)`.
- It is now `process.exitCode`. Readings at 03:55:36 (before the change: exit 0, so it is intermittent), 03:55:56 and 03:56:01 (after) exited 0.
- The census test that called the gate path "fine — it awaits two fetches" now pins it, and fails on the old line (mutation-checked).

**Deployed:** Vercel success 04:01:20. The first post-deploy watch firing, 04:15:28, read `ok | 4 bucket(s)`.

## 4. Status, commits, CI

**`fixes:status`** (05:02):
- FIX-1221 **open**. It closes on the first firing at 2026-09-26 01:51 UTC; the bullet has the append.
- FIX-1222 **closed** (`173a301f`, local+prod).
- FIX-1223 **closed** (`e174cfe3`, local+prod).
- FIX-1219 closed (append). FIX-1194 open (append).
- FIX-1073 closed, so no append.
- **57 open** of 1,210 ids. `fixes:check` is clean after every commit.

**Commits** (all on `origin/main`):

| sha | what |
|---|---|
| `245a4022` | file FIX-1223 |
| `173a301f` | the migration, three test files, the canary's burst text (`Fixes: FIX-1222`) |
| `5dab7e84` | item 2 + the census exit-code fix |
| `f1b185bb` | appends |
| `3c6c3503` | sync |
| `e174cfe3` | FIX-1223 receipt, allow-empty (`Fixes: FIX-1223`) |
| `ad106814` | sync |

**CI** (`tests.yml`), all green:

| run | commit |
|---|---|
| 36092258136 | `173a301f` |
| 36092603856 | `5dab7e84` |

The docs-only and empty commits skip `tests.yml`.

**Local verification:**

| check | result |
|---|---|
| unit suites | data **1004** (997 pass, 7 skipped, 0 fail; was 993); db **251** (was 250); app **321** |
| typecheck + lint | 5/5 turbo tasks, both commits |
| `check:proconfig` | 24 migrations scanned, 0 undeclared; fixtures 18/18 |
| `check:reads` / `render-timeouts` | 0 / 0 |
| `check:workflow-parity` | 0 undeclared |
| app build | pass |

New test files: `cron-startup-burst-weight.test.ts` (3 tests) and `cron-job-run-details-retention.test.ts` (5).

Mutation checks:
- The era fixture on the old body: support 6 → 4 after the purge, so the test catches it.
- The census anchor on the old `process.exit` line: fails.

**The local clone** carries the migration. Its retention job is active (local jobid 86), like `abuse-events-retention` there, and deletes only local history.

## 5. What contradicted cc-153, cc-155 or this prompt

1. **The reader FIX-1221 was protecting had not been reading anything.** The canary's call to `list_scheduled_rollup_pipelines()` has failed on **every run since 2026-09-08 09:11 UTC** with `canceling statement due to statement timeout` (PostgREST's `authenticator` carries 8 s). It logged a `console.warn` and fell back to a **1-entry literal**, so for 17 days the canary checked rollup freshness for one pipeline and nothing paged.
   - The last good run was 09-07 09:47 (51 pipelines). Sampled runs 09-08, 09-09, 09-10, 09-11, 09-15, 09-19, 09-23 and 09-24 (05:00 and 09:35) all fell back.
   - The cost was `by_time`'s range-only join: 2,353 pg_cron runs × 64k firings = **151M comparisons, 35.7 s**.
   - The prompt's step (b), shrinking the window, **does not help**. The crawl arms' runs are all recent, and the probe raised the table's daily rate; its 42-day form measured **89.6 s**.
   - Filed and fixed as **FIX-1223**, in the same redefinition, because (b) is that CTE. Output byte-identical, 0.35 s.
   - Restoring it changes what the canary watches from 1 pipeline to 52. On the 03:1x state that **escalates 0 and reports 6**: `donor_party_rollup_orphan_sweep` 1,310 h, `donor_rollup_bulk`, `legistar`, `sector_affinity_rollup_backfill`, `small_dollar_rollup_backfill`, `treemap_individuals_focused_backfill`, all report-only.
   - `canary-front-door.ts` also names `rollup:front_door_watch` staleness as "the rollup registry's" job. That was dark too until now.
2. **The v_min_support premise mostly doesn't bind.**
   - `v_min_support` is tested only on `observed_median`. Every weekly with an observed cadence is GHA- or unknown-driven, with no driver job, so no era boundary and no `job_run_details` read.
   - Every pg_cron weekly is `cron_schedule`, where support isn't tested.
   - After (a), closures come from `data_sync_log` over the full 90 days, so the table's retention no longer feeds any cadence at all. N now governs only the correlation evidence.
   - I still chose N the prompt's way (42, irs990 binding), as a stated floor. **Can the weekly cadence be satisfied under ~45 d?** For the observed weeklies, yes: 35 d at zero slack, 42 d with irs990 and one lost firing. For the pg_cron weeklies, no (1–3 closures), but it doesn't matter for them.
3. **No FIX-1135 verdict flipped.** Two era boundaries moved earlier (group_donor 1 d 3 h, platform_counts 2 h 22 m), onto their ledger rows. The output was byte-identical, as read 3 predicted.
4. **The weighted burst reproduced 64/64**, and with zero slack against the probe: it has never had a startup timeout, so no historical bucket exercises the 0.5 weight. The clone fixtures do (9 + 2 = 10.0 escalates; 9 + 1 = 9.5 does not, although 10 runs met the old rule).
5. **The slot is 01:51, not 01:50.** Three precedent migrations (FIX-1141, FIX-1129, FIX-1146) raise on an even minute because both `*/2` watchdogs fire on every even minute. The migration carries the same guard.
6. **The new job shows in `missing_daily` until the 05:00 canary records it.** Nothing wrote its `cron_job_first_seen` row, and FIX-1150's exemption needs one. The canary's `record_cron_jobs_seen()` runs before the health RPC, so the canary itself exempts it. Until then the status page (a render, not the escalating consumer) lists it. That is the path FIX-1150 designed as "the SAFE direction". A migration that schedules a daily job could insert its ledger row and skip the window; this one didn't. **The 05:00 canary did exactly that:** the ledger row landed at 05:00:43, and the run was not flagged.
7. **The census gate path could exit 127 on a PASS** (§3). cc-155's test comment said it was safe because it awaits two fetches, and it isn't. Fixed on item 2's commit, because the failure class is the same as item 2's: an instrument state misread as dark. **Did any consumer hard-code the 410 wording where read 6 did not find it?** No. The only other printer is `canary-front-door.ts`, which prints the stored `logs_api` string.
8. **cc-153's replay query was not in the repo.** Rebuilt; it reproduces cc-153's baseline (64 buckets on 13 days).
9. **Environment.**
   - The worktree's `.env.local.prod` is a local stub, so the gate/claim/push and every prod read ran the primary's scripts and env by absolute path. Nothing was written to the primary tree; a probe file I briefly copied into it was removed.
   - An earlier census reading exited non-zero only because my own `| head` closed the pipe (EPIPE). The unpiped reading passed.
   - The prompt header's "~20:10 PDT / 03:10 UTC" matched the clock: the run started 03:06 UTC.
10. **For Cowork — another canary read that has been failing quietly.** Every canary run logs `prod session read failed (non-fatal): buildDbUrl: need SUPABASE_DB_URL, a local NEXT_PUBLIC_SUPABASE_URL, or (NEXT_PUBLIC_SUPABASE_URL + SUPABASE_DB_PASSWORD) to compose a DSN` and `session holds: unknown (read failed)`.
    - It appears in today's 05:00 run and in both 09-24 runs (05:00, 09:35).
    - The canary's prod-session and session-hold detectors read "unknown" every run: the same shape as FIX-1223 (a `console.warn` in place of an instrument). The workflow's env gives it no DSN.
    - Not in FIXES.md or any cc report. Not investigated and not filed (the prompt's filing budget was the one conditional 🟢 S), so it is left as found.
